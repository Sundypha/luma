# Luma (Flutter app)

The **Luma** product app. Dart package name: `luma` (this directory is still `apps/ptrack` in the repo layout).

See the [repository root README](../../README.md) for workspace setup, FVM, and how to run the app.
