---
phase: 09-prediction-of-next-period
plan: 03
type: execute
wave: 3
depends_on:
  - 09-02
files_modified:
  - apps/ptrack/lib/features/calendar/calendar_painters.dart
  - apps/ptrack/lib/features/calendar/calendar_view_model.dart
  - apps/ptrack/lib/features/calendar/calendar_screen.dart
  - apps/ptrack/lib/features/calendar/day_detail_sheet.dart
  - apps/ptrack/lib/features/home/home_view_model.dart
  - apps/ptrack/lib/features/home/home_screen.dart
  - apps/ptrack/lib/features/home/cycle_position.dart
  - apps/ptrack/lib/features/settings/prediction_settings.dart
  - apps/ptrack/test/calendar_painters_test.dart
autonomous: true
requirements:
  - PRED-02
  - PRED-03
  - PRED-04
  - NFR-06

must_haves:
  truths:
    - "Calendar shows 3 distinct confidence tiers with dual visual channels (opacity + hatch density)"
    - "Tapping a predicted day shows 'X of N methods agree on this day' with per-algorithm breakdown"
    - "Home screen shows 'How is this calculated?' link opening layered explanation"
    - "User can switch prediction display mode between consensus-only, show-all, and show-all-with-note in Settings"
    - "Milestone messages appear when new algorithms activate"
    - "Predicted days are distinguishable from logged days without relying on color alone (NFR-06)"
    - "Calendar shows a subtle legend explaining the confidence tiers"
  artifacts:
    - path: "apps/ptrack/lib/features/calendar/calendar_painters.dart"
      provides: "ConfidenceHatchedCirclePainter with 3 tier configs replacing HatchedCirclePainter"
      contains: "ConfidenceHatchedCirclePainter"
    - path: "apps/ptrack/lib/features/calendar/calendar_view_model.dart"
      provides: "CalendarViewModel using EnsembleCoordinator + PredictionSettings"
      contains: "EnsembleCoordinator"
    - path: "apps/ptrack/lib/features/home/home_view_model.dart"
      provides: "HomeViewModel with ensemble data, milestone message, explanation"
      contains: "milestoneMessage"
    - path: "apps/ptrack/lib/features/calendar/day_detail_sheet.dart"
      provides: "Per-day algorithm agreement detail with expandable breakdown"
      contains: "methods agree"
    - path: "apps/ptrack/lib/features/home/home_screen.dart"
      provides: "How is this calculated? link and milestone card"
      contains: "How is this calculated"
    - path: "apps/ptrack/lib/features/settings/prediction_settings.dart"
      provides: "PredictionSettingsTile widget in settings"
      contains: "PredictionSettingsTile"
  key_links:
    - from: "apps/ptrack/lib/features/calendar/calendar_view_model.dart"
      to: "packages/ptrack_data/lib/src/prediction/ensemble_coordinator.dart"
      via: "CalendarViewModel._recompute calls EnsembleCoordinator.predictNext"
      pattern: "EnsembleCoordinator.*predictNext"
    - from: "apps/ptrack/lib/features/calendar/calendar_painters.dart"
      to: "apps/ptrack/lib/features/calendar/calendar_day_data.dart"
      via: "buildCalendarDayCell reads predictionConfidenceTier and passes to ConfidenceHatchedCirclePainter"
      pattern: "ConfidenceHatchedCirclePainter.*tier"
    - from: "apps/ptrack/lib/features/calendar/day_detail_sheet.dart"
      to: "apps/ptrack/lib/features/calendar/calendar_day_data.dart"
      via: "DayDetailSheet reads predictionAgreementCount for 'X of N methods agree'"
      pattern: "predictionAgreementCount"
    - from: "apps/ptrack/lib/features/home/home_view_model.dart"
      to: "packages/ptrack_data/lib/src/prediction/ensemble_coordinator.dart"
      via: "HomeViewModel._recompute calls EnsembleCoordinator.predictNext"
      pattern: "EnsembleCoordinator.*predictNext"
---

<objective>
Wire the ensemble prediction system through the UI layer: confidence-tiered calendar painters, ViewModel ensemble integration, prediction explanation UX on home and day detail, settings for display mode, and milestone messages.

Purpose: Complete the user-facing Phase 9 experience — users see graduated confidence on the calendar (NFR-06), understand how predictions are derived (PRED-03), can adjust display preferences, and encounter non-medical framing throughout (PRED-04).
Output: Updated calendar painters, ViewModels, home screen, day detail sheet, settings tile, confidence legend, and tests.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-prediction-of-next-period/09-CONTEXT.md
@.planning/phases/09-prediction-of-next-period/09-RESEARCH.md
@.planning/phases/09-prediction-of-next-period/09-01-SUMMARY.md
@.planning/phases/09-prediction-of-next-period/09-02-SUMMARY.md

@apps/ptrack/lib/features/calendar/calendar_painters.dart
@apps/ptrack/lib/features/calendar/calendar_day_data.dart
@apps/ptrack/lib/features/calendar/calendar_view_model.dart
@apps/ptrack/lib/features/calendar/calendar_screen.dart
@apps/ptrack/lib/features/calendar/day_detail_sheet.dart
@apps/ptrack/lib/features/home/home_view_model.dart
@apps/ptrack/lib/features/home/home_screen.dart
@apps/ptrack/lib/features/home/cycle_position.dart
@apps/ptrack/lib/features/settings/mood_settings.dart
@apps/ptrack/lib/features/settings/prediction_settings.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: ConfidenceHatchedCirclePainter, ViewModel ensemble wiring, CyclePosition update, and confidence legend</name>
  <files>
    apps/ptrack/lib/features/calendar/calendar_painters.dart
    apps/ptrack/lib/features/calendar/calendar_view_model.dart
    apps/ptrack/lib/features/home/home_view_model.dart
    apps/ptrack/lib/features/home/cycle_position.dart
    apps/ptrack/lib/features/calendar/calendar_screen.dart
    apps/ptrack/test/calendar_painters_test.dart
  </files>
  <action>
**calendar_painters.dart** — Replace `HatchedCirclePainter` with `ConfidenceHatchedCirclePainter`:

1. Create `ConfidenceHatchedCirclePainter extends CustomPainter`:
   - Constructor: `ConfidenceHatchedCirclePainter({required this.tier, this.color = kPeriodColorLight})`
   - `tier` is 1, 2, or 3 (clamped).
   - Tier config map (static const):
     - Tier 1: opacity 0.30, spacing 7.0px, strokeWidth 1.0px (sparse, faint)
     - Tier 2: opacity 0.55, spacing 4.5px, strokeWidth 1.3px (medium)
     - Tier 3: opacity 0.85, spacing 3.0px, strokeWidth 1.6px (dense, strong)
   - `paint()`: Same diagonal-stripe-in-circle technique as existing HatchedCirclePainter but using tier-specific config for opacity (`color.withValues(alpha: config.opacity)`), spacing, and strokeWidth.
   - `shouldRepaint`: compare tier and color.
   - Two independent visual channels (opacity + hatch density) satisfy NFR-06 (no color-only distinction).

2. Keep `HatchedCirclePainter` as `@Deprecated` (for migration safety) or remove if all callers are updated. Prefer removal since Plan 02 already updated CalendarDayData.

3. Update `buildCalendarDayCell()`:
   - Replace `if (data.isPredictedPeriod && !hasPeriod)` block:
     - Check `data.predictionConfidenceTier > 0 && !hasPeriod`.
     - Pass `ConfidenceHatchedCirclePainter(tier: data.predictionConfidenceTier)`.

4. Add `buildConfidenceLegend()` widget function returning a small Row/Wrap:
   - 3 items: each shows a small (16×16) CustomPaint with `ConfidenceHatchedCirclePainter(tier: N)` + label.
   - Tier 1: "1 method", Tier 2: "2 methods", Tier 3: "3+ methods"
   - Compact, subtle styling (bodySmall, onSurfaceVariant color). Use a `Padding` with 8px horizontal.
   - This widget will be placed below the calendar grid in `CalendarScreen`.

**calendar_view_model.dart** — Wire ensemble:

1. Add `EnsembleCoordinator` as a field (create in constructor with default algorithms).
2. Add `PredictionDisplayMode _displayMode = PredictionDisplayMode.consensusOnly`.
3. Add `EnsemblePredictionResult? _ensembleResult` field.
4. Add `int _previousActiveCount = 0` for milestone tracking.
5. In constructor: load `PredictionSettings.load()` asynchronously and store. On load completion, call `_recompute()` + `notifyListeners()`.
6. Update `_recompute()`:
   - Call `_ensembleCoordinator.predictNext(storedPeriods: storedPeriods, calendar: _calendar, previousActiveCount: _previousActiveCount)`.
   - Store `_ensembleResult`.
   - Track `_previousActiveCount = _ensembleResult.activeAlgorithmCount`.
   - Derive `_prediction = _ensembleResult.consensusPrediction` (backward compat).
   - Call `buildCalendarDayDataMap(periodsWithDays: ..., ensemble: _ensembleResult, displayMode: _displayMode, today: ...)`.
7. Add `EnsemblePredictionResult? get ensembleResult => _ensembleResult` (for day detail access).
8. Add `void updateDisplayMode(PredictionDisplayMode mode)` that sets `_displayMode`, calls `_recompute()`, `notifyListeners()`, and `PredictionSettings.save(mode)`.

**home_view_model.dart** — Wire ensemble:

1. Add `EnsembleCoordinator` field and `EnsemblePredictionResult? _ensembleResult`.
2. Add `int _previousActiveCount = 0`.
3. Update `_recompute()`:
   - Run `EnsembleCoordinator().predictNext(...)` instead of `PredictionCoordinator().predictNext(...)`.
   - Store ensemble result.
   - Derive `_prediction = _ensembleResult.consensusPrediction`.
   - Use consensus prediction for `computeCyclePosition(...)`.
4. Expose: `String? get milestoneMessage => _ensembleResult?.milestoneMessage`.
5. Expose: `String get ensembleExplanationText => _ensembleResult?.explanationText ?? ''`.
6. Expose: `int get activeAlgorithmCount => _ensembleResult?.activeAlgorithmCount ?? 0`.

**cycle_position.dart** — Update `computeCyclePosition()`:
- No API change needed — it already takes `PredictionResult prediction`. The ViewModels pass `consensusPrediction` from ensemble result. Verify this works.
- If `PredictionResult` subtypes haven't changed, no code changes needed here. Just verify through test that CyclePosition still computes correctly with consensus result.

**calendar_screen.dart** — Add the confidence legend:
- Below the `TableCalendar` widget (or in the `Column` that contains it), add `buildConfidenceLegend()` with subtle padding.
- Only show legend when `viewModel.ensembleResult?.activeAlgorithmCount > 0` (hide when no predictions).

**calendar_painters_test.dart** — Tests:
1. `ConfidenceHatchedCirclePainter` paints without error for each tier (1, 2, 3)
2. `shouldRepaint` returns true when tier changes, false when same
3. `buildCalendarDayCell` with `predictionConfidenceTier: 2` renders `ConfidenceHatchedCirclePainter`
4. `buildCalendarDayCell` with `predictionConfidenceTier: 0` does NOT render prediction painter
5. `buildConfidenceLegend` renders 3 legend items
  </action>
  <verify>
    <automated>cd apps/ptrack && flutter test test/calendar_painters_test.dart -r expanded && flutter analyze --fatal-infos</automated>
    <manual>Calendar shows 3 visually distinct tier levels; legend appears below calendar; ViewModels correctly consume ensemble</manual>
  </verify>
  <done>ConfidenceHatchedCirclePainter renders 3 distinct tiers with dual visual channels (opacity + density). CalendarViewModel and HomeViewModel use EnsembleCoordinator. Confidence legend shows below calendar. CyclePosition works with consensus prediction.</done>
</task>

<task type="auto">
  <name>Task 2: Day detail prediction info, home explanation UX, milestone messages, and PredictionSettingsTile</name>
  <files>
    apps/ptrack/lib/features/calendar/day_detail_sheet.dart
    apps/ptrack/lib/features/home/home_screen.dart
    apps/ptrack/lib/features/settings/prediction_settings.dart
  </files>
  <action>
**day_detail_sheet.dart** — Update prediction display:

1. Update `_predictionCard()` to show per-day agreement info:
   - Accept `CalendarDayData dayData` and `EnsemblePredictionResult? ensemble` as parameters.
   - Header: "Period expected around this day" (existing).
   - Add subtitle: "{agreementCount} of {activeCount} methods agree on this day." using `dayData.predictionAgreementCount` and `ensemble?.activeAlgorithmCount`.
   - Below subtitle, add expandable section ("See details" text button):
     - When expanded, list each algorithm output from `ensemble?.algorithmOutputs`:
       - "{displayName}: expects around {formatted date}"
     - Only show algorithms that include this day in their predicted range.
   - Footer: "Estimates only — not medical advice." (preserve existing).
   - All text uses "methods agree" framing. Never say "confidence", "certainty", or "accurate" (PRED-04).

2. Update `_buildPredictedFuture()` and `_buildPredictedPastOrToday()`:
   - Pass `dayData` and `viewModel.ensembleResult` to updated `_predictionCard()`.

**home_screen.dart** — Add explanation link and milestone:

1. After the "Next period expected..." text or the insufficient-data text, add:
   - A small `TextButton` (or `InkWell`): "How is this calculated?" with a subtle style (bodySmall, primary color).
   - On tap, show a `showModalBottomSheet` with the full explanation text from `viewModel.ensembleExplanationText`. Structure:
     - Title: "How your prediction works"
     - Body: `Text(viewModel.ensembleExplanationText)` with bodyMedium style.
     - If `activeAlgorithmCount > 0`: "Currently using {N} prediction methods." subtitle.
     - Dismiss button at bottom.
   - If `ensembleExplanationText` is empty (no prediction), don't show the link.

2. Milestone message card:
   - If `viewModel.milestoneMessage != null`, show a subtle `Card` below the TodayCard:
     - Icon: `Icons.auto_awesome` (celebration).
     - Text: the milestone message.
     - Dismissible (tap X to close, or build with a `Dismissible` or `IconButton` close).
     - Persist dismissal: store a `milestone_dismissed_{N}` flag in SharedPreferences (where N = activeAlgorithmCount). Only show if not dismissed for this count.
   - Style: compact, encouraging, non-intrusive. Primary color accent.

**prediction_settings.dart** — Add `PredictionSettingsTile` widget:

1. Follow `MoodSettingsTile` pattern exactly:
   - StatefulWidget loading `PredictionSettings.load()` in `initState`.
   - Show current mode as a `ListTile` with a `DropdownButton` or segmented display.
   - Title: "Prediction display"
   - Subtitle: varies by mode:
     - consensusOnly: "Show days where 2+ methods agree"
     - showAll: "Show all predicted days"
     - showAllWithNote: "Show all with low-confidence note"
   - On change: call `PredictionSettings.save(mode)`.
   - The CalendarViewModel.updateDisplayMode() should be called when the mode changes. Since settings doesn't have direct access to the ViewModel, persist via SharedPreferences and CalendarViewModel reloads on next build. Alternatively, accept an optional `ValueChanged<PredictionDisplayMode>` callback.

2. Integration: The `PredictionSettingsTile` needs to be placed in the Settings screen. Look for the settings screen in the app's drawer/navigation and add this tile near the `MoodSettingsTile`. (Check `about_screen.dart` or drawer code for the settings location.)
  </action>
  <verify>
    <automated>cd apps/ptrack && flutter analyze --fatal-infos && flutter test --reporter expanded</automated>
    <manual>Tap a predicted day → see "X of N methods agree" with expandable detail. Home screen shows "How is this calculated?" link. Milestone card appears when appropriate. Settings has "Prediction display" option.</manual>
  </verify>
  <done>Day detail sheet shows per-day algorithm agreement with expandable per-algorithm breakdown. Home screen has "How is this calculated?" explanation link and milestone message card. PredictionSettingsTile in settings allows switching between consensus-only, show-all, and show-all-with-note modes. All copy uses "methods agree" framing (PRED-04). NFR-06 satisfied with dual-channel tiers.</done>
</task>

</tasks>

<verification>
1. `cd apps/ptrack && flutter analyze --fatal-infos` — zero issues
2. `cd apps/ptrack && flutter test` — all tests pass
3. `cd packages/ptrack_domain && dart test` — all tests pass
4. `cd packages/ptrack_data && dart test` — all tests pass
5. Visual check: 3 distinct confidence tiers visible on calendar
6. Day detail sheet: "X of N methods agree" text appears for predicted days
7. Home screen: "How is this calculated?" link opens explanation
8. Settings: "Prediction display" tile changes display mode
9. grep for forbidden phrases in all new/modified files returns zero matches
</verification>

<success_criteria>
- Calendar renders 3 confidence tiers with distinct opacity AND hatch density (NFR-06)
- Confidence legend below calendar explains tier meanings
- Tapping predicted day shows per-day algorithm agreement and expandable detail (PRED-03)
- Home screen "How is this calculated?" opens layered multi-algorithm explanation (PRED-03)
- Milestone messages celebrate new algorithm activation (per CONTEXT.md)
- PredictionSettingsTile in settings lets user switch display mode
- All new copy uses "methods agree" framing, zero forbidden phrases (PRED-04)
- Logged days remain solid bands — only predicted future days show confidence tiers
</success_criteria>

<output>
After completion, create `.planning/phases/09-prediction-of-next-period/09-03-SUMMARY.md`
</output>
