---
phase: 13-pdf-export
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - apps/ptrack/pubspec.yaml
  - apps/ptrack/lib/features/pdf_export/pdf_section_config.dart
  - apps/ptrack/lib/features/pdf_export/pdf_report_data.dart
  - apps/ptrack/lib/features/pdf_export/pdf_data_collector.dart
  - apps/ptrack/test/features/pdf_export/pdf_data_collector_test.dart
autonomous: true
requirements: [PDF-01, PDF-02, PDF-04, PDF-05]

must_haves:
  truths:
    - "PdfSectionConfig captures which report sections are enabled/disabled"
    - "Three presets (summary, standard, full) produce correct section combinations"
    - "PdfDataCollector filters periods by date range and computes accurate statistics"
    - "Statistics include cycle count, average cycle length, average period duration, flow/pain/mood distributions"
    - "Empty or out-of-range data produces a valid empty report (metadata only)"
  artifacts:
    - path: "apps/ptrack/lib/features/pdf_export/pdf_section_config.dart"
      provides: "Section enum, config model, preset factory, persistence"
      exports: ["PdfSection", "PdfSectionConfig", "PdfExportPreset"]
    - path: "apps/ptrack/lib/features/pdf_export/pdf_report_data.dart"
      provides: "Structured report data model with stats and day-level data"
      exports: ["PdfReportData", "CycleStatsSummary", "DaySummaryRow", "NoteEntry"]
    - path: "apps/ptrack/lib/features/pdf_export/pdf_data_collector.dart"
      provides: "Queries repository and computes report data for a date range"
      exports: ["PdfDataCollector"]
    - path: "apps/ptrack/test/features/pdf_export/pdf_data_collector_test.dart"
      provides: "TDD tests for data collection and stats computation"
      min_lines: 80
  key_links:
    - from: "apps/ptrack/lib/features/pdf_export/pdf_data_collector.dart"
      to: "packages/ptrack_data/lib/src/repositories/period_repository.dart"
      via: "PeriodRepository.watchPeriodsWithDays() snapshot or listOrderedByStartUtc()"
      pattern: "StoredPeriodWithDays"
    - from: "apps/ptrack/lib/features/pdf_export/pdf_section_config.dart"
      to: "shared_preferences"
      via: "SharedPreferences for persisting user's section choices"
      pattern: "SharedPreferences"
---

<objective>
Add `pdf` and `printing` dependencies, create the PDF section configuration model (presets + per-section toggles + persistence), the report data model, and a TDD-driven data collector that queries period data for a date range and computes summary statistics.

Purpose: Establish the core data pipeline for PDF generation — models that downstream plans (document builder, UI) depend on. TDD ensures stats accuracy before building the visual layer.

Output: Section config model with presets, report data model, tested data collector.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-pdf-export-of-period-statistics-and-details-user-selectable-if-all-or-none-goal-is-to-have-a-pdf-ready-for-a-physician-or-gynecologist/13-CONTEXT.md
@apps/ptrack/pubspec.yaml
@packages/ptrack_domain/lib/src/period/period_models.dart
@packages/ptrack_domain/lib/src/period/logging_types.dart
@packages/ptrack_domain/lib/src/period/cycle_length.dart
@packages/ptrack_data/lib/src/repositories/period_repository.dart
@packages/ptrack_data/lib/ptrack_data.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deps and create section config + report data models</name>
  <files>
    apps/ptrack/pubspec.yaml
    apps/ptrack/lib/features/pdf_export/pdf_section_config.dart
    apps/ptrack/lib/features/pdf_export/pdf_report_data.dart
  </files>
  <action>
    **pubspec.yaml:** Add `pdf: ^3.11.1` and `printing: ^5.13.4` to dependencies. Run `flutter pub get` in the app directory.

    **pdf_section_config.dart:** Create:
    - `enum PdfSection { overviewStats, cycleHistory, cycleChart, daySummaryTable, notesLog }` — the five toggleable sections.
    - `enum PdfExportPreset { summary, standard, full }` with a factory that returns the enabled section set:
      - `summary` → `{overviewStats}`
      - `standard` → `{overviewStats, cycleHistory, daySummaryTable}`
      - `full` → all five sections
    - `@immutable class PdfSectionConfig` with fields:
      - `Set<PdfSection> enabledSections`
      - `DateTime rangeStart` (inclusive)
      - `DateTime rangeEnd` (inclusive)
      - Helper: `bool isEnabled(PdfSection s)`, `bool get hasAnySections`
      - Factory: `PdfSectionConfig.fromPreset(PdfExportPreset preset, {DateTime? rangeStart, DateTime? rangeEnd})` — uses preset's sections, defaults rangeEnd to today and rangeStart to 12 months ago.
      - `copyWith(...)` method.
    - Static persistence methods using SharedPreferences:
      - `static Future<PdfSectionConfig> loadSaved()` — reads stored sections + range; returns full preset if no stored value (first-time default per CONTEXT.md).
      - `static Future<void> save(PdfSectionConfig config)` — persists section set. Store as comma-separated section names under key `pdf_export_sections`.

    **pdf_report_data.dart:** Create:
    - `@immutable class CycleStatsSummary` — `int cycleCount`, `double? avgCycleLengthDays`, `double? avgPeriodDurationDays`, `int? shortestCycleDays`, `int? longestCycleDays`.
    - `@immutable class FlowDistribution` — `Map<FlowIntensity, int> counts` (how many days at each level).
    - `@immutable class PainDistribution` — `Map<PainScore, int> counts`.
    - `@immutable class MoodDistribution` — `Map<Mood, int> counts`.
    - `@immutable class DaySummaryRow` — `DateTime dateUtc`, `FlowIntensity? flow`, `PainScore? pain`, `Mood? mood`, `bool hasNotes` — one row per day in the date range that has data.
    - `@immutable class NoteEntry` — `DateTime dateUtc`, `String notes` — only days with non-empty notes.
    - `@immutable class CycleLengthEntry` — `DateTime periodStartUtc`, `int lengthDays` — for the chart/history section.
    - `@immutable class PdfReportData` — `DateTime generatedAt`, `DateTime rangeStart`, `DateTime rangeEnd`, `String locale`, `CycleStatsSummary stats`, `FlowDistribution flowDist`, `PainDistribution painDist`, `MoodDistribution moodDist`, `List<CycleLengthEntry> cycleLengths`, `List<DaySummaryRow> daySummaryRows`, `List<NoteEntry> noteEntries`.
  </action>
  <verify>
    <automated>cd apps/ptrack && flutter pub get && dart analyze lib/features/pdf_export/pdf_section_config.dart lib/features/pdf_export/pdf_report_data.dart</automated>
  </verify>
  <done>Section config model supports 3 presets + per-section toggles + persistence; report data model captures all stats and day-level data structures; `pdf` and `printing` packages resolve.</done>
</task>

<task type="tdd">
  <name>Task 2: TDD — PDF data collector with stats computation</name>
  <files>
    apps/ptrack/lib/features/pdf_export/pdf_data_collector.dart
    apps/ptrack/test/features/pdf_export/pdf_data_collector_test.dart
  </files>
  <action>
    **TDD RED phase first.** Write tests in `pdf_data_collector_test.dart` before the implementation:

    Test cases (use `StoredPeriodWithDays` fixtures directly — no mock needed since collector is pure logic over the list):
    1. **Empty periods → empty report**: zero cycles, null averages, empty lists.
    2. **Periods outside date range → filtered out**: only periods whose start falls within rangeStart..rangeEnd are counted.
    3. **Cycle stats computed correctly**: Given 3 consecutive periods (e.g. starts on Jan 1, Jan 29, Feb 26), assert cycleCount=2 (completed cycles between consecutive pairs), avgCycleLengthDays=28, avgPeriodDurationDays matches.
    4. **Flow/pain/mood distributions**: Given day entries with known values, assert correct counts per enum value.
    5. **DaySummaryRows sorted by date ascending**: Given scattered day entries, output rows are date-ordered and within range.
    6. **NoteEntries only include non-empty notes**: Days with null/empty notes are excluded.
    7. **CycleLengthEntry list**: One entry per completed cycle with correct length.
    8. **Single period (no completed cycle)**: cycleCount=0, null averages, but period duration and day data still present.

    Run tests — they must FAIL (no implementation yet).

    **TDD GREEN phase.** Create `pdf_data_collector.dart`:
    - `class PdfDataCollector` with a single method:
      ```dart
      PdfReportData collect({
        required List<StoredPeriodWithDays> periodsWithDays,
        required PdfSectionConfig config,
        required PeriodCalendarContext calendar,
        required String locale,
      })
      ```
    - Implementation:
      1. Filter `periodsWithDays` to periods whose span overlaps `config.rangeStart..config.rangeEnd`.
      2. Sort filtered periods by startUtc ascending.
      3. Compute completed cycles using `completedCycleBetweenStarts()` from domain (pairs of consecutive period starts).
      4. Compute `CycleStatsSummary`: count completed cycles, average their lengths, compute average period duration (endUtc - startUtc + 1 day per period), min/max.
      5. Collect all day entries from filtered periods, filter to date range, compute `FlowDistribution`, `PainDistribution`, `MoodDistribution`.
      6. Build `DaySummaryRow` list (sorted by dateUtc ascending).
      7. Build `NoteEntry` list (only non-null, non-empty notes).
      8. Build `CycleLengthEntry` list from completed cycles.
      9. Return `PdfReportData` with `generatedAt: DateTime.now().toUtc()`.

    Run tests — they must PASS.

    **TDD REFACTOR** if needed: extract helper methods for clarity.
  </action>
  <verify>
    <automated>cd apps/ptrack && flutter test test/features/pdf_export/pdf_data_collector_test.dart</automated>
  </verify>
  <done>All 8+ test cases pass; data collector correctly filters by date range, computes cycle stats, distributions, and day-level data; handles edge cases (empty, single period, out-of-range).</done>
</task>

</tasks>

<verification>
- `flutter pub get` succeeds with new dependencies
- `dart analyze` clean on new files
- All data collector tests pass
- Section config presets produce expected section sets
</verification>

<success_criteria>
- `pdf` and `printing` packages added and resolve
- PdfSectionConfig supports 3 presets, per-section toggles, SharedPreferences persistence, default=full on first use
- PdfReportData model captures all stats (cycle count, averages, distributions) and day-level data
- PdfDataCollector passes all TDD test cases for date filtering, stats computation, edge cases
</success_criteria>

<output>
After completion, create `.planning/phases/13-pdf-export-of-period-statistics-and-details-user-selectable-if-all-or-none-goal-is-to-have-a-pdf-ready-for-a-physician-or-gynecologist/13-01-SUMMARY.md`
</output>
