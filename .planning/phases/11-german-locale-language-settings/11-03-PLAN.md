---
phase: 11-german-locale-language-settings
plan: 03
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - apps/ptrack/lib/**/*.dart
  - apps/ptrack/lib/l10n/app_en.arb
  - apps/ptrack/lib/l10n/app_de.arb
  - tool/**/*.dart
  - pubspec.yaml
  - .github/workflows/*.yml
autonomous: true
requirements:
  - I18N-04
  - I18N-05

must_haves:
  truths:
    - "User-visible **dates**, **numbers**, and **plural** UI use **active locale** conventions via `Localizations.localeOf(context)`, `intl` formatters, or ARB plural/select — not hard-coded US/English patterns in scoped widgets (see task list for concrete surfaces)"
    - "**Calendar week start** follows **locale default** only (**11-CONTEXT**); no user toggle added"
    - "**Export/import filenames** stay **ASCII-safe** (no localized filenames)"
    - "A **script or test** in CI fails if `app_de.arb` is missing any key required by `app_en.arb` (**I18N-05**)"
  artifacts:
    - path: "tool/arb_de_key_parity.dart"
      provides: "Executable check: DE keys ⊇ EN keys (adjust path/name as needed)"
    - path: ".github/workflows/*.yml"
      provides: "CI step invoking the parity check on pull requests"
  key_links:
    - from: "tool/arb_de_key_parity.dart"
      to: "apps/ptrack/lib/l10n/app_en.arb"
      via: "JSON key diff"
    - from: "apps/ptrack/lib/features/calendar/*.dart"
      to: "intl / table_calendar locale"
      via: "Formatting uses resolved locale"
---

<objective>
Finish **locale-aware formatting** (**I18N-04**) and add a **CI guard** so German ARB cannot silently miss keys (**I18N-05**).

Purpose: Meets roadmap criteria 3–4 and **11-CONTEXT** (numbers via standard `intl`, week start via locale, filenames unchanged).
Output: Audited formatters, optional ARB plural tweaks, parity script + CI wiring, tests.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-german-locale-language-settings/11-CONTEXT.md
@apps/ptrack/lib/features/calendar
@apps/ptrack/lib/features/home
@apps/ptrack/lib/features/logging
@apps/ptrack/lib/l10n/app_en.arb
</context>

<tasks>

<task type="auto">
  <name>Task 1: Locale-aware dates and numbers in UI</name>
  <files>
    apps/ptrack/lib/features/calendar
    apps/ptrack/lib/features/home
    apps/ptrack/lib/features/logging
    apps/ptrack/lib/features/export_import
    apps/ptrack/lib/l10n/app_en.arb
    apps/ptrack/lib/l10n/app_de.arb
  </files>
  <action>
1. **Audit** user-visible date/time formatting: replace fixed `DateFormat('...')` without locale with patterns that use **`Localizations.localeOf(context)`** (or `Intl.defaultLocale` set from app locale only if already centralized — prefer context).

2. Ensure **`table_calendar`** (if used) receives a **`locale`** string consistent with active locale (e.g. `de_DE` / `en_US` from `locale.toString()` where the package expects it).

3. **Numbers** (durations, counts, percentages if any): use **`NumberFormat`** from `intl` with the active locale — **no** custom overrides unless fixing a verified bug (**11-CONTEXT**).

4. **Plurals**: where UI shows “1 day” vs “N days” (or similar), use **ARB plural** messages or `intl` plural rules so **German** grammar is correct; add keys to **both** ARBs.

5. **Explicitly out of scope here:** localized **export/import file names** — keep **ASCII/English** names per **11-CONTEXT**.

6. **Week start:** do not add a settings toggle; rely on locale/calendar defaults.

7. List touched widgets in **SUMMARY** for verification traceability.
  </action>
  <verify>
    <automated>cd apps/ptrack; flutter analyze; flutter test</automated>
  </verify>
  <done>Analyzer clean; at least one test documents German vs English formatting difference for a representative date or number (optional: golden test if project already uses goldens).</done>
</task>

<task type="auto">
  <name>Task 2: ARB key parity CI guard</name>
  <files>
    tool/arb_de_key_parity.dart
    pubspec.yaml
    .github/workflows/*.yml
  </files>
  <action>
1. Add a **small Dart script** under repo `tool/` (path adjustable) that:
   - Loads **`apps/ptrack/lib/l10n/app_en.arb`** and **`app_de.arb`** as JSON.
   - Collects message keys (exclude `@metadata` keys if comparing user-visible messages only — compare the same rule set `gen-l10n` uses).
   - Exits **non-zero** if any key in the template is **missing** from `app_de.arb`.

2. Wire the script into **CI** (`.github/workflows/ci.yml` or equivalent) **and** add a **`melos` script** under root `pubspec.yaml` (e.g. `ci:arb:`) so `fvm exec melos run ci:arb` fails locally when `de` keys drift.

3. Document the command in **`11-03-SUMMARY.md`** for contributors.

4. **Do not** fail on extra keys in `de` unless project policy wants strict symmetry; default: **de must be superset or equal** to en message keys.
  </action>
  <verify>
    <automated>dart run tool/arb_de_key_parity.dart</automated>
  </verify>
  <done>Script exits 0 on current repo; intentionally deleting a `de` key in a scratch branch fails the script (executor validates once during implementation).</done>
</task>

</tasks>

<verification>
- Formatting behaves per active locale for listed surfaces; filenames for export/import remain ASCII.
- Parity script integrated into CI; removing a required `de` translation breaks the build.
</verification>

<success_criteria>
**I18N-04** satisfied for scoped widgets; **I18N-05** enforced in automation.
</success_criteria>

<output>
After completion, create `.planning/phases/11-german-locale-language-settings/11-03-SUMMARY.md`
</output>
