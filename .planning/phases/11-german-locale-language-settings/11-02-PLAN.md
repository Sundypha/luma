---
phase: 11-german-locale-language-settings
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - apps/ptrack/lib/main.dart
  - apps/ptrack/lib/features/shell/tab_shell.dart
  - apps/ptrack/lib/features/settings/app_language_settings.dart
  - apps/ptrack/test/**/*.dart
autonomous: true
requirements:
  - I18N-02

must_haves:
  truths:
    - "User can open **Settings**, pick **language** in order: **System default (follow device)**, **English**, **German**, per **11-CONTEXT**"
    - "Choice **persists** in `SharedPreferences` (or existing persistence pattern) and is reapplied on next app **cold start**"
    - "New install default is **follow device**; unsupported device locales resolve UI to **English** with **no** extra explanation"
    - "When user **manually** changes language in Settings, they are told the app **must restart** to apply (snackbar or dialog — discretion); after restart, `MaterialApp` uses the stored resolution"
    - "While **follow device** is selected, **OS locale changes** take effect on **next cold start** only (no mid-session hot switch requirement)"
    - "Locale resolution: **`de*`** → German catalog; **`en*`** → English; other → **English**"
  artifacts:
    - path: "apps/ptrack/lib/features/settings/app_language_settings.dart"
      provides: "Persistence + types for app language mode (followDevice / en / de)"
    - path: "apps/ptrack/lib/main.dart"
      provides: "MaterialApp locale + supportedLocales including en and de; wiring from prefs"
    - path: "apps/ptrack/lib/features/shell/tab_shell.dart"
      provides: "Dedicated Settings section for language (not buried)"
  key_links:
    - from: "apps/ptrack/lib/features/settings/app_language_settings.dart"
      to: "apps/ptrack/lib/main.dart"
      via: "Loaded preference selects `locale` / `localeListResolutionCallback`"
    - from: "apps/ptrack/lib/features/shell/tab_shell.dart"
      to: "app_language_settings"
      via: "Settings UI invokes persistence and restart prompt"
---

<objective>
Implement **user-selectable app language** with persistence and **MaterialApp** locale wiring (**I18N-02**), honoring all locked UX rules in **11-CONTEXT**.

Purpose: Meets roadmap criterion 1 (settings + persistence + en/de/follow-device). Complements German strings from plan 01.
Output: Settings section, preference store, `main.dart` locale behavior, tests.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-german-locale-language-settings/11-CONTEXT.md
@apps/ptrack/lib/main.dart
@apps/ptrack/lib/features/shell/tab_shell.dart
@apps/ptrack/lib/l10n/app_localizations.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: App language preference model + persistence</name>
  <files>
    apps/ptrack/lib/features/settings/app_language_settings.dart
  </files>
  <action>
1. Introduce an enum or sealed choice: **`followDevice`**, **`english`**, **`german`** (names flexible).

2. Persist the selection with **`SharedPreferences`** using a single well-named key. **Default** for missing key: **`followDevice`** (new installs).

3. Expose **`load()`** / **`save()`** (or a small `AppLanguageController` / `ChangeNotifier`) that `main.dart` and Settings can call. Keep API test-friendly (inject prefs or use platform interface fakes like other settings tests).

4. Implement **resolution helper**: given `AppLanguagePreference` + `Platform`/device locales, return the effective **`Locale`** for `MaterialApp` (`Locale('en')`, `Locale('de')`, or `null` to mean “defer to device” when using Flutter’s resolution) — align with **11-CONTEXT** (unsupported → English).

5. **Do not** implement mid-session hot switch for follow-device; document in code comment that OS locale changes apply on next process start.
  </action>
  <verify>
    <automated>cd apps/ptrack &amp;&amp; flutter test</automated>
  </verify>
  <done>Unit/widget tests cover default, save, reload, and resolution mapping for representative device locale lists.</done>
</task>

<task type="auto">
  <name>Task 2: Settings UI + restart prompt</name>
  <files>
    apps/ptrack/lib/features/shell/tab_shell.dart
    apps/ptrack/lib/features/settings/app_language_settings.dart
    apps/ptrack/lib/l10n/app_en.arb
    apps/ptrack/lib/l10n/app_de.arb
  </files>
  <action>
1. Add a **dedicated** Settings section (e.g. header + tiles) in **`_SettingsScreen`** (`tab_shell.dart`) for **app language**, not hidden inside unrelated groups.

2. Present options in order: **System default (follow device)** → **English** → **German**. Labels must come from **ARB** (add keys to `app_en.arb` / `app_de.arb` and regenerate).

3. On change: persist immediately, then show **restart** guidance per **11-CONTEXT** (snackbar or dialog — implementer’s choice). Optionally expose a “Restart app” button only if platform-appropriate; otherwise clear copy that user should restart manually.

4. Reflect **current** selection in the UI (radio, segmented control, or selected tile).

5. **Export/import filenames** remain **ASCII/English** per **11-CONTEXT** — do not localize backup filenames in this task.
  </action>
  <verify>
    <automated>cd apps/ptrack; flutter test</automated>
  </verify>
  <done>Widget test(s) select a language, verify preference written and UI shows selection (mock/fake prefs).</done>
</task>

<task type="auto">
  <name>Task 3: MaterialApp locale wiring</name>
  <files>
    apps/ptrack/lib/main.dart
    apps/ptrack/test/**/*.dart
  </files>
  <action>
1. Load persisted app language **before** or **when** building `MaterialApp` (may require lifting state: e.g. `LumaApp` reads prefs in `initState` or pass `AsyncSnapshot` from `main` — choose minimal diff consistent with codebase).

2. Set **`supportedLocales`** to include **English** and **German** (and any delegates Phase 10 added). Use **`localizationsDelegates`** unchanged from Phase 10 pattern.

3. Set **`locale`** (or `localeResolutionCallback` / `localeListResolutionCallback`) so:
   - Explicit **English** / **German** choices force the corresponding locale.
   - **Follow device** uses Flutter/device resolution with **fallback to English** for unsupported locales **silently**.

4. Apply the same behavior to **`homeOverride` MaterialApp** used in tests so widget tests match production resolution.

5. Ensure **German** (`de`) uses the **`de` ARB** from plan 01 (already generated).

6. Do **not** add extra locales beyond en/de.
  </action>
  <verify>
    <automated>cd apps/ptrack; flutter analyze; flutter test</automated>
  </verify>
  <done>Analyzer clean; tests pass; manual spot-check: change language, restart app, verify UI locale matches stored choice.</done>
</task>

</tasks>

<verification>
Full `apps/ptrack` test suite and analyze pass; roadmap success criterion 1 satisfied (settings, en/de/follow-device, persistence across restart).
</verification>

<success_criteria>
- Language visible and changeable in Settings with correct ordering and ARB-backed labels.
- Persistence + `MaterialApp` behavior match **11-CONTEXT** (restart to apply manual change; cold start for OS changes when following device).
</success_criteria>

<output>
After completion, create `.planning/phases/11-german-locale-language-settings/11-02-SUMMARY.md`
</output>
