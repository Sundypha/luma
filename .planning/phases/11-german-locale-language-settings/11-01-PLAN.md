---
phase: 11-german-locale-language-settings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/ptrack/lib/l10n/app_de.arb
  - apps/ptrack/lib/l10n/app_en.arb
  - apps/ptrack/test/**/*.dart
autonomous: true
requirements:
  - I18N-03

must_haves:
  truths:
    - "`app_de.arb` exists beside `app_en.arb` and defines **every** message key present in the English template (no missing keys for the shipped catalog)"
    - "`flutter gen-l10n` / `flutter pub get` succeeds and analyzer is clean with German locale in the generated `AppLocalizations` surface"
    - "German copy follows **11-CONTEXT**: informal **du**, conversational everyday tone for cycle/symptom labels; prediction/disclaimer strings preserve **legal weight and intent** vs English (may be longer)"
    - "At least one automated test loads **Locale('de')** and exercises in-scope UI without `FlutterError` / missing delegate failures"
  artifacts:
    - path: "apps/ptrack/lib/l10n/app_de.arb"
      provides: "Full German message catalog keyed to English template"
    - path: "apps/ptrack/lib/l10n/app_en.arb"
      provides: "Source of truth for key set (executor syncs any new keys from Phase 10 completion)"
  key_links:
    - from: "apps/ptrack/lib/l10n/app_de.arb"
      to: "flutter gen-l10n outputs"
      via: "ARB ICU placeholders must match `app_en.arb` variable names"
---

<objective>
Add a complete **German (`de`)** ARB catalog with **key parity** to English, per **I18N-03** and **11-CONTEXT** copy rules.

Purpose: Unblocks language resolution, settings (plan 02), and locale-sensitive formatting (plan 03) without runtime missing-translation crashes.
Output: `app_de.arb`, green `gen-l10n`, focused tests under `de`.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-german-locale-language-settings/11-CONTEXT.md
@apps/ptrack/l10n.yaml
@apps/ptrack/lib/l10n/app_en.arb
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create app_de.arb with full key parity</name>
  <files>
    apps/ptrack/lib/l10n/app_de.arb
    apps/ptrack/lib/l10n/app_en.arb
  </files>
  <action>
1. Treat **`app_en.arb`** as the canonical key set (post–Phase 10). Enumerate all message keys (including metadata/`@key` entries for placeholders).

2. Create **`app_de.arb`** with **identical keys** and matching ICU placeholder names for every entry. Translate values to German per **11-CONTEXT**:
   - Informal **du** for UI addressing the user.
   - Everyday, conversational German for symptoms/cycle/period labels (not clinical-first).
   - Prediction and disclaimer strings: same **legal intent** as English; rephrase as needed for accurate German.

3. For **plural / select** ARB entries, provide grammatically correct German forms (plan 03 may refine formatting callsites, but ARB must compile).

4. Run **`flutter gen-l10n`** from `apps/ptrack` (or `flutter pub get` if that triggers gen). Fix any ARB syntax or ICU errors until generation succeeds.

5. Do **not** add extra locales beyond `de` in this plan.
  </action>
  <verify>
    <automated>cd apps/ptrack &amp;&amp; flutter gen-l10n &amp;&amp; flutter analyze</automated>
    <manual>Optional: spot-check a few disclaimer lines against English for intent parity.</manual>
  </verify>
  <done>Generated localizations include German; analyzer reports no errors in `apps/ptrack`.</done>
</task>

<task type="auto">
  <name>Task 2: Tests — German locale smoke</name>
  <files>
    apps/ptrack/test/**/*.dart
  </files>
  <action>
1. Add or extend tests that pump **`MaterialApp`** (or existing test harness from Phase 10) with **`locale: const Locale('de')`** and the same `localizationsDelegates` / `supportedLocales` as production (including `Locale('de')` once supported).

2. Cover at least: open **one settings-related** surface and **one primary flow** (e.g. shell or home) so `AppLocalizations` resolves under `de` without throwing.

3. Assertions may be **structural** (finder finds widgets) rather than brittle full-string matches, unless a stable German substring is appropriate.

4. Ensure **`LumaApp`** / `homeOverride` test paths stay consistent with Phase 10 delegate wiring.
  </action>
  <verify>
    <automated>cd apps/ptrack; flutter test</automated>
  </verify>
  <done>Tests pass; at least one test explicitly uses `Locale('de')`.</done>
</task>

</tasks>

<verification>
`melos exec` (or repo CI equivalent) for `flutter analyze` and `flutter test` on `apps/ptrack` passes. Team native-speaker review of final `app_de.arb` is encouraged in SUMMARY (not a blocking automation gap).
</verification>

<success_criteria>
- Key parity: every `app_en.arb` user message key exists in `app_de.arb`.
- Codegen and analyzer clean.
- Automated `de` locale smoke tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/11-german-locale-language-settings/11-01-SUMMARY.md`
</output>
