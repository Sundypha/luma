---
phase: 02-domain-persistence-prediction-v1
plan: 03
type: tdd
wave: 2
depends_on:
  - "01"
files_modified:
  - packages/ptrack_domain/lib/src/prediction/prediction_engine.dart
  - packages/ptrack_domain/lib/src/prediction/prediction_rules.md
  - packages/ptrack_domain/test/prediction_engine_test.dart
autonomous: true
requirements:
  - PRED-01
  - PRED-02
  - PRED-03
must_haves:
  truths:
    - "Next-period estimate uses median of up to six completed cycles after deterministic exclusions (long-gap, within-window outliers, long-bleed) with no ML."
    - "With fewer than two completed cycles after filtering, the engine returns an insufficient-history outcome with explanation steps."
    - "High variability widens ranges and can downgrade presentation tier per CONTEXT thresholds (document numbers in code comments)."
    - "Explanation steps list which cycles were used, exclusions, median, and range math in factual bullets."
  artifacts:
    - path: "packages/ptrack_domain/lib/src/prediction/prediction_engine.dart"
      provides: "Pure prediction API consuming completed cycles; produces PredictionResult + explanation"
    - path: "packages/ptrack_domain/lib/src/prediction/prediction_rules.md"
      provides: "Human-readable rule spec mirrored by tests"
    - path: "packages/ptrack_domain/test/prediction_engine_test.dart"
      provides: "Golden-vector style cases for median/outliers/insufficient history"
  key_links:
    - from: "packages/ptrack_domain/lib/src/prediction/prediction_engine.dart"
      to: "packages/ptrack_domain/lib/src/prediction/prediction_result.dart"
      via: "returns sealed/variant results"
      pattern: "PredictionResult"
---

<objective>
Implement **deterministic prediction** and **explanation generation** in pure Dart using **TDD**, strictly following `02-CONTEXT.md` math rules.

Purpose: Delivers **PRED-01** (documented rules), **PRED-02** (uncertainty handling), and **PRED-03** (structured explanation) independent of SQLite and UI.
Output: `PredictionEngine` (name may vary), `prediction_rules.md` summarizing algorithms and thresholds, and comprehensive tests.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/02-domain-persistence-prediction-v1/02-CONTEXT.md
@.planning/phases/02-domain-persistence-prediction-v1/02-RESEARCH.md
@packages/ptrack_domain/lib/src/period/cycle_length.dart
@packages/ptrack_domain/lib/src/prediction/prediction_result.dart
@packages/ptrack_domain/lib/src/prediction/explanation_step.dart
</context>

<feature>
  <name>Deterministic next-start prediction</name>
  <files>packages/ptrack_domain/lib/src/prediction/prediction_engine.dart,packages/ptrack_domain/test/prediction_engine_test.dart,packages/ptrack_domain/lib/src/prediction/prediction_rules.md</files>
  <behavior>
    Input: ordered completed cycles (with lengths in days), optional annotations for excluded spans (long gap, long bleed, statistical outlier) surfaced as structured reasons for explanation output.
    Output: `PredictionResult` variants reflecting insufficient history (&lt;2 usable cycles), range-only/high-variability, or point estimate with range; plus ordered `ExplanationStep` list describing median choice, exclusions, and next-start date math (UTC instant or date-only policy consistent with Plan 01).
    Cases: baseline median; outlier dropped; long-gap cycle excluded; insufficient history; high spread widens interval and may downgrade tier.
  </behavior>
  <implementation>
    RED: write failing tests per case. GREEN: implement minimal engine satisfying tests. REFACTOR: extract pure helpers for median, exclusion filters, and explanation assembly. Document chosen numeric thresholds in `prediction_rules.md` and mirror in tests.
  </implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1: RED — prediction engine tests</name>
  <files>packages/ptrack_domain/test/prediction_engine_test.dart,packages/ptrack_domain/lib/src/prediction/prediction_rules.md</files>
  <action>
    Author `prediction_engine_test.dart` with **failing** calls to a not-yet-implemented API (explicit TODO throws). Cover at minimum: (a) insufficient history, (b) plain median of 3 cycles, (c) one within-window outlier excluded, (d) long-gap exclusion, (e) high-variability widens range / tier downgrade.
    Add `prediction_rules.md` outlining expected behavior and threshold placeholders matching tests.
  </action>
  <verify>
    <automated>melos exec -c 1 --scope=ptrack_domain -- fvm flutter analyze</automated>
    <manual>Run `fvm flutter test packages/ptrack_domain/test/prediction_engine_test.dart` and confirm failures are assertion failures from missing engine (RED), not analyzer errors.</manual>
    <sampling_rate>after RED commit</sampling_rate>
  </verify>
  <done>Test file compiles; assertions fail until engine exists; rules doc committed.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement PredictionEngine</name>
  <files>packages/ptrack_domain/lib/src/prediction/prediction_engine.dart,packages/ptrack_domain/lib/ptrack_domain.dart</files>
  <action>
    Implement the engine to satisfy all tests; keep functions **pure** (no I/O). Ensure exclusions required by CONTEXT attach **machine-readable reasons** usable by UI later. Export the engine entrypoint from `ptrack_domain.dart`.
  </action>
  <verify>
    <automated>cd D:\CODE\ptrack && melos exec -c 1 --scope=ptrack_domain -- fvm flutter test packages/ptrack_domain/test/prediction_engine_test.dart</automated>
    <sampling_rate>after GREEN commit</sampling_rate>
  </verify>
  <done>All prediction engine tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: REFACTOR — tighten docs and edge cases</name>
  <files>packages/ptrack_domain/lib/src/prediction/prediction_engine.dart,packages/ptrack_domain/lib/src/prediction/prediction_rules.md,packages/ptrack_domain/test/prediction_engine_test.dart</files>
  <action>
    Refactor for clarity without behavior change; add any missing edge-case tests (e.g. exactly six cycles windowing, open cycles not passed in). Align `prediction_rules.md` verbatim with implemented thresholds.
  </action>
  <verify>
    <automated>melos exec -c 1 --scope=ptrack_domain -- fvm flutter test</automated>
    <sampling_rate>before merge</sampling_rate>
  </verify>
  <done>Full `ptrack_domain` test suite green; rules doc matches code.</done>
</task>

</tasks>

<verification>
`melos exec -c 1 --scope=ptrack_domain -- fvm flutter test` passes.
</verification>

<success_criteria>
Prediction behavior is **fully described by tests + prediction_rules.md** and matches CONTEXT median / window / uncertainty rules.
</success_criteria>

<output>
After completion, create `.planning/phases/02-domain-persistence-prediction-v1/02-03-SUMMARY.md`
</output>
