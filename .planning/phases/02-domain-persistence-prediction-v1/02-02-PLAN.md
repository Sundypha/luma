---
phase: 02-domain-persistence-prediction-v1
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - packages/ptrack_data/pubspec.yaml
  - packages/ptrack_data/lib/ptrack_data.dart
  - packages/ptrack_data/lib/src/db/ptrack_database.dart
  - packages/ptrack_data/lib/src/db/tables.dart
  - packages/ptrack_data/lib/src/db/migrations.dart
  - packages/ptrack_data/lib/src/mappers/period_mapper.dart
  - packages/ptrack_data/test/migration_test.dart
  - packages/ptrack_data/test/test_utils.dart
  - packages/ptrack_data/drift_schemas/README.md
autonomous: true
requirements:
  - NFR-02
must_haves:
  truths:
    - "SQLite schema carries an integer user version compatible with export documentation expectations."
    - "Opening a DB at schema version N and migrating to N+1 preserves fixture data (automated test)."
    - "Opening a DB newer than the app supports fails closed with a clear, non-silent error path."
    - "Period rows round-trip through mapper to domain types from Plan 01."
  artifacts:
    - path: "packages/ptrack_data/lib/src/db/ptrack_database.dart"
      provides: "Drift database with schemaVersion and migration callbacks"
    - path: "packages/ptrack_data/test/migration_test.dart"
      provides: "Fixture migration coverage for at least v1 introduction"
    - path: "packages/ptrack_data/lib/src/mappers/period_mapper.dart"
      provides: "Row ↔ domain mapping for periods"
  key_links:
    - from: "packages/ptrack_data/lib/src/mappers/period_mapper.dart"
      to: "packages/ptrack_domain"
      via: "package import"
      pattern: "ptrack_domain"
---

<objective>
Add **Drift-backed SQLite** persistence in `ptrack_data` with **versioned migrations**, **mapper to domain types**, and **automated migration tests** using on-disk fixtures.

Purpose: Satisfies **NFR-02** (no silent loss, tested upgrades) before prediction wiring.
Output: `PtrackDatabase` (name may vary) at `schemaVersion: 1`, migration path from empty, mapper tests, documented fixture layout for future bumps.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-domain-persistence-prediction-v1/02-CONTEXT.md
@.planning/phases/02-domain-persistence-prediction-v1/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dependencies and Drift schema v1</name>
  <files>packages/ptrack_data/pubspec.yaml,packages/ptrack_data/lib/src/db/ptrack_database.dart,packages/ptrack_data/lib/src/db/tables.dart,packages/ptrack_data/lib/ptrack_data.dart</files>
  <action>
    Add `drift`, `sqlite3_flutter_libs`, `path`, and `path_provider` to `ptrack_data`; add `drift_dev` + `build_runner` as dev_dependencies. Run codegen per Drift docs.
    Declare `ptrack_data` → `ptrack_domain` path dependency for typed mapping.
    Create initial `periods` (or equivalently named) table matching fields required by domain models (UTC start/end, metadata needed for validation such as local-day keys if stored denormalized—**prefer minimal duplication**: store UTC instants and derive local-day checks at validation layer when saving).
    Implement `schemaVersion: 1` and `onCreate` only if no upgrade path yet; use **transactions** in `onUpgrade` stubs for future bumps even if v1 only creates schema.
    Export a narrow public API (e.g. `openPtrackDatabase({...})` factory) from `ptrack_data.dart`.
  </action>
  <verify>
    <automated>melos bootstrap && melos exec -c 1 --scope=ptrack_data -- fvm dart run build_runner build --delete-conflicting-outputs</automated>
    <sampling_rate>after codegen</sampling_rate>
  </verify>
  <done>Codegen succeeds; database class compiles; schema v1 creates expected table(s).</done>
</task>

<task type="auto">
  <name>Task 2: Mappers, newer-schema guard, and migration fixture test</name>
  <files>packages/ptrack_data/lib/src/mappers/period_mapper.dart,packages/ptrack_data/lib/src/db/migrations.dart,packages/ptrack_data/test/migration_test.dart,packages/ptrack_data/test/test_utils.dart,packages/ptrack_data/drift_schemas/README.md</files>
  <action>
    Implement **bidirectional** mapping between Drift rows and `ptrack_domain` period types; add unit tests for mapper round-trip.
    Implement detection for **on-disk user_version greater than supported** by this app version: opening such a DB must **throw** or return a **Result** type that the app layer can surface—**no silent open**. Test by opening a hand-crafted SQLite file or using a test-only hook to set pragma user_version high.
    Add `test/migration_test.dart` that uses a **committed fixture** SQLite file at version **1** (or starts at 0 and asserts migration to 1) verifying representative rows survive migration. Document in `drift_schemas/README.md` how to regenerate fixtures when bumping schema.
    Ensure migration steps are **transactional**; failures must not leave a partially applied schema (assert via test where practical).
  </action>
  <verify>
    <automated>melos exec -c 1 --scope=ptrack_data -- fvm flutter test</automated>
    <sampling_rate>after task commit</sampling_rate>
  </verify>
  <done>Mapper + migration tests pass; newer-schema guard test passes; README explains fixture discipline for future versions.</done>
</task>

</tasks>

<verification>
`melos exec -c 1 --scope=ptrack_data -- fvm flutter test` passes; `fvm flutter analyze` clean for `ptrack_data`.
</verification>

<success_criteria>
Persistence layer proves **tested migration** and **safe failure** on unsupported newer schema; domain mapping is reliable.
</success_criteria>

<output>
After completion, create `.planning/phases/02-domain-persistence-prediction-v1/02-02-SUMMARY.md`
</output>
