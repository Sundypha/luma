---
phase: 02-domain-persistence-prediction-v1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ptrack_domain/lib/ptrack_domain.dart
  - packages/ptrack_domain/lib/src/period/period_models.dart
  - packages/ptrack_domain/lib/src/period/period_validation.dart
  - packages/ptrack_domain/lib/src/period/cycle_length.dart
  - packages/ptrack_domain/lib/src/prediction/prediction_result.dart
  - packages/ptrack_domain/lib/src/prediction/explanation_step.dart
  - packages/ptrack_domain/test/period_validation_test.dart
  - packages/ptrack_domain/test/cycle_length_test.dart
  - packages/ptrack_domain/test/prediction_result_test.dart
autonomous: true
requirements:
  - PRED-02
  - PRED-03
must_haves:
  truths:
    - "Period records use UTC instants; validation rejects overlaps, end-before-start, and duplicate starts on the same calendar day."
    - "Completed-cycle length uses one documented canonical rule (start → day before next start) with executable examples."
    - "Prediction outputs are modeled as structured variants (e.g. insufficient history vs point/range) without UI."
    - "Explanation is representable as an ordered list of factual steps independent of Flutter widgets."
  artifacts:
    - path: "packages/ptrack_domain/lib/src/period/period_validation.dart"
      provides: "Save/edit validation aligned with Phase 2 CONTEXT"
    - path: "packages/ptrack_domain/lib/src/prediction/prediction_result.dart"
      provides: "Sealed or variant type encoding uncertainty tiers"
    - path: "packages/ptrack_domain/lib/src/prediction/explanation_step.dart"
      provides: "Machine-readable explanation steps for later UI"
  key_links:
    - from: "packages/ptrack_domain/lib/src/period/period_validation.dart"
      to: "packages/ptrack_domain/lib/src/period/period_models.dart"
      via: "validation operates on period value types"
      pattern: "Period"
---

<objective>
Define **period value types**, **validation rules**, **completed-cycle length semantics**, and **prediction/explanation data models** in `ptrack_domain` with fast unit tests—no database or Flutter UI.

Purpose: Locks semantics from `02-CONTEXT.md` into code before Drift and the prediction engine land, so persistence and algorithms share one source of truth.
Output: Exported domain API surface, documented cycle-length definition, and tests covering validation + type invariants.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-domain-persistence-prediction-v1/02-CONTEXT.md
@.planning/phases/02-domain-persistence-prediction-v1/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Period models and validation</name>
  <files>packages/ptrack_domain/lib/ptrack_domain.dart,packages/ptrack_domain/lib/src/period/period_models.dart,packages/ptrack_domain/lib/src/period/period_validation.dart,packages/ptrack_domain/test/period_validation_test.dart</files>
  <action>
    Introduce immutable types for a stored period (UTC start/end instants, optional end for open periods) and a validation API used before persistence (e.g. `PeriodValidation.validateForSave(...)` returning structured errors).
    Implement CONTEXT rules: reject overlapping periods; reject end before start; reject a second period whose **start** falls on the same **local calendar day** as an existing period’s start (use a supplied `TimeZoneLocation` or `DateTime` + explicit local-date helper—document the chosen approach); allow single-day spans.
    Open periods must be representable but clearly excluded from any helper that lists **completed** cycles (surface this via API naming to avoid misuse).
    Export public API from `ptrack_domain.dart` without deep barrel files unless needed.
  </action>
  <verify>
    <automated>melos exec -c 1 --scope=ptrack_domain -- fvm flutter test</automated>
    <sampling_rate>after task commit</sampling_rate>
  </verify>
  <done>All new validation tests pass; overlapping/duplicate-day/end-before-start cases are covered with readable assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Cycle length definition and prediction-facing types</name>
  <files>packages/ptrack_domain/lib/src/period/cycle_length.dart,packages/ptrack_domain/lib/src/prediction/prediction_result.dart,packages/ptrack_domain/lib/src/prediction/explanation_step.dart,packages/ptrack_domain/test/cycle_length_test.dart,packages/ptrack_domain/test/prediction_result_test.dart</files>
  <action>
    Implement **one** canonical **completed-cycle length** in days: from period **start** (local calendar day convention consistent with CONTEXT: anchor on UTC-stored instants interpreted in a given timezone for “calendar day”) through the **day before** the next period **start**. Document with 2–3 examples in doc comments and tests (including a simple non-DST and one DST-adjacent case if feasible).
    Add `CompletedCycle`-style value objects as needed for prediction input (no DB).
    Model `PredictionResult` (sealed classes or equivalent) to encode **insufficient history**, **range-only / high-variability**, and **point ± range** presentations per CONTEXT—without embedding UI copy.
    Add `ExplanationStep` (ordered, factual) and a small pure function `List&lt;ExplanationStep&gt;` builder stub or factory placeholders if full logic waits for Plan 03—types must be stable for the engine plan.
  </action>
  <verify>
    <automated>melos exec -c 1 --scope=ptrack_domain -- fvm flutter test</automated>
    <sampling_rate>after task commit</sampling_rate>
  </verify>
  <done>Cycle length tests pin the documented definition; prediction result variants compile and have at least one test each for serialization or equality where relevant.</done>
</task>

</tasks>

<verification>
`melos exec -c 1 --scope=ptrack_domain -- fvm flutter test` passes; `fvm flutter analyze` clean for `ptrack_domain`.
</verification>

<success_criteria>
Domain layer encodes CONTEXT validation and uncertainty/explanation **types**; tests demonstrate the canonical cycle-length rule.
</success_criteria>

<output>
After completion, create `.planning/phases/02-domain-persistence-prediction-v1/02-01-SUMMARY.md`
</output>
