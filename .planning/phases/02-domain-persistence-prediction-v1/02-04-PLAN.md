---
phase: 02-domain-persistence-prediction-v1
plan: 04
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
  - "03"
files_modified:
  - packages/ptrack_data/lib/src/repositories/period_repository.dart
  - packages/ptrack_data/lib/src/prediction/prediction_coordinator.dart
  - packages/ptrack_data/lib/ptrack_data.dart
  - packages/ptrack_data/test/period_repository_test.dart
  - packages/ptrack_data/test/prediction_coordinator_test.dart
  - packages/ptrack_domain/lib/src/prediction/prediction_copy.dart
  - packages/ptrack_domain/test/prediction_copy_test.dart
  - apps/ptrack/test/prediction_explanation_widget_test.dart
autonomous: true
requirements:
  - PRED-02
  - PRED-03
  - PRED-04
  - NFR-02
must_haves:
  truths:
    - "Saving periods through the repository applies domain validation and persists via Drift transactions."
    - "A coordinator composes load → derive completed cycles → PredictionEngine → user-facing explanation text without medical or contraception framing."
    - "A widget test renders formatted explanation text so a human-readable narrative is provably present before Phase 5 UI."
    - "Integration tests prove create → migrate → reload preserves period rows (extends NFR-02 beyond bare migration fixtures)."
  artifacts:
    - path: "packages/ptrack_data/lib/src/repositories/period_repository.dart"
      provides: "Transactional persistence API with validation"
    - path: "packages/ptrack_data/lib/src/prediction/prediction_coordinator.dart"
      provides: "Orchestrates engine with stored data"
    - path: "packages/ptrack_domain/lib/src/prediction/prediction_copy.dart"
      provides: "PRED-04-safe templates + formatter helpers"
    - path: "apps/ptrack/test/prediction_explanation_widget_test.dart"
      provides: "Widget-level proof of readable explanation"
  key_links:
    - from: "packages/ptrack_data/lib/src/prediction/prediction_coordinator.dart"
      to: "packages/ptrack_domain/lib/src/prediction/prediction_engine.dart"
      via: "direct call"
      pattern: "PredictionEngine"
---

<objective>
Wire **repository + coordinator** so stored periods flow through **domain validation**, **Drift persistence**, and the **prediction engine**, emitting **PRED-04-safe** copy and a **widget-tested** readable explanation.

Purpose: Closes the loop between layers for Phase 2 success criteria (correct data, deterministic prediction, honest uncertainty, readable explanation) without building calendar/home UI.
Output: Public coordinator/repository API from `ptrack_data`, copy helpers in `ptrack_domain`, tests spanning persistence + prediction + formatting.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/02-domain-persistence-prediction-v1/02-CONTEXT.md
@.planning/phases/02-domain-persistence-prediction-v1/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PeriodRepository with validation and transactions</name>
  <files>packages/ptrack_data/lib/src/repositories/period_repository.dart,packages/ptrack_data/test/period_repository_test.dart,packages/ptrack_data/lib/ptrack_data.dart</files>
  <action>
    Implement `PeriodRepository` (name may vary) exposing async methods to insert/update/list periods using Drift **transactions** for writes. Call `ptrack_domain` validation before insert/update; map validation failures to stable error types (no silent drop).
    Tests: use in-memory Drift executor or temporary file DB; cover successful round-trip and a rejected overlap/dup-day case.
    Export the repository factory from `ptrack_data.dart`.
  </action>
  <verify>
    <automated>melos exec -c 1 --scope=ptrack_data -- fvm flutter test packages/ptrack_data/test/period_repository_test.dart</automated>
    <sampling_rate>after task commit</sampling_rate>
  </verify>
  <done>Repository tests pass; invalid saves do not persist rows.</done>
</task>

<task type="auto">
  <name>Task 2: Prediction coordinator + PRED-04 copy</name>
  <files>packages/ptrack_data/lib/src/prediction/prediction_coordinator.dart,packages/ptrack_data/test/prediction_coordinator_test.dart,packages/ptrack_domain/lib/src/prediction/prediction_copy.dart,packages/ptrack_domain/test/prediction_copy_test.dart</files>
  <action>
    Implement a coordinator that: loads closed+open periods as needed, derives **completed cycles** per Plan 01 rules (open excluded from stats), invokes `PredictionEngine`, and builds **final explanation strings** by combining `ExplanationStep` with templates from `prediction_copy.dart`.
    Centralize user-visible templates in `prediction_copy.dart` with conservative language (**estimates**, **not medical advice**, no contraception efficacy claims). Add unit tests that scan for forbidden phrases (e.g. “guarantee”, “100% effective”, “prevent pregnancy”) and assert insufficient-history messaging does not invent dates.
    Add `prediction_coordinator_test.dart` using fake/stubbed periods to assert end-to-end structured output matches engine expectations.
  </action>
  <verify>
    <automated>melos exec -c 1 --scope=ptrack_data -- fvm flutter test packages/ptrack_data/test/prediction_coordinator_test.dart</automated>
    <sampling_rate>after task commit</sampling_rate>
  </verify>
  <done>Coordinator tests pass; copy tests pass; PRED-04 guardrails encoded.</done>
</task>

<task type="auto">
  <name>Task 3: Widget test for readable explanation</name>
  <files>apps/ptrack/test/prediction_explanation_widget_test.dart</files>
  <action>
    Add a **widget test** under `apps/ptrack` that pumps a tiny `MaterialApp` subtree (may live in test-only widget) rendering formatted explanation text produced by coordinator/copy helpers from a **fixture** prediction result. Assert key phrases exist (e.g. mentions “estimate” or equivalent approved wording) and that the text is non-empty for sufficient-history fixtures.
    This satisfies the roadmap’s “user can read plain-language explanation” at test level before Phase 5 surfaces.
  </action>
  <verify>
    <automated>melos exec -c 1 --scope=ptrack -- fvm flutter test apps/ptrack/test/prediction_explanation_widget_test.dart</automated>
    <sampling_rate>after task commit</sampling_rate>
  </verify>
  <done>Widget test passes locally and in CI via Melos.</done>
</task>

</tasks>

<verification>
`melos exec -c 1 --dir-exists=test -- fvm flutter test` passes from repo root; `melos exec -c 1 -- fvm flutter analyze` passes for touched packages.
</verification>

<success_criteria>
Data + prediction + copy are **integrated** with automated tests; explanation is **human-readable** in widget tests; persistence remains migration-safe.
</success_criteria>

<output>
After completion, create `.planning/phases/02-domain-persistence-prediction-v1/02-04-SUMMARY.md`
</output>
