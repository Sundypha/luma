---
phase: 15-code-review
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - packages/ptrack_data/lib/src/export/import_service.dart
  - packages/ptrack_data/lib/ptrack_data.dart
  - packages/ptrack_data/test/export/import_service_test.dart
  - apps/ptrack/lib/features/export_import/import_view_model.dart
autonomous: true
requirements: []

must_haves:
  truths:
    - "Imported periods are validated with the same domain rules as normal inserts (overlap, end-before-start, duplicate local start day) before any row is written."
    - "Day entry upsert keys on (periodId, dateUtc); ambiguous or orphan references fail with LumaImportException, not StateError."
    - "Valid round-trip exports still apply successfully; regression tests cover malformed payloads."
  artifacts:
    - path: "packages/ptrack_data/lib/src/export/import_service.dart"
      provides: "Validated applyImport; scoped duplicate lookup; orphan ref handling"
      exports: ["ImportService"]
    - path: "packages/ptrack_data/test/export/import_service_test.dart"
      provides: "TDD coverage for overlap, bad ref, same-date different periods, replace/skip"
      min_lines: 120
  key_links:
    - from: "packages/ptrack_data/lib/src/export/import_service.dart"
      to: "packages/ptrack_domain/lib/src/period/period_validation.dart"
      via: "PeriodValidation.validateForSave + PeriodCalendarContext"
      pattern: "domain validation on import"
---

<objective>
Close **high-severity** code review gaps in the `.luma` import path: (1) periods bypass `PeriodValidation`, (2) duplicate day handling uses `dateUtc` only instead of `(periodId, dateUtc)`, (3) orphan `periodRefId` force-unwrap crashes.

Purpose: Treat backups as untrusted input; preserve data integrity and predictable UX on bad files.

Output: Hardened `ImportService.applyImport` + tests; wire `PeriodCalendarContext` into import (constructor or method parameter) wherever `ImportService` is constructed in the app.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/CODE_REVIEW.md
@.planning/PROJECT.md
@.planning/STATE.md
@packages/ptrack_domain/lib/src/period/period_validation.dart
@packages/ptrack_domain/lib/src/period/period_models.dart
@packages/ptrack_data/lib/src/export/import_service.dart
@packages/ptrack_data/lib/src/repositories/period_repository.dart
@packages/ptrack_data/test/export/import_service_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject calendar context and validate imported periods</name>
  <files>
    packages/ptrack_data/lib/src/export/import_service.dart
    packages/ptrack_data/lib/ptrack_data.dart
  </files>
  <action>
    - Add `PeriodCalendarContext` to `ImportService` (constructor parameter, required).
    - Inside `applyImport` transaction, **before** inserting periods:
      1. Load existing `PeriodSpan` list from DB (same mapping as `PeriodRepository.listOrderedByStartUtc` / `periodRowToDomain`).
      2. For each imported period, parse `startUtc`/`endUtc` into `PeriodSpan`; run `PeriodValidation.validateForSave(candidate:, existing: accumulated, calendar:)`. On rejection, throw a **new** `LumaImportException` subclass (e.g. `LumaImportValidationException`) with a stable, user-facing message key or string (executor may add ARB later in a follow-up if needed; keep English message acceptable for this plan).
      3. On success, append candidate to `accumulated` and insert row as today.
    - Re-export `ImportService` API change: update all call sites (grep `ImportService(`).
  </action>
  <verify>
    <automated>cd packages/ptrack_data && dart analyze lib/src/export/import_service.dart</automated>
  </verify>
  <done>ImportService requires calendar; invalid overlapping or end-before-start payloads throw typed import exception before inserts.</done>
</task>

<task type="tdd">
  <name>Task 2: TDD — day entries: (periodId, dateUtc), orphan refs, replace/skip</name>
  <files>
    packages/ptrack_data/lib/src/export/import_service.dart
    packages/ptrack_data/test/export/import_service_test.dart
  </files>
  <action>
    **RED:** Add tests in `import_service_test.dart` (memory DB + `ImportService` with `PeriodCalendarContext(tz.UTC)` from `package:timezone/timezone.dart`, matching `period_validation_test`):

    1. **Orphan periodRefId:** day entry references missing `ref_id` → expect `LumaImportException` (or `LumaInvalidFileException`); assert **no** new periods/entries committed (transaction rolls back).
    2. **Same calendar date, two different periods:** Pre-seed two periods with day entries on same `dateUtc` for different `periodId`s. Import entry for period A on that date with `replace` → only A's row updated; B's row unchanged.
    3. **Skip strategy:** duplicate `(periodId, dateUtc)` → skipped count incremented, no overwrite of other period's row.
    4. **Overlapping imported periods:** two spans that fail `validateForSave` → throws, DB unchanged.
    5. **Happy path:** existing valid `LumaExportData` round-trip still applies (may use `ExportService` + `applyImport` like other tests).

    **GREEN:** Replace `getSingleOrNull` on `dateUtc` alone with query filtered by **`periodId` + normalized `dateUtc`** (use same midnight-UTC normalization as today). Replace `refMap[id]!` with lookup + throw typed exception with ref id / index in message.

    **REFACTOR:** Extract small private helpers if needed; keep transaction single-pass.
  </action>
  <verify>
    <automated>cd packages/ptrack_data && dart test test/export/import_service_test.dart</automated>
  </verify>
  <done>All new tests pass; import no longer crashes on orphan ref or ambiguous date-only query.</done>
</task>

<task type="human-verify">
  <name>Task 3: App wiring + smoke import in UI or integration</name>
  <files>
    apps/ptrack/lib/features/export_import/import_view_model.dart
  </files>
  <action>
    - Update `ImportService` construction to pass device calendar context (same source as `PeriodRepository` / `main.dart` `calendarForDevice()` — inject from caller or read from existing app DI pattern).
    - Manually: export a small backup, re-import with replace/skip; confirm success. Optionally attempt a hand-edited invalid JSON backup if tooling allows (or unit tests suffice).
  </action>
  <verify>
    <automated>cd apps/ptrack && flutter analyze lib/features/export_import/import_view_model.dart</automated>
  </verify>
  <done>App compiles; import flow still works for valid files.</done>
</task>

</tasks>

<verification>
- `dart test packages/ptrack_data/test/export/import_service_test.dart` passes
- `dart analyze` / `flutter analyze` clean on touched packages
- No uncaught exceptions on malformed import in tests
</verification>

<success_criteria>
- Findings 1–3 in `docs/CODE_REVIEW.md` are addressed in code and tests
- Public API changes are reflected at all `ImportService` call sites
</success_criteria>

<output>
After completion, create `15-01-SUMMARY.md` in this phase directory.
</output>
