---
phase: 15-code-review
plan: 02
type: implementation
wave: 2
depends_on: []
files_modified:
  - apps/ptrack/lib/main.dart
  - apps/ptrack/lib/features/lock/delete_ptrack_db_file_io.dart
  - apps/ptrack/lib/features/lock/delete_ptrack_db_file.dart
  - apps/ptrack/lib/features/lock/delete_ptrack_db_file_stub.dart
  - apps/ptrack/test/features/lock/reset_flow_test.dart
autonomous: true
requirements: []

must_haves:
  truths:
    - "Factory reset path can detect failure to delete the SQLite file; UI or logs do not claim a full wipe when deletion failed."
    - "Reset sequence remains ordered: lock → prefs → onboarding reload → DB close → file delete → navigation."
  artifacts:
    - path: "apps/ptrack/lib/features/lock/delete_ptrack_db_file_io.dart"
      provides: "Non-swallowing result type or rethrow after logging"
      exports: ["deletePtrackDatabaseFileIfExists"]
    - path: "apps/ptrack/test/features/lock/reset_flow_test.dart"
      provides: "Tests for reset orchestration or delete result handling"
      min_lines: 40
  key_links:
    - from: "apps/ptrack/lib/main.dart"
      to: "apps/ptrack/lib/features/lock/delete_ptrack_db_file_io.dart"
      via: "_resetApp"
      pattern: "destructive reset"
---

<objective>
Address **Finding 4** (reset flow critical, DB delete errors swallowed): make database file deletion outcome observable and surface partial failure so users are not misled after "factory reset."

Purpose: Reliability and trust for a local-first app where wipe is a security/privacy action.

Output: Structured result from delete helper; `_resetApp` reacts (e.g. `debugPrint` + optional `SnackBar` via `ScaffoldMessenger` if context available, or callback to show error — executor chooses minimal UX consistent with existing patterns); tests where feasible without full platform IO (prefer injecting `File`/`delete` for test seam or testing the result type mapping).
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/CODE_REVIEW.md
@apps/ptrack/lib/main.dart
@apps/ptrack/lib/features/lock/delete_ptrack_db_file_io.dart
@apps/ptrack/lib/features/lock/delete_ptrack_db_file.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Return result from DB file delete (no silent failure)</name>
  <files>
    apps/ptrack/lib/features/lock/delete_ptrack_db_file_io.dart
    apps/ptrack/lib/features/lock/delete_ptrack_db_file.dart
  </files>
  <action>
    - Introduce a small result type, e.g. `enum PtrackDbDeleteResult { deleted, notFound, failed }` or `sealed class` with optional error message.
    - Change `deletePtrackDatabaseFileIfExists` to return `Future<PtrackDbDeleteResult>` (update stub/non-IO implementation in `delete_ptrack_db_file.dart` if present for tests).
    - On failure, include `Object`/`String` detail for debug logging; **do not** empty-catch without recording failure.
    - Update all call sites (grep `deletePtrackDatabaseFileIfExists`).
  </action>
  <verify>
    <automated>cd apps/ptrack && dart analyze lib/features/lock/delete_ptrack_db_file_io.dart lib/features/lock/delete_ptrack_db_file.dart</automated>
  </verify>
  <done>Delete API returns distinguishable success / not found / failure.</done>
</task>

<task type="tdd">
  <name>Task 2: Tests + _resetApp handles failed delete</name>
  <files>
    apps/ptrack/lib/main.dart
    apps/ptrack/test/features/lock/reset_flow_test.dart
  </files>
  <action>
    - Add tests that are practical in CI:
      - If a test seam exists (e.g. injectable delete function into `LumaApp`), assert that when delete returns `failed`, reset still navigates to onboarding **and** failure is observable (callback invoked, or flag set).
      - If full injection is too invasive for one plan, add **unit tests** for the pure result-mapping helper and a **widget test** that provides a fake `onReset` / delete override if the app exposes `homeOverride`-style hooks — prefer smallest change that gives regression signal.
    - In `_resetApp`, after `close()`, await delete result; on `failed`, `debugPrint` with error; if project already uses logging abstraction, use it. Optional: store last reset warning for a one-shot SnackBar on next frame (only if low-risk).
  </action>
  <verify>
    <automated>cd apps/ptrack && flutter test test/features/lock/reset_flow_test.dart</automated>
  </verify>
  <done>At least one automated test covers failed-delete handling; no silent swallow.</done>
</task>

</tasks>

<verification>
- `flutter test` for new test file passes
- `flutter analyze` clean on touched app files
</verification>

<success_criteria>
- `docs/CODE_REVIEW.md` Finding 4 acceptance criteria met: failure detectable, not silently ignored
- User-visible or debug-visible signal when file delete fails
</success_criteria>

<output>
After completion, create `15-02-SUMMARY.md` in this phase directory.
</output>
