---
phase: 15-code-review
plan: 03
type: tdd
wave: 2
depends_on: []
files_modified:
  - packages/ptrack_data/lib/src/repositories/period_repository.dart
  - packages/ptrack_data/test/period_repository_test.dart
autonomous: true
requirements: []

must_haves:
  truths:
    - "`watchPeriodsWithDays` reload uses O(1–2) SQL round-trips for periods+days (not one query per period)."
    - "Streamed snapshots are identical to prior ordering and content: newest period first, day entries ascending by date per period."
  artifacts:
    - path: "packages/ptrack_data/lib/src/repositories/period_repository.dart"
      provides: "Batched day entry load inside watchPeriodsWithDays.load()"
      exports: ["PeriodRepository"]
    - path: "packages/ptrack_data/test/period_repository_test.dart"
      provides: "Regression tests for watch stream data parity with many periods"
      min_lines: 0
  key_links:
    - from: "packages/ptrack_data/lib/src/repositories/period_repository.dart"
      to: "packages/ptrack_data/lib/src/db/ptrack_database.dart"
      via: "Drift select on dayEntries with single query + in-memory groupBy"
      pattern: "batch fetch"
---

<objective>
Address **Finding 5**: remove N+1 queries in `PeriodRepository.watchPeriodsWithDays` by batch-loading all day entries for the loaded period IDs, then grouping in memory.

Purpose: Reduce jank and battery use as history grows; keep behavior bit-for-bit equivalent for consumers (`HomeViewModel`, `CalendarViewModel`, PDF path).

Output: Refactored `load()` inner function; extended or new tests proving ordering and contents match pre-refactor behavior for multi-period fixtures.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/CODE_REVIEW.md
@packages/ptrack_data/lib/src/repositories/period_repository.dart
@packages/ptrack_data/test/period_repository_test.dart
</context>

<tasks>

<task type="tdd">
  <name>Task 1: TDD — assert watch snapshot matches for N periods</name>
  <files>
    packages/ptrack_data/test/period_repository_test.dart
  </files>
  <action>
    - Add a test that seeds **at least 3–5 periods** each with **multiple** day entries (use existing test helpers / `NativeDatabase.memory()` patterns).
    - Subscribe to `watchPeriodsWithDays()`, take first emission (or use `expectLater` + seed then trigger), assert:
      - Period order: descending by `startUtc` (match current contract).
      - Each period's `dayEntries` sorted ascending by date.
      - Counts and ids match what direct queries would return.
    - Run test against **current** implementation (baseline passes).
  </action>
  <verify>
    <automated>cd packages/ptrack_data && dart test test/period_repository_test.dart</automated>
  </verify>
  <done>New multi-period watch test exists and passes on baseline implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Batch-load day entries and group by periodId</name>
  <files>
    packages/ptrack_data/lib/src/repositories/period_repository.dart
  </files>
  <action>
    - Replace the per-`r.id` loop query with:
      1. If `periodRows` is empty, return `[]` without querying `dayEntries`.
      2. `select(dayEntries)` filtered by `periodId.isIn(periodIds)` (non-empty ids only).
      3. Sort rows by `(periodId, dateUtc)` or group then sort lists per period.
      4. Build `Map<int, List<DayEntry>>` keyed by period id.
      5. Assemble `StoredPeriodWithDays` in the same outer order as today.
    - Preserve `sameSnapshot` deep comparison behavior.
    - Optional: add a dev-only assert or comment documenting "2 queries per refresh."
  </action>
  <verify>
    <automated>cd packages/ptrack_data && dart test test/period_repository_test.dart</automated>
  </verify>
  <done>Full period repository test suite passes; watch test still passes.</done>
</task>

</tasks>

<verification>
- `dart test packages/ptrack_data/test/period_repository_test.dart` passes
- `dart analyze packages/ptrack_data/lib/src/repositories/period_repository.dart` clean
</verification>

<success_criteria>
- Finding 5 acceptance: O(1–2) queries per refresh (document in SUMMARY or code comment)
- No change to public stream contract
</success_criteria>

<output>
After completion, create `15-03-SUMMARY.md` in this phase directory.
</output>
