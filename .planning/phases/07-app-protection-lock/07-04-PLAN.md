---
phase: 07-app-protection-lock
plan: 04
type: execute
wave: 4
depends_on:
  - 07-03
gap_closure: true
files_modified:
  - apps/ptrack/lib/main.dart
  - apps/ptrack/lib/features/lock/lock_gate.dart
  - apps/ptrack/test/features/lock/lock_gate_test.dart
autonomous: false
requirements:
  - LOCK-01
  - LOCK-02

must_haves:
  truths:
    - "When lock engages (background transition or Lock now), the user sees LockScreen, not a route stacked above it (e.g. LockSettingsScreen)."
    - "Background/resume on Android and iOS still sets locked state when app lock is enabled; lifecycle handling is not weaker than 07-03 for supported platforms."
    - "Automated widget test proves a pushed settings route is cleared (or equivalent) so LockScreen is visible after lock now signal."
    - "Human re-run of UAT items 1-10 updates 07-UAT.md with outcomes after fixes."
  artifacts:
    - path: "apps/ptrack/lib/main.dart"
      provides: "Root navigatorKey (or equivalent) so lock can pop overlay routes before locking"
      contains: "navigatorKey"
    - path: "apps/ptrack/lib/features/lock/lock_gate.dart"
      provides: "Lifecycle + lock-now path invokes route pop-to-root (or documented overlay) before setState locked"
      contains: "LockGate"
    - path: "apps/ptrack/test/features/lock/lock_gate_test.dart"
      provides: "Regression: lock visible above pushed route"
      min_lines: 70
  key_links:
    - from: "main.dart (MaterialApp)"
      to: "LockGate"
      via: "navigatorKey passed or callback closes Navigator routes before _lockNowSignal / lifecycle lock"
      pattern: "LockGate"
    - from: "lock_gate.dart"
      to: "Navigator"
      via: "popUntil isFirstRoute (or root overlay) when transitioning to locked"
      pattern: "popUntil"
---

<objective>
Close UAT gaps **LOCK-01** and **LOCK-02** from `07-UAT.md`: lock behavior failed in practice because **LockScreen can render under pushed routes** (e.g. `LockSettingsScreen` on the root `Navigator`). `LockGate` only replaces its **child** (`TabShell`); any `MaterialPageRoute` pushed from inside that subtree stays **on top**, so â€œLock nowâ€ and resume-from-background can appear to do nothing while the user still sees settings.

Purpose: Make lock presentation **globally visible** whenever the app transitions to the locked state, matching `07-CONTEXT.md` (strict resume lock, Lock now without leaving the app).

Output: Navigator-coordinated lock + lifecycle hardening (if needed), regression widget test, refreshed human UAT sign-off.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-app-protection-lock/07-CONTEXT.md
@.planning/phases/07-app-protection-lock/07-UAT.md
@.planning/phases/07-app-protection-lock/07-VERIFICATION.md
@.planning/phases/07-app-protection-lock/07-03-SUMMARY.md
@apps/ptrack/lib/main.dart
@apps/ptrack/lib/features/lock/lock_gate.dart
@apps/ptrack/lib/features/lock/lock_settings_tile.dart
@apps/ptrack/lib/features/lock/lock_settings_screen.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pop root routes before locking (Lock now + lifecycle)</name>
  <files>apps/ptrack/lib/main.dart, apps/ptrack/lib/features/lock/lock_gate.dart</files>
  <action>
Add a **root** `GlobalKey` for `NavigatorState` on `MaterialApp` in `LumaApp` (same `MaterialApp` that hosts `home: LockGate` / onboarding / first log). Pass a **single callback** into `LockGate` (e.g. `onBeforeLock`) that synchronously runs `navigatorKey.currentState?.popUntil` with a predicate that keeps only the first route (see Flutter docs for `Route.isFirst`), when non-null. Call this **only when** `lockService.isEnabled` is true (avoid popping flows not on this navigator; for the current tree, settings push is always on this navigator).

Invoke `onBeforeLock` **immediately before** `setState` that sets `_isLocked` to true in:
- `_lockIfEnabled()` (lifecycle path)
- `_onLockNowSignal()` (Lock now path)

**Lifecycle hardening (Android/iOS only, keep existing `_backgroundLockSupported` guard):** If `onPause` alone is insufficient on some devices, also subscribe to `AppLifecycleListener.onHide` (Flutter 3.x) with the **same** `_lockIfEnabled` body, or document in code why `onPause` is sufficient after testingâ€”**do not** double-lock flicker (guard so transition to locked is idempotent).

Do **not** change PIN/crypto behavior or settings copy except where required for navigation safety.

Match existing import/style conventions; keep the diff focused on navigation + lock presentation.
  </action>
  <verify>
    <automated>cd apps/ptrack; fvm flutter analyze</automated>
    <automated>cd apps/ptrack; fvm flutter test test/features/lock/</automated>
    <manual>Optional: quick smoke on emulatorâ€”open App lock settings, tap Lock now, confirm PIN screen appears on top.</manual>
  </verify>
  <done>Lock engagement paths call pop-to-root (or equivalent) before showing `LockScreen`; `flutter analyze` and `test/features/lock/` pass.</done>
</task>

<task type="auto">
  <name>Task 2: Widget regression - pushed route must not hide lock</name>
  <files>apps/ptrack/test/features/lock/lock_gate_test.dart</files>
  <action>
Extend tests using a **root** `MaterialApp(navigatorKey: ..., home: LockGate(...))`. Push a **dummy** `MaterialPageRoute` (placeholder scaffold) via `navigatorKey.currentState!.push`, pump, then increment the same `ValueNotifier` counter used as `lockNowSignal` with `lockService.isEnabled == true` in prefs. **Expect** `find.byType(LockScreen)` finds one widget and the dummy routeâ€™s unique text is **not** on screen (popped). Cover the case where lock is disabled and signal firesâ€”should **not** pop routes (no spurious pop).

Use existing mock `LockService` pattern from `lock_gate_test.dart`.
  </action>
  <verify>
    <automated>cd apps/ptrack; fvm flutter test test/features/lock/lock_gate_test.dart</automated>
  </verify>
  <done>Test fails on pre-fix behavior (lock under route) and passes with Task 1 implementation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Re-run Phase 7 UAT checklist</name>
  <files>.planning/phases/07-app-protection-lock/07-UAT.md, .planning/REQUIREMENTS.md</files>
  <action>After Tasks 1-2 merge, run the full device checklist, record honest results in 07-UAT.md, and update REQUIREMENTS.md LOCK-01/LOCK-02 if criteria are met.</action>
  <what-built>Navigator-safe lock presentation plus automated regression test.</what-built>
  <how-to-verify>
1. On a **physical device or official emulator** (Android or iOS): `cd apps/ptrack` then `fvm flutter run`.
2. Execute **all 10 items** in `07-03-PLAN.md` Task 2 / `07-VERIFICATION.md` human list (first launch, enable/cancel, background resume, Lock now, wrong PIN, biometrics cancel, disable re-auth, forgot PIN reset, subtitle when off), paying special attention to **Item 4** (resume) and **Item 5** (Lock now) with **LockSettingsScreen open** and closed.
3. Update `.planning/phases/07-app-protection-lock/07-UAT.md`: set per-test results, `uat_outcome`, Gaps section (clear or add new diagnosed gaps with `status: diagnosed` and concrete `missing` if any issue remains).
4. If **LOCK-01** / **LOCK-02** are satisfied, update `.planning/REQUIREMENTS.md` tracker lines for those IDs per project convention.
  </how-to-verify>
  <resume-signal>Type "approved" or paste updated UAT summary + any remaining gap text</resume-signal>
  <verify>
    <automated>cd apps/ptrack; fvm flutter test test/features/lock/</automated>
    <manual>Complete 07-UAT.md updates and optional REQUIREMENTS.md tracker edits per how-to-verify.</manual>
  </verify>
  <done>07-UAT.md reflects retest; executor can mark phase verification complete or file new gaps.</done>
</task>

</tasks>

<verification>
- `fvm flutter analyze` (apps/ptrack) â€” clean
- `fvm flutter test test/features/lock/` â€” all pass
- Human UAT in `07-UAT.md` reflects retest after this fix; requirements tracker updated if passed
</verification>

<success_criteria>
- Lock UI is visible when locking from **Lock now** and after **background** even if user had pushed **App lock** settings (or any route on the root navigator).
- Regression test prevents reintroducing â€œlock under modal route.â€
- UAT document updated; phase requirements **LOCK-01** / **LOCK-02** closable when human signs off.
</success_criteria>

<output>
After completion, create `.planning/phases/07-app-protection-lock/07-04-SUMMARY.md`
</output>
