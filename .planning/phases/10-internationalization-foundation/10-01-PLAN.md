---
phase: 10-internationalization-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/ptrack/pubspec.yaml
  - apps/ptrack/l10n.yaml
  - apps/ptrack/lib/l10n/app_en.arb
  - apps/ptrack/lib/main.dart
autonomous: true
requirements:
  - I18N-01

must_haves:
  truths:
    - "`flutter gen-l10n` succeeds for `apps/ptrack` and CI (`melos exec flutter analyze` / `flutter test`) still passes"
    - "`MaterialApp` (both `homeOverride` and production branches) declares `localizationsDelegates` including `AppLocalizations.delegate` plus Material/Cupertino/Widgets global delegates"
    - "`supportedLocales` is defined for English; unsupported device locales resolve to English template (Phase 10 has no `de` ARB yet)"
    - "Template ARB `app_en.arb` exists under `lib/l10n/` per `l10n.yaml`; key naming follows Flutter defaults (camelCase)"
  artifacts:
    - path: "apps/ptrack/l10n.yaml"
      provides: "gen_l10n configuration (arb-dir, template-arb-file, output class name)"
    - path: "apps/ptrack/lib/l10n/app_en.arb"
      provides: "English template messages (expand in plan 02)"
    - path: "apps/ptrack/lib/main.dart"
      provides: "Localized MaterialApp wiring"
  key_links:
    - from: "apps/ptrack/lib/main.dart"
      to: "package:flutter_gen/gen_l10n/app_localizations.dart"
      via: "import generated localizations"
---

<objective>
Add Flutter **codegen i18n** (`flutter gen-l10n`) to `apps/ptrack`, English template ARB, and wire **`MaterialApp`** localization delegates and locale list so Phase 02 can migrate strings without rework.

Purpose: Satisfy **I18N-01** infrastructure (toolchain + delegates). User-visible migration is **plan 02**.
Output: `l10n.yaml`, `app_en.arb` (seed keys), `pubspec` `generate: true`, `main.dart` updated; documented codegen step for contributors.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-internationalization-foundation/10-CONTEXT.md
@apps/ptrack/pubspec.yaml
@apps/ptrack/lib/main.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: l10n.yaml, pubspec, seed ARB, codegen</name>
  <files>
    apps/ptrack/pubspec.yaml
    apps/ptrack/l10n.yaml
    apps/ptrack/lib/l10n/app_en.arb
  </files>
  <action>
1. Add **`l10n.yaml`** at `apps/ptrack/l10n.yaml`:
   - `arb-dir: lib/l10n`
   - `template-arb-file: app_en.arb`
   - `output-localization-file: app_localizations.dart` (or default)
   - Optional: `nullable-getter: false` if team prefers non-null strings

2. In **`apps/ptrack/pubspec.yaml`** under `flutter:` add **`generate: true`** (keep existing keys).

3. Create **`apps/ptrack/lib/l10n/app_en.arb`** with minimal seed entries used immediately in Task 2:
   - `appTitle` → `"Luma"` (brand stays proper name per CONTEXT)
   - Optionally 1–2 placeholder keys if needed for compile sanity

4. Run **`flutter gen-l10n`** from `apps/ptrack` (or `flutter pub get` which triggers gen). Ensure **`package:flutter_gen/gen_l10n/app_localizations.dart`** (or project-relative import per SDK) resolves in analyzer.

5. If repo policy requires committing generated files, document choice in plan SUMMARY; **default for Flutter 3.x**: generated code under `.dart_tool` / synthetic package — do **not** commit if standard ephemeral gen; ensure **CI** runs `flutter pub get` before analyze (already does via melos).

6. If `melos.yaml` or workspace needs an explicit bootstrap step for codegen, update only if a current CI gap appears after analyze.
  </action>
  <verify>Run `melos exec -c 1 --scope luma -- flutter analyze` from repo root (or equivalent) with zero errors.</verify>
</task>

<task type="auto">
  <name>Task 2: MaterialApp localization wiring</name>
  <files>
    apps/ptrack/lib/main.dart
  </files>
  <action>
1. Import generated `AppLocalizations` and delegate.

2. **Production `MaterialApp`** (non-`homeOverride`):
   - Set `localizationsDelegates` to include at minimum:
     `AppLocalizations.delegate`, `GlobalMaterialLocalizations.delegate`, `GlobalWidgetsLocalizations.delegate`, `GlobalCupertinoLocalizations.delegate`
   - Set `supportedLocales: AppLocalizations.supportedLocales` (or explicit `[Locale('en')]` aligned with ARB)
   - Replace hard-coded `title: 'Luma'` with `onGenerateTitle: (ctx) => AppLocalizations.of(ctx)!.appTitle` **or** `title` from l10n if API allows — use pattern that matches Flutter docs and analyzer.

3. **`homeOverride` `MaterialApp`** (widget tests): add the **same** `localizationsDelegates` and `supportedLocales` so tests can obtain `AppLocalizations` when needed.

4. **Do not** add a language picker (Phase 11).

5. **Locale resolution:** With only `en` ARB, ensure behavior matches CONTEXT: unsupported locales fall back to English — typically satisfied by `supportedLocales` listing English; document in SUMMARY if a custom `localeListResolutionCallback` is added.
  </action>
  <verify>Run `flutter test` for `apps/ptrack` (full package or smoke test) ensuring no localization assertion failures.</verify>
</task>

</tasks>
