---
phase: 10-internationalization-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - apps/ptrack/lib/**/*.dart
  - packages/ptrack_domain/lib/src/prediction/prediction_copy.dart
  - packages/ptrack_domain/lib/**/*.dart
  - apps/ptrack/test/**/*.dart
autonomous: true
requirements:
  - I18N-01

must_haves:
  truths:
    - "No user-visible English string remains in **apps/ptrack/lib** as a raw literal in production UI paths (Text, labels, tooltips, Semantics, SnackBar, Dialog titles/body, list tiles, accessibility labels) — all use `AppLocalizations` or a thin wrapper"
    - "`ptrack_domain` does **not** add a `flutter` dependency; user-facing sentences from `prediction_copy.dart` (and any similar) are surfaced via **app-layer** mappers using `AppLocalizations` + structured payloads / enums"
    - "Widget tests wrap UIs under `MaterialApp` (or test helper) that supplies the same `localizationsDelegates` and `supportedLocales` as production"
    - "`melos exec ... flutter test` and `flutter analyze` pass repo-wide"
  artifacts:
    - path: "apps/ptrack/lib/l10n/app_en.arb"
      provides: "Complete English catalog for all migrated strings (keys grouped by feature optional)"
    - path: "apps/ptrack/lib/l10n/ (optional helpers)"
      provides: "e.g. context extension `l10n` if used"
    - path: "apps/ptrack/lib/... (feature files)"
      provides: "Localized UI"
  key_links:
    - from: "apps/ptrack/lib/features/..."
      to: "package:flutter_gen/gen_l10n/app_localizations.dart"
      via: "BuildContext + ARB getters"
    - from: "packages/ptrack_domain/lib/src/prediction/prediction_copy.dart"
      to: "apps/ptrack/lib/l10n/prediction_l10n.dart (new)"
      via: "Domain keeps payloads; app maps to ARB strings"
---

<objective>
Migrate **all** user-visible strings in the Flutter app to **ARB**-backed `AppLocalizations`, including **semantics and tooltips**, per **10-CONTEXT**. Refactor **domain prediction copy** so English sentences are not embedded in `ptrack_domain` for user-visible paths.

Purpose: Complete **I18N-01** for the shipped app (English catalog only; **de** is Phase 11).
Output: Expanded `app_en.arb`, updated feature `dart` files, app-side prediction text mapper, green tests/analyze.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-internationalization-foundation/10-CONTEXT.md
@.planning/REQUIREMENTS.md
@packages/ptrack_domain/lib/src/prediction/prediction_copy.dart
@apps/ptrack/lib/features
</context>

<tasks>

<task type="auto">
  <name>Task 1: Domain vs app — prediction and user-facing copy</name>
  <files>
    packages/ptrack_domain/lib/src/prediction/prediction_copy.dart
    packages/ptrack_domain/lib/src/prediction/prediction_algorithm.dart
    apps/ptrack/lib/l10n/app_en.arb
    apps/ptrack/lib/l10n/prediction_localizations.dart
  </files>
  <action>
1. **Constraint:** `ptrack_domain` remains **pure Dart** — no `AppLocalizations`, no `flutter` imports.

2. Refactor **`prediction_copy.dart`** (and **`displayName` on `PredictionAlgorithm`** if user-visible):
   - Option A (preferred): Domain exposes **stable message ids + structured data** (existing `ExplanationFactKind` / payloads). Remove or stop exporting English sentence builders used by UI.
   - Option B: Keep internal English for **tests of domain logic** only if unavoidable; UI **must not** display domain raw strings.

3. Add **`apps/ptrack/lib/l10n/prediction_localizations.dart`** (name flexible): functions like `String describeFact(AppLocalizations l10n, ExplanationStep step)` that switch on `kind` and use **ARB** placeholders for interpolated values (dates, counts, R², etc.). Add all needed keys to **`app_en.arb`** with `{variable}` placeholders per [ARB ICU](https://github.com/google/app-resource-bundle/wiki/ApplicationResourceBundleSpecification) rules.

4. Update **call sites** in `apps/ptrack` (home, calendar, day detail, settings, tests) to use the mapper + `AppLocalizations`.

5. Update **`packages/ptrack_domain/test`** if tests asserted full English sentences — assert on **kind/payload** or move string assertions to app tests with `AppLocalizationsEn` / loaded ARB.
  </action>
  <verify>`flutter test` in `packages/ptrack_domain` and `apps/ptrack` pass.</verify>
</task>

<task type="auto">
  <name>Task 2: Feature sweep — onboarding, first log, shell, lock</name>
  <files>
    apps/ptrack/lib/features/onboarding
    apps/ptrack/lib/features/shell
    apps/ptrack/lib/features/lock
    apps/ptrack/lib/l10n/app_en.arb
  </files>
  <action>
For each file: replace user-visible literals with ARB keys. Include **semanticsLabel**, **tooltip**, **hint** text. Keep **"Luma"** as brand per CONTEXT (dedicated ARB key with value `Luma`).

Cover: onboarding wizard, first log, tab shell / drawer / FAB tooltips, lock screens, PIN sheets, settings tiles that are in these folders.
  </action>
  <verify>Widget tests for these features updated with localization harness; tests pass.</verify>
</task>

<task type="auto">
  <name>Task 3: Feature sweep — home, calendar, logging, settings, export/import</name>
  <files>
    apps/ptrack/lib/features/home
    apps/ptrack/lib/features/calendar
    apps/ptrack/lib/features/logging
    apps/ptrack/lib/features/settings
    apps/ptrack/lib/features/export_import
    apps/ptrack/lib/l10n/app_en.arb
  </files>
  <action>
Same migration pattern as Task 2 for remaining primary flows. Pay attention to:
- **Dynamic strings** (snackbars with embedded values) → ARB placeholders
- **table_calendar** header / button strings if customized in app code
- **Export/import** validation error messages shown to users (must be ARB)
  </action>
  <verify>Run `apps/ptrack` widget tests + `melos exec` test gate.</verify>
</task>

<task type="auto">
  <name>Task 4: Test harness + regression pass</name>
  <files>
    apps/ptrack/test
  </files>
  <action>
1. Add or extend a **test helper** (e.g. `test_helpers/l10n_app.dart`) that pumps widgets with:
   - `MaterialApp` / `CupertinoApp` as appropriate
   - `localizationsDelegates` and `supportedLocales` matching `main.dart`

2. Fix any test that used **`MaterialApp(home: …)`** without delegates.

3. Repo-wide: `melos exec -c 1 -- flutter analyze` and `melos exec -c 1 --dir-exists=test -- flutter test` (per CI).

4. Optional quick grep (document in SUMMARY): search for obvious leftover UI literals in `apps/ptrack/lib` (e.g. `Text('` patterns) — goal zero false positives for user-visible strings; allow const in tests only if documented.
  </action>
  <verify>CI-equivalent commands pass locally.</verify>
</task>

</tasks>
