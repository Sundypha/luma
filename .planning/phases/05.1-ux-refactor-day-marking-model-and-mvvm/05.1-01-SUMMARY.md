---
phase: 05.1-ux-refactor-day-marking-model-and-mvvm
plan: 01
subsystem: domain
tags: [dart, tdd, sealed-class, calendar, period-span]

requires:
  - phase: 04-core-logging
    provides: Period span semantics and logging groundwork for period days
provides:
  - Pure computeMarkDay / computeUnmarkDay over UTC calendar midnights
  - SpanRecord input type and sealed DayMarkOp / DayUnmarkOp results
  - Exhaustive unit tests for create, extend, merge, shrink, split, delete, no-ops, month boundaries
affects:
  - 05.1-02-PLAN.md (repository will map StoredPeriod → SpanRecord and apply ops)
  - 05.1-03 through 05.1-05 (UI / VM consume repository)

tech-stack:
  added: []
  patterns:
    - "Sealed result types for mark/unmark operations (no Flutter or IO in day_marking.dart)"
    - "Merge uses min(id) as keepId and max(id) as absorbId for stable persistence"

key-files:
  created:
    - packages/ptrack_domain/lib/src/period/day_marking.dart
    - packages/ptrack_domain/test/day_marking_test.dart
  modified:
    - packages/ptrack_domain/lib/ptrack_domain.dart

key-decisions:
  - "Merge pair selection uses lowest period id from each adjacency list (end-touch and start-touch) when multiple matches exist"
  - "Day already in a closed span yields MarkNoOp; inclusive start/end count as marked"

patterns-established:
  - "Domain day-marking: normalize with _utcDay, adjacency via _nextDay/_prevDay on span edges"

requirements-completed: [LOG-01, LOG-04, LOG-05]

duration: 25min
completed: 2026-04-06
---

# Phase 05.1 Plan 01: Day-marking domain pure functions Summary

**Sealed `DayMarkOp` / `DayUnmarkOp` types with `computeMarkDay` and `computeUnmarkDay` deriving create, extend, merge, shrink, split, and delete from UTC calendar-day adjacency and containment — TDD with 19 unit tests.**

## Performance

- **Duration:** ~25 min
- **Started:** 2026-04-06 (executor session)
- **Completed:** 2026-04-06
- **Tasks:** 2 (TDD RED + GREEN)
- **Files modified:** 3

## Accomplishments

- Implemented pure span derivation for mark (create / extend / merge / no-op) and unmark (delete / shrink / split / no-op).
- Added `SpanRecord` and barrel export from `ptrack_domain`; no `ptrack_data` dependency.
- Covered month boundaries (Dec 31 ↔ Jan 1), single-day neighbors, and interior splits.

## Task Commits

1. **Task 1: TDD RED — failing tests + types/stubs** - `83703d0` (test)
2. **Task 2: TDD GREEN — full implementation** - `5d88893` (feat)

**Plan metadata:** Planning bundle commit includes this SUMMARY with `STATE.md` and `ROADMAP.md` updates (see `git log` for hash).

_Note: TDD split per executor protocol (test then feat)._

## Files Created/Modified

- `packages/ptrack_domain/lib/src/period/day_marking.dart` — `SpanRecord`, sealed ops, `computeMarkDay`, `computeUnmarkDay`, UTC helpers.
- `packages/ptrack_domain/test/day_marking_test.dart` — Exhaustive cases for all six operations plus boundaries.
- `packages/ptrack_domain/lib/ptrack_domain.dart` — Export `day_marking.dart`.

## Decisions Made

- **Merge ids:** `keepId` = smaller of the two adjacent period ids, `absorbId` = larger, so apply order is deterministic.
- **Already marked:** If the day lies in an existing span (inclusive start/end), return `MarkNoOp` rather than extending.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Domain algorithm ready for `05.1-02`: repository can convert `StoredPeriod` rows to `SpanRecord` and persist results of each op type.

---
*Phase: 05.1-ux-refactor-day-marking-model-and-mvvm*
*Completed: 2026-04-06*

## Self-Check: PASSED

- `packages/ptrack_domain/lib/src/period/day_marking.dart` — FOUND
- `packages/ptrack_domain/test/day_marking_test.dart` — FOUND
- Commits `83703d0`, `5d88893` — FOUND; planning docs commit — FOUND (same branch)
