---
phase: 05.1-ux-refactor-day-marking-model-and-mvvm
plan: 02
subsystem: database
tags: [drift, sqlite, migration, repository, day-marking, transactions]

requires:
  - phase: 05.1-01
    provides: computeMarkDay, computeUnmarkDay, SpanRecord, DayMarkOp, DayUnmarkOp
provides:
  - Schema v3 migration closing all open periods (end_utc from max(day_entries) or start_utc)
  - PeriodRepository.markDay / unmarkDay executing domain ops in single Drift transactions
  - DayMarkOutcome (success with optional periodId, failure with reason)
  - Integration tests for migration and mark/unmark including merge/split day-entry moves
affects:
  - 05.1-03 (ViewModels can call repository mark/unmark)
  - UI layers that will toggle days instead of explicit span edits

tech-stack:
  added: []
  patterns:
    - "Legacy null endUtc in span list: treat as startUtc for domain SpanRecord.end"
    - "Merge: UPDATE keep period, bulk reassign day_entries from absorbId, DELETE absorb row"

key-files:
  created: []
  modified:
    - packages/ptrack_data/lib/src/db/ptrack_database.dart
    - packages/ptrack_data/test/migration_test.dart
    - packages/ptrack_data/lib/src/repositories/period_repository.dart
    - packages/ptrack_data/lib/ptrack_data.dart
    - packages/ptrack_data/test/period_repository_test.dart

key-decisions:
  - "Kept endUtc nullable in Drift table; migration only fills NULL rows so existing Dart PeriodSpan(endUtc: null) call sites stay valid"

patterns-established:
  - "v2→v3 migration uses raw UPDATE with subquery (no TableMigration / column type change)"

requirements-completed: [LOG-01, LOG-04, LOG-05, LOG-06]

duration: 30min
completed: 2026-04-06
---

# Phase 05.1 Plan 02: Repository and migration summary

**Schema v3 closes open periods in SQL; PeriodRepository applies day-marking domain ops transactionally with correct day_entry reassignment on merge and split.**

## Performance

- **Duration:** 30 min
- **Started:** 2026-04-06T00:00:00Z (approx.)
- **Completed:** 2026-04-06T00:00:00Z (approx.)
- **Tasks:** 2
- **Files modified:** 5

## Accomplishments

- `ptrackSupportedSchemaVersion` is 3; `onUpgrade` closes `end_utc IS NULL` rows using `COALESCE(MAX(date_utc), start_utc)`.
- `markDay` / `unmarkDay` load ordered spans, call domain compute functions, and execute MarkCreate/Extend/Merge and UnmarkDelete/Shrink/Split in `_db.transaction()`.
- Migration and repository integration tests cover open-without-entries, open-with-entries, closed periods, merge with entry move, split with entry distribution, edge shrink with entry removal.

## Task Commits

1. **Task 1: Database migration v2→v3 — close open periods** — `0698c49` (feat)
2. **Task 2: Repository markDay/unmarkDay methods** — `c0a8b4f` (feat)

**Plan metadata:** Planning-only commit follows task commits (message `docs(05.1-02): Complete repository and migration plan`).

## Files Created/Modified

- `packages/ptrack_data/lib/src/db/ptrack_database.dart` — schema 3 + `from < 3` SQL migration in transaction
- `packages/ptrack_data/test/migration_test.dart` — v1/v2 expectations for closed periods; raw v2→v3 cases
- `packages/ptrack_data/lib/src/repositories/period_repository.dart` — `DayMarkOutcome`, `markDay`, `unmarkDay`, helpers
- `packages/ptrack_data/lib/ptrack_data.dart` — export `DayMarkOutcome` types
- `packages/ptrack_data/test/period_repository_test.dart` — `markDay/unmarkDay` group (six scenarios)

## Decisions Made

- Left `endUtc` nullable in `tables.dart` per plan so in-memory constructs and legacy mappers stay unchanged; migrated DBs have no NULL `end_utc` rows.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- Bridging merge test initially used gaps of two days (Mar1, Mar5, Mar3), which does not satisfy adjacency in `computeMarkDay`; adjusted setup to spans Mar1–2 and Mar4–5 with Mar3 as bridge.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Data layer can execute day toggles; **05.1-03** can wire ViewModels to `PeriodRepository.markDay` / `unmarkDay`.

---
*Phase: 05.1-ux-refactor-day-marking-model-and-mvvm*
*Completed: 2026-04-06*

## Self-Check: PASSED

- `packages/ptrack_data/lib/src/db/ptrack_database.dart` — FOUND
- `packages/ptrack_data/lib/src/repositories/period_repository.dart` — FOUND
- Commits `0698c49`, `c0a8b4f` — FOUND (`git log --oneline -5`)
