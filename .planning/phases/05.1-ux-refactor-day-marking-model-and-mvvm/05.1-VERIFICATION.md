---
phase: 05.1-ux-refactor-day-marking-model-and-mvvm
verified: 2026-04-06T12:00:00Z
status: human_needed
score: 29/30 plan must-have truths verified (1 uncertain: full flutter test run)
re_verification: false
human_verification:
  - test: "Run full test suite with Flutter SDK (e.g. fvm flutter test from repo / melos)"
    expected: "All packages pass; no regressions after 05.1-05 Task 1"
    why_human: "Verifier environment had Dart on PATH but not flutter; plan 05.1-05 requires flutter test across packages"
  - test: "05.1-05 Task 2 — full UX UAT checklist (per 05.1-05-SUMMARY / ROADMAP)"
    expected: "Calendar day-tap flows, FAB, home quick actions, first-log range marking, symptom form, future-day gating behave as specified"
    why_human: "Explicitly pending per STATE/ROADMAP; visual and end-to-end behavior not fully automatable here"
---

# Phase 05.1: UX refactor — day-marking model and MVVM — Verification Report

**Phase goal:** Replace explicit start/end period actions with a day-marking toggle model (mark/unmark days, system derives spans) and refactor the presentation layer from ad-hoc `StatefulWidget` state to MVVM with `ChangeNotifier` ViewModels — consistent day-tap behavior, future-day gating, simplified symptom form, no open periods.

**Verified:** 2026-04-06T12:00:00Z  
**Status:** human_needed  
**Re-verification:** No — initial verification (no prior `*VERIFICATION.md` in this folder).

## Goal achievement

### Observable truths (aggregated from plans 01–05)

| # | Truth | Status | Evidence |
|---|--------|--------|----------|
| 1 | `computeMarkDay` / `computeUnmarkDay` implement create, extend, merge, shrink, split, delete | ✓ VERIFIED | `packages/ptrack_domain/lib/src/period/day_marking.dart`; `day_marking_test.dart` (262 lines) exercises operations |
| 2 | Barrel exports day marking | ✓ VERIFIED | `ptrack_domain.dart` exports `src/period/day_marking.dart` |
| 3 | After v2→v3 migration, open periods closed (no NULL `end_utc` left from migration path) | ✓ VERIFIED | `ptrack_database.dart` `onUpgrade` `from < 3` runs SQL `COALESCE(MAX(day_entries.date_utc), start_utc)`; `migration_test.dart` |
| 4 | `markDay` / `unmarkDay` run in single DB transaction using domain ops | ✓ VERIFIED | `period_repository.dart` `_db.transaction` + `computeMarkDay` / `computeUnmarkDay`; merge/split reassign rows |
| 5 | `insertPeriod` / `updatePeriod` / `deletePeriod` still present | ✓ VERIFIED | `period_repository.dart` |
| 6 | Calendar + Home use `ListenableBuilder`, not `StreamBuilder` | ✓ VERIFIED | `calendar_screen.dart`, `home_screen.dart`; VMs extend `ChangeNotifier`, subscribe to `watchPeriodsWithDays` |
| 7 | `TabShell` creates/disposes VMs; FAB uses `markToday` / `showSymptomFormSheet` | ✓ VERIFIED | `tab_shell.dart` |
| 8 | Every calendar day tap opens day detail | ✓ VERIFIED | `onDaySelected` → `showDayDetailSheet` in `calendar_screen.dart` |
| 9 | Day detail: empty past/today → “I had my period”; symptoms / remove / edit / clear copy | ✓ VERIFIED | `day_detail_sheet.dart` (579 lines) |
| 10 | Future days view-only messaging | ✓ VERIFIED | `isFuture` branches, `_buildEmptyFuture` copy |
| 11 | Symptom sheet single-purpose (flow/pain/mood/notes), no date/period UI | ✓ VERIFIED | `symptom_form_sheet.dart` doc + 212 lines; params carry `day`/`periodId` only |
| 12 | Day detail wires `showSymptomFormSheet`, `markDay`, `unmarkDay` | ✓ VERIFIED | grep hits in `day_detail_sheet.dart` |
| 13 | `logging_bottom_sheet.dart` deleted | ✓ VERIFIED | file absent under `apps/ptrack/lib/features/logging/` |
| 14 | No Dart imports of `logging_bottom_sheet` | ✓ VERIFIED | `rg logging_bottom_sheet *.dart` → no matches |
| 15 | `FirstLogScreen` uses `repository.markDay` | ✓ VERIFIED | loop over date range calling `markDay` in `first_log_screen.dart` |
| 16 | Home quick actions match INVESTIGATION §3.7 | ✓ VERIFIED | `today_card.dart` strings: “I had my period today”, “Add symptoms for today”, “Edit today's log”; `_handleTodayQuickAction` + `showSymptomFormSheet` |
| 17 | All tests pass after dead code removal | ? UNCERTAIN | `dart analyze` clean for `apps/ptrack`, `packages/ptrack_data`; full `flutter test` not executed (Flutter not on PATH in verifier shell) |

**Score:** 29/30 truths verified at code level; 1 uncertain pending local `flutter test`.

### Required artifacts (sampling / min_lines)

| Artifact | Expected | Status |
|----------|-----------|--------|
| `day_marking.dart` | Pure mark/unmark ops | ✓ |
| `day_marking_test.dart` | ≥150 lines | ✓ (262) |
| `period_repository.dart` | `markDay` / `unmarkDay` / `DayMarkOutcome` | ✓ |
| `calendar_view_model.dart` / `home_view_model.dart` | `ChangeNotifier` + stream subscription | ✓ |
| `calendar_view_model_test.dart` | ≥80 lines | ✓ (100) |
| `home_view_model_test.dart` | ≥60 lines | ✓ (83) |
| `symptom_form_sheet.dart` | ≥80 lines | ✓ (212) |
| `day_detail_sheet.dart` | ≥150 lines | ✓ (579) |

### Key links (manual)

| From | To | Via | Status |
|------|-----|-----|--------|
| `period_repository.dart` | `day_marking.dart` | `computeMarkDay` / `computeUnmarkDay` | ✓ WIRED |
| `ptrack_database.dart` | migrations | `from < 3` | ✓ WIRED |
| `calendar_view_model.dart` | `period_repository.dart` | `watchPeriodsWithDays` | ✓ WIRED |
| `tab_shell.dart` | view models | create / dispose | ✓ WIRED |
| `calendar_screen.dart` | `day_detail_sheet.dart` | `showDayDetailSheet` | ✓ WIRED |
| `day_detail_sheet.dart` | symptom form / repository | `showSymptomFormSheet`, `markDay`, `unmarkDay` | ✓ WIRED |
| `home_screen.dart` | `symptom_form_sheet.dart` | `showSymptomFormSheet` | ✓ WIRED |
| `first_log_screen.dart` | repository | `markDay` | ✓ WIRED |

**Note:** Plan 01 key link cited `PeriodSpan` inside `day_marking.dart`; implementation uses `SpanRecord` (domain-only). Behavior and tests still satisfy the goal.

## Requirements coverage

All IDs appear in plan frontmatter across 05.1-01 … 05.1-05. Mapped to implementation evidence:

| Requirement | Source plan(s) | Status | Evidence |
|-------------|----------------|--------|----------|
| LOG-01 | 01, 02, 05 | ✓ SATISFIED | `markDay` / span derivation; first log marks days; day detail marks |
| LOG-02 | 04, 05 | ✓ SATISFIED | `symptom_form_sheet` / ViewModel — optional symptom fields |
| LOG-03 | 04, 05 | ✓ SATISFIED | Same form + save paths |
| LOG-04 | 01, 02, 05 | ✓ SATISFIED | `unmarkDay` split/shrink/delete + transactional updates |
| LOG-05 | 01, 02, 05 | ✓ SATISFIED | Domain ops prevent invalid mark/unmark; repository validation layer unchanged for writes |
| LOG-06 | 02, 04, 05 | ✓ SATISFIED | Entries tied to UTC days / periods; retroactive marking supported |
| CAL-01 | 03 | ✓ SATISFIED | `TableCalendar` + month swipe in `calendar_screen.dart` |
| CAL-03 | 04 | ✓ SATISFIED | Day tap → `showDayDetailSheet` |
| CAL-05 | 03 | ✓ SATISFIED | `_recompute` on stream in `CalendarViewModel` |
| HOME-01 | 03 | ✓ SATISFIED | `computeCyclePosition` + prediction in `HomeViewModel` |
| HOME-02 | 03 | ✓ SATISFIED | `TodayCard` + `todayEntry` |
| HOME-03 | 03, 04, 05 | ✓ SATISFIED | `TodayCard` + FAB in `tab_shell.dart` |
| HOME-04 | 03 | ✓ SATISFIED | No “cycle health” / score copy in `home/` (grep) |

**Orphaned requirements:** None — every listed ID is claimed by at least one 05.1 plan.

**REQUIREMENTS.md:** Checkbox lines show these IDs as done and mapped to Phase 5/4; 05.1 is the inserted refinement. No conflict found for verification purposes.

## Anti-patterns

| File | Pattern | Severity | Notes |
|------|---------|----------|--------|
| `packages/ptrack_domain/test/prediction_copy_test.dart` | deprecated `RegExp` info | ℹ️ Info | Unrelated to 05.1 feature paths |

No `TODO` / `FIXME` / placeholder stubs found in `day_detail_sheet`, `symptom_form_view_model`, or core 05.1 views checked.

## Human verification required

See YAML `human_verification` above: run `flutter test` with project FVM/SDK, then complete **05.1-05 Task 2** UAT before treating the phase as closed (per `STATE.md` / ROADMAP).

## Gaps summary

No **code** gaps found that block the stated architecture or day-marking model. Remaining gap is **process / confirmation**: full automated test run in a Flutter-equipped environment, plus explicit human UAT for Task 2.

---

_Verified: 2026-04-06T12:00:00Z_  
_Verifier: Claude (gsd-verifier subagent)_
