---
phase: 05.1-ux-refactor-day-marking-model-and-mvvm
plan: 03
subsystem: ui
tags: [flutter, mvvm, changenotifier, listenablebuilder, table_calendar]

requires:
  - phase: 05.1-ux-refactor-day-marking-model-and-mvvm
    provides: markDay/unmarkDay repository API and schema v3 from 05.1-02
provides:
  - CalendarViewModel and HomeViewModel with repository stream subscription
  - CalendarScreen and HomeScreen as StatelessWidget + ListenableBuilder
  - TabShell VM lifecycle and FAB mark-today vs logging routing
affects:
  - 05.1-04 (symptom sheet replaces logging bottom sheet)
  - 05.1-05 (dead code removal)

tech-stack:
  added: []
  patterns:
    - "ChangeNotifier ViewModels owned by TabShell; cancel stream subscriptions in dispose"
    - "Sheets receive repository/calendar via VM getters for constructor injection without prop drilling database"

key-files:
  created:
    - apps/ptrack/lib/features/calendar/calendar_view_model.dart
    - apps/ptrack/lib/features/home/home_view_model.dart
    - apps/ptrack/test/features/calendar/calendar_view_model_test.dart
    - apps/ptrack/test/features/home/home_view_model_test.dart
  modified:
    - apps/ptrack/lib/features/calendar/calendar_screen.dart
    - apps/ptrack/lib/features/home/home_screen.dart
    - apps/ptrack/lib/features/shell/tab_shell.dart
    - apps/ptrack/lib/main.dart
    - apps/ptrack/test/features/calendar/calendar_screen_test.dart
    - apps/ptrack/test/logging_test.dart
    - apps/ptrack/test/widget_test.dart

key-decisions:
  - "Expose hasInitialEvent and loadError on ViewModels so ListenableBuilder can mirror prior StreamBuilder loading and error UI."
  - "Expose repository and calendar on ViewModels so calendar/home sheets keep call sites simple without passing database."

patterns-established:
  - "TabShell FAB: ListenableBuilder on HomeViewModel; markToday when !isTodayMarked else showLoggingBottomSheet."
  - "Widget tests that share one broadcast stream for watchPeriodsWithDays must re-emit after new subscribers (e.g. LoggingBottomSheet) attach."

requirements-completed: [CAL-01, CAL-05, HOME-01, HOME-02, HOME-03, HOME-04]

duration: 30min
completed: 2026-04-06
---

# Phase 05.1 Plan 03: MVVM ViewModels and ListenableBuilder Summary

**CalendarViewModel and HomeViewModel drive home and calendar tabs via ChangeNotifier streams; TabShell FAB marks today first or opens logging when today already lies in a period span.**

## Performance

- **Duration:** 30 min
- **Started:** 2026-04-06T00:00:00Z
- **Completed:** 2026-04-06T00:35:00Z
- **Tasks:** 2
- **Files modified:** 11 (4 created, 7 modified in app/tests)

## Accomplishments

- Moved prediction, day map, cycle position, and today-entry logic into testable ViewModels subscribed to `watchPeriodsWithDays`.
- Replaced `StreamBuilder` on calendar and home with `ListenableBuilder`; removed `database` from `TabShell` and `main.dart`.
- Updated FAB behavior, calendar screen tests (real ViewModel + mock repo), and logging/widget tests for mark-today flow.

## Task Commits

Each task was committed atomically:

1. **Task 1: CalendarViewModel + HomeViewModel with unit tests** — `260727a` (feat)
2. **Task 2: Refactor views to MVVM — CalendarScreen, HomeScreen, TabShell** — `950520a` (feat)

**Plan metadata:** Same commit as this summary file (`docs(05.1-03): complete MVVM plan execution summary`).

## Files Created/Modified

- `apps/ptrack/lib/features/calendar/calendar_view_model.dart` — Stream-backed calendar state, selection, mark/unmark commands.
- `apps/ptrack/lib/features/home/home_view_model.dart` — Cycle position, today entry, `isTodayMarked`, `markToday`.
- `apps/ptrack/lib/features/calendar/calendar_screen.dart` — StatelessWidget + `ListenableBuilder`.
- `apps/ptrack/lib/features/home/home_screen.dart` — Same; uses VM for cycle copy and `TodayCard`.
- `apps/ptrack/lib/features/shell/tab_shell.dart` — Creates/disposes VMs; FAB routing and tooltips.
- `apps/ptrack/lib/main.dart` — `TabShell` without `database` parameter.
- `apps/ptrack/test/features/calendar/calendar_view_model_test.dart` — Mock repo + broadcast stream tests.
- `apps/ptrack/test/features/home/home_view_model_test.dart` — Marked-today and `markToday` tests.
- `apps/ptrack/test/features/calendar/calendar_screen_test.dart` — Pumps `CalendarViewModel` with mock repo.
- `apps/ptrack/test/logging_test.dart` — Mark-today vs sheet; closed span fixture; broadcast re-emit for sheet.
- `apps/ptrack/test/widget_test.dart` — `TabShell` constructor without database.

## Decisions Made

- Kept loading and error gates on ViewModels (`hasInitialEvent`, `loadError`) so UX matches the old `StreamBuilder` behavior before first repository emission or on stream errors.
- Exposed `repository` and `calendar` on ViewModels for `showLoggingBottomSheet` / `showDayDetailSheet` without threading `PtrackDatabase` through the shell.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing critical] Loading and error state on ViewModels**

- **Found during:** Task 2 (screen refactor)
- **Issue:** Plan did not list loading/error fields; removing `StreamBuilder` would drop waiting and error UI.
- **Fix:** Added `hasInitialEvent`, `loadError`, and stream `onError` handling on both ViewModels; screens branch like the prior builder.
- **Files modified:** `calendar_view_model.dart`, `home_view_model.dart`, `calendar_screen.dart`, `home_screen.dart`
- **Verification:** Widget and unit tests pass.
- **Committed in:** `950520a`

**2. [Rule 3 - Blocking] Sheet tests with shared broadcast stream**

- **Found during:** Task 2 (`logging_test.dart` mark-today then sheet)
- **Issue:** `LoggingBottomSheet` subscribes to `watchPeriodsWithDays()` after emissions already fired on a broadcast mock; `_periodsStreamReady` never flipped and `pumpAndSettle` timed out.
- **Fix:** Re-emit current snapshot after the sheet subscribes; use a closed span including today so FAB opens sheet in start-new-period mode where tests expect `insertPeriod` validation.
- **Files modified:** `test/logging_test.dart`
- **Verification:** `flutter test` passes.
- **Committed in:** `950520a`

---

**Total deviations:** 2 auto-fixed (1 missing critical, 1 blocking)

**Impact on plan:** Preserves UX parity and stable tests; no product scope change beyond documented test fixtures.

## Issues Encountered

None beyond the broadcast-stream test timing above (resolved in deviation 2).

## User Setup Required

None.

## Next Phase Readiness

- Ready for `05.1-04`: replace logging sheet entry points with `SymptomFormSheet` using the same VM/shell wiring.
- ViewModels already expose `markDay` / `unmarkDay` on calendar for future day-detail actions.

---

*Phase: 05.1-ux-refactor-day-marking-model-and-mvvm*

*Completed: 2026-04-06*

## Self-Check: PASSED

- `05.1-03-SUMMARY.md` present at `.planning/phases/05.1-ux-refactor-day-marking-model-and-mvvm/05.1-03-SUMMARY.md`
- Task commits `260727a`, `950520a` and docs commit for this summary present in `git log`
