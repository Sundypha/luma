---
phase: 05.1-ux-refactor-day-marking-model-and-mvvm
plan: 05
type: execute
wave: 5
depends_on: ["05.1-04"]
files_modified:
  - apps/ptrack/lib/features/logging/logging_bottom_sheet.dart
  - apps/ptrack/lib/features/onboarding/first_log_screen.dart
  - apps/ptrack/lib/features/shell/tab_shell.dart
  - apps/ptrack/lib/features/home/home_screen.dart
  - apps/ptrack/test/features/onboarding/first_log_screen_test.dart
  - apps/ptrack/test/logging_test.dart
  - apps/ptrack/test/widget_test.dart
autonomous: false
requirements:
  - LOG-01
  - LOG-02
  - LOG-03
  - LOG-04
  - LOG-05
  - LOG-06
  - CAL-01
  - CAL-03
  - CAL-05
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04

must_haves:
  truths:
    - "logging_bottom_sheet.dart is deleted — no file exists at that path"
    - "No import of logging_bottom_sheet.dart remains anywhere in the codebase"
    - "first_log_screen uses repository.markDay instead of showLoggingBottomSheet"
    - "All tests pass after dead code removal (flutter test across all packages)"
    - "Home quick actions match INVESTIGATION.md §3.7"
  artifacts:
    - path: "apps/ptrack/lib/features/logging/logging_bottom_sheet.dart"
      provides: "DELETED — must not exist"
    - path: "apps/ptrack/lib/features/onboarding/first_log_screen.dart"
      provides: "Updated to use markDay for first period creation"
    - path: "apps/ptrack/test/logging_test.dart"
      provides: "Updated tests for new FAB + symptom form behavior"
  key_links:
    - from: "apps/ptrack/lib/features/onboarding/first_log_screen.dart"
      to: "packages/ptrack_data/lib/src/repositories/period_repository.dart"
      via: "Calls repository.markDay for first period creation"
      pattern: "markDay"
    - from: "apps/ptrack/lib/features/home/home_screen.dart"
      to: "apps/ptrack/lib/features/logging/symptom_form_sheet.dart"
      via: "Home quick actions open symptom form via showSymptomFormSheet"
      pattern: "showSymptomFormSheet"
---

<objective>
Remove the old logging_bottom_sheet.dart (1143 lines of dead code), update first_log_screen to use the day-marking model, finalize home quick actions, fix all remaining references, and verify the complete refactor with a human checkpoint.

Purpose: Clean break from the old model. No dual code paths. Every import and test references the new architecture.
Output: Deleted logging sheet, updated first-log screen, all tests green, human verification of the full UX.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.1-ux-refactor-day-marking-model-and-mvvm/INVESTIGATION.md (§3.6–3.8 — FAB, Home Quick Actions, Migration)
@.planning/phases/05.1-ux-refactor-day-marking-model-and-mvvm/05.1-04-SUMMARY.md
@apps/ptrack/lib/features/logging/logging_bottom_sheet.dart (to delete)
@apps/ptrack/lib/features/onboarding/first_log_screen.dart (to update)
@apps/ptrack/lib/features/shell/tab_shell.dart (finalize FAB)
@apps/ptrack/lib/features/home/home_screen.dart (finalize quick actions)
@apps/ptrack/lib/features/home/today_card.dart (may need onLogToday callback update)
@apps/ptrack/test/logging_test.dart (to update)
@apps/ptrack/test/features/onboarding/first_log_screen_test.dart (to update)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete logging_bottom_sheet.dart, update first_log_screen, finalize home actions</name>
  <files>
    apps/ptrack/lib/features/logging/logging_bottom_sheet.dart
    apps/ptrack/lib/features/onboarding/first_log_screen.dart
    apps/ptrack/lib/features/shell/tab_shell.dart
    apps/ptrack/lib/features/home/home_screen.dart
    apps/ptrack/lib/features/home/today_card.dart
    apps/ptrack/test/features/onboarding/first_log_screen_test.dart
    apps/ptrack/test/logging_test.dart
    apps/ptrack/test/widget_test.dart
  </files>
  <action>
    **Step 1: Find and catalog all imports of `logging_bottom_sheet.dart`:**
    Run `rg "logging_bottom_sheet" apps/ptrack/lib apps/ptrack/test` to find every reference. Expected locations: tab_shell.dart, calendar_screen.dart, home_screen.dart, day_detail_sheet.dart, first_log_screen.dart, logging_test.dart, possibly widget_test.dart.

    **Step 2: Remove remaining references from views (most already replaced in Plans 03-04):**
    - `tab_shell.dart`: Remove `import '../logging/logging_bottom_sheet.dart'`. The FAB should already call homeVm.markToday() (from Plan 03). If there's still a fallback to showLoggingBottomSheet for "already marked today", replace with `showSymptomFormSheet(context, repository: ..., day: todayUtc, periodId: todayPeriodId)`. Get todayPeriodId from homeVm — add a `int? todayPeriodId` getter to HomeViewModel that returns the period covering today.
    - `home_screen.dart`: Remove logging_bottom_sheet import. Update `TodayCard.onLogToday` callback:
      - If today not marked: call `viewModel.markToday()` then open symptom form.
      - If today marked, no entry: open symptom form for today.
      - If today marked + entry: open symptom form pre-filled (edit mode).
      These match INVESTIGATION.md §3.7 home quick actions. Get periodId from viewModel.
    - `today_card.dart`: If `onLogToday` callback signature needs to change (e.g., to pass different params), update it. Keep it as a simple `VoidCallback` if the parent handles the logic.
    - `calendar_screen.dart`: Should already be clean from Plan 04. Verify no logging_bottom_sheet import remains.
    - `day_detail_sheet.dart`: Should already be clean from Plan 04. Verify.

    **Step 3: Update first_log_screen.dart:**
    Currently calls `showLoggingBottomSheet` to create the first period. Replace with:
    1. Show a date picker (or keep the existing date picker UI).
    2. When user picks a date, call `repository.markDay(selectedDate)` instead of `repository.insertPeriod(PeriodSpan(startUtc: date, endUtc: null))`.
    3. After markDay succeeds, call `widget.onComplete()` to navigate to home.
    4. Optionally offer to open symptom form for the marked day.

    **Step 4: Delete `logging_bottom_sheet.dart`.**
    This file (1143 lines) contains:
    - `_SheetCreateIntent` enum — dead
    - `_ongoingUnclosedAsOfToday` function — dead
    - `_promptOrphanDayEntries` — dead (orphan handling now in repository transactions)
    - `showLoggingBottomSheet` function — dead
    - `LoggingBottomSheet` widget — dead
    Delete the entire file.

    **Step 5: Update tests:**
    - `logging_test.dart`: This tests TabShell FAB behavior. Rewrite to test the new "Mark today" behavior: mock HomeViewModel, verify markToday() is called when FAB tapped and today is not marked. Verify symptom form opens when today IS marked.
    - `first_log_screen_test.dart`: Update to expect `repository.markDay(date)` call instead of `repository.insertPeriod(...)`.
    - `widget_test.dart`: If it imports or references logging_bottom_sheet, update.

    **Step 6: Run full test suite across all packages to verify nothing is broken:**
    ```
    melos exec -- flutter test
    ```

    **Step 7: Run analyzer to ensure no unused imports or dead references:**
    ```
    melos exec -- flutter analyze
    ```
  </action>
  <verify>
    <automated>cd apps/ptrack && flutter analyze && flutter test -r expanded</automated>
    <manual>Run `rg "logging_bottom_sheet" apps/` — must return zero results</manual>
  </verify>
  <done>logging_bottom_sheet.dart deleted. No reference to it anywhere. first_log_screen uses markDay. All tests pass. Analyzer clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete UX refactor</name>
  <action>
Human verification of the full Phase 05.1 UX refactor. Claude has automated everything in Plans 01–05 Task 1. This checkpoint confirms the new day-marking model, MVVM architecture, and simplified symptom form work correctly on a real device/emulator.

What was built: Day-marking toggle model (mark/unmark days, system derives spans), MVVM with ChangeNotifier ViewModels (CalendarViewModel, HomeViewModel, SymptomFormViewModel), consistent day-tap behavior (every tap → day detail), future-day gating, simplified symptom form, "Mark today" FAB, dead code removal (logging_bottom_sheet.dart deleted).
  </action>
  <verify>
    <manual>
Run the app: `cd apps/ptrack &amp;&amp; fvm flutter run`

**Calendar tab:**
1. Tap an empty past day → day detail shows "I had my period" button. Tap it → day gets marked, calendar shows period band for that single day.
2. Tap the day before the marked day → "I had my period" → period extends to 2 days.
3. Tap the marked day → shows "Add symptoms" + "Remove this day" + "Delete entire period".
4. Tap "Add symptoms" → symptom form opens (flow/pain/mood/notes only, no date picker). Save.
5. Tap the day with symptoms → shows data + "Edit" + "Clear symptoms" + "Remove this day".
6. Tap "Remove this day" on a middle day of a 3+ day period → period splits into two.
7. Tap a future day → view-only, no action buttons.
8. If predictions are showing: tap a future predicted day → shows prediction card, no actions.
9. Tap a past predicted day → shows prediction card + "I had my period".

**Home tab:**
10. Verify cycle position and today card display correctly.
11. If today not marked: "I had my period today" quick action works.
12. If today marked: "Add symptoms" or "Edit today's log" quick action works.

**FAB:**
13. On home tab, tap FAB → marks today (or opens symptom form if already marked).

**First log (fresh start — optional):**
14. Clear app data. Complete onboarding. First log screen → pick a date → marks that day.
    </manual>
  </verify>
  <done>All 14 manual checks pass. Day-marking model works end-to-end. No regressions in existing functionality. Type "pass" or describe issues.</done>
</task>

</tasks>

<verification>
melos exec -- flutter test
melos exec -- flutter analyze
All packages: zero failures, zero analyzer issues.
</verification>

<success_criteria>
- logging_bottom_sheet.dart does not exist
- `rg "logging_bottom_sheet" apps/` returns zero results
- first_log_screen uses markDay
- All tests pass across all packages
- Human verification: day-marking UX works as designed
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-ux-refactor-day-marking-model-and-mvvm/05.1-05-SUMMARY.md`
</output>
