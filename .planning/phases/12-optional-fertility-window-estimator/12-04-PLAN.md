---
phase: 12-optional-fertility-window-estimator
plan: 04
type: execute
wave: 3
depends_on: [12-01, 12-02, 12-03]
files_modified:
  - apps/ptrack/lib/features/home/home_view_model.dart
  - apps/ptrack/lib/features/home/home_screen.dart
  - apps/ptrack/lib/features/shell/tab_shell.dart
autonomous: false
requirements: [FERT-03, FERT-04]

must_haves:
  truths:
    - "Home screen shows fertile window card with date range when enabled and data available"
    - "Home card has subtle 'Estimate only' footer as persistent reminder"
    - "Suggestion card appears on home for users who have not enabled or dismissed it"
    - "Suggestion card shows grayed-out/explained state when insufficient cycle data"
    - "Dismissing suggestion card permanently hides it — only discoverable via settings afterward"
    - "Toggling fertility off removes the home card and stops showing the suggestion card prompt"
  artifacts:
    - path: "apps/ptrack/lib/features/home/home_view_model.dart"
      provides: "HomeViewModel fertility window computation, suggestion card state"
      contains: "FertilityWindowCalculator"
    - path: "apps/ptrack/lib/features/home/home_screen.dart"
      provides: "Fertility window card, suggestion card widgets"
      contains: "fertilityHomeCardTitle"
    - path: "apps/ptrack/lib/features/shell/tab_shell.dart"
      provides: "onFertilityToggled callback wired to both VMs"
      contains: "updateFertilityEnabled"
  key_links:
    - from: "apps/ptrack/lib/features/home/home_view_model.dart"
      to: "FertilityWindowCalculator"
      via: "_recompute computes fertile window"
      pattern: "FertilityWindowCalculator\\.compute"
    - from: "apps/ptrack/lib/features/home/home_screen.dart"
      to: "HomeViewModel"
      via: "reads fertileWindow and suggestion state"
      pattern: "viewModel\\.fertileWindow"
    - from: "apps/ptrack/lib/features/shell/tab_shell.dart"
      to: "HomeViewModel.updateFertilityEnabled"
      via: "onFertilityToggled callback from settings"
      pattern: "updateFertilityEnabled"
---

<objective>
Add the fertile window card and suggestion card to the home screen, wire the fertility toggle callback through tab_shell to both ViewModels.

Purpose: FERT-03 (home visualization) and FERT-04 (clean disable removes visuals). The suggestion card provides first-use discoverability per CONTEXT decision ("always available from first use, grayed out if not enough data"). The tab_shell callback ensures toggling in settings immediately updates both calendar and home.
Output: Home shows fertility card when enabled, suggestion card for discovery, and toggle callback wiring.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-optional-fertility-window-estimator/12-CONTEXT.md
@.planning/phases/12-optional-fertility-window-estimator/12-01-SUMMARY.md
@.planning/phases/12-optional-fertility-window-estimator/12-02-SUMMARY.md
@apps/ptrack/lib/features/home/home_screen.dart
@apps/ptrack/lib/features/home/home_view_model.dart
@apps/ptrack/lib/features/home/cycle_position.dart
@apps/ptrack/lib/features/shell/tab_shell.dart
@apps/ptrack/lib/features/settings/fertility_settings.dart
@packages/ptrack_domain/lib/src/prediction/fertility_window.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: HomeViewModel fertility wiring + home fertility card + suggestion card</name>
  <files>
    apps/ptrack/lib/features/home/home_view_model.dart
    apps/ptrack/lib/features/home/home_screen.dart
  </files>
  <action>
**HomeViewModel** (`home_view_model.dart`):

Import `FertilitySettings` and `FertilityWindowCalculator` (from ptrack_domain). Follow the same pattern as CalendarViewModel (Plan 03).

- Add private fields: `_fertilityEnabled` (bool, false), `_cycleLengthOverride` (int?), `_lutealPhaseDays` (int, 14), `_suggestionCardDismissed` (bool, false).
- In constructor's `Future.wait`, add fertility setting loads: `FertilitySettings.loadEnabled()`, `FertilitySettings.loadCycleLengthOverride()`, `FertilitySettings.loadLutealPhaseDays()`, `FertilitySettings.loadSuggestionCardDismissed()`.
- Add computed property `FertileWindow? get fertileWindow`: computed in `_recompute`. Store as `_fertileWindow` private field.
- In `_recompute`, after prediction computation:
  1. If `_fertilityEnabled == false` → `_fertileWindow = null`.
  2. If enabled: compute cycle length (auto from history or override), get last period start, call `FertilityWindowCalculator.compute(...)`. Store result.
  3. Use same completed-cycle-length extraction as CalendarViewModel (consecutive period starts → `completedCycleBetweenStarts`).
- Expose getters:
  - `bool get fertilityEnabled` — for card visibility
  - `FertileWindow? get fertileWindow` — for card date range
  - `bool get showSuggestionCard` — true when `!_fertilityEnabled && !_suggestionCardDismissed`
  - `bool get hasEnoughDataForFertility` — true when there are ≥2 completed cycles
  - `int? get computedAverageCycleLength` — the auto-computed value (for card explanation text)
- Add `updateFertilityEnabled(bool enabled)` async method: updates field, saves, recomputes, notifies.
- Add `dismissSuggestionCard()` async method: saves `suggestionCardDismissed = true`, updates field, notifies.

**HomeScreen** (`home_screen.dart`):

### Fertility window card
- Show below the TodayCard (and milestone notice) when `viewModel.fertilityEnabled && viewModel.fertileWindow != null`.
- Card style: follow the existing `_MilestoneNotice` card pattern (Card with primaryContainer background, but use a fertility-themed color — `Color(0xFF26A69A).withValues(alpha: 0.15)` for subtle teal tint).
- Content:
  - Row: Leading icon `Icons.spa` in teal, then column with title + date range.
  - Title: ARB `fertilityHomeCardTitle` ("Fertile window").
  - Date range: Format `fertileWindow.startUtc` and `fertileWindow.endUtc` using `MaterialLocalizations.formatMediumDate` — ARB `fertilityHomeCardRange` with placeholders.
  - Below: explanation line ARB `fertilityHomeCardExplanation` with `{days}` placeholder = `computedAverageCycleLength`.
  - Footer: ARB `fertilityHomeCardFooter` ("Estimate only") in `bodySmall` with `onSurfaceVariant` color — persistent subtle reminder per CONTEXT ("subtle footer on the home card always visible").
- Per CONTEXT: "Tapping a fertile day" should show info — for the home card, tapping could expand/collapse or just be informational. Keep it simple: no tap action on the home card (it's already showing the info).

### Suggestion card
- Show when `viewModel.showSuggestionCard == true` — appears for users who haven't enabled or dismissed.
- Per CONTEXT: "always available from first use", "grayed out or explained if not enough data yet".
- Card style: similar to _MilestoneNotice but with a neutral/subdued color when insufficient data.
- Content:
  - If `viewModel.hasEnoughDataForFertility`:
    - Title: ARB `fertilitySuggestionTitle`
    - Body: ARB `fertilitySuggestionBody`
    - Action: TextButton with ARB `fertilitySuggestionEnable` — navigates to Settings (or directly calls the enable flow). For simplicity, show a "Go to Settings" action that opens the Settings screen. Per CONTEXT the home card enables discovery but the actual toggle is in settings.
  - If NOT enough data:
    - Same title, but body shows ARB `fertilitySuggestionNotEnoughData`
    - No action button (grayed out state)
    - Reduced opacity (0.6) on the card to convey "not yet available"
  - Dismiss (X) button: calls `viewModel.dismissSuggestionCard()`. Per CONTEXT: "never comes back" after dismissal.
- Place the suggestion card ABOVE the TodayCard (prominent discovery position). Do NOT show if fertility is already enabled.
  </action>
  <verify>
    <automated>cd apps/ptrack && dart analyze lib/features/home/home_view_model.dart lib/features/home/home_screen.dart</automated>
    <manual>Trace states: (1) New user, no periods → suggestion card grayed out, no fertility card. (2) 3 periods logged, not enabled → suggestion card active with "Enable". (3) Enabled → fertility card shows, no suggestion card. (4) Disabled → no fertility card, suggestion card reappears (unless dismissed). (5) Suggestion dismissed → no suggestion card ever.</manual>
  </verify>
  <done>Home shows fertility card when enabled with date range and footer. Suggestion card visible for undecided users with grayed-out insufficient-data state. Dismissal is permanent. dart analyze clean.</done>
</task>

<task type="auto">
  <name>Task 2: Tab shell callback wiring for fertility toggle</name>
  <files>apps/ptrack/lib/features/shell/tab_shell.dart</files>
  <action>
**Wire the onFertilityToggled callback in tab_shell.dart:**

In `_TabShellState._openSettings`, change `onFertilityToggled: null` to an actual callback:

```dart
onFertilityToggled: (enabled) {
  unawaited(_calendarVm.updateFertilityEnabled(enabled));
  unawaited(_homeVm.updateFertilityEnabled(enabled));
},
```

This ensures that when the user toggles fertility in Settings:
1. CalendarViewModel immediately recomputes (shows/hides fertility dots).
2. HomeViewModel immediately recomputes (shows/hides fertility card).
3. Both VMs notify listeners, so the UI updates when the user navigates back.

Also ensure the `_SettingsScreen` passes `onFertilityToggled` through to `FertilitySettingsTile` (should already be set up by Plan 02).
  </action>
  <verify>
    <automated>cd apps/ptrack && dart analyze lib/features/shell/tab_shell.dart</automated>
    <manual>Verify that CalendarViewModel.updateFertilityEnabled and HomeViewModel.updateFertilityEnabled methods exist (from Plan 03 Task 1 and this plan's Task 1 respectively).</manual>
  </verify>
  <done>Settings fertility toggle immediately updates both calendar and home ViewModels. No stale state when navigating back from settings.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of fertility feature</name>
  <what-built>Complete fertility window feature: calculation engine (Plan 01), settings opt-in with disclaimer and input form (Plan 02), calendar teal diamond dots with legend (Plan 03), home fertility card with suggestion card (this plan).</what-built>
  <how-to-verify>
1. Run `cd apps/ptrack && flutter run` (or use the IDE).
2. **Fresh state test:** Clear app data or fresh install. Verify:
   - Home screen shows suggestion card (grayed out if &lt;2 periods logged).
   - Settings → no fertility toggle is ON.
   - Calendar shows NO fertility dots.
3. **Log 3+ periods** spanning different months (use the day detail sheet to mark past days).
4. **Suggestion card test:** Home suggestion card should now be active (not grayed out). Tap "Go to Settings" or navigate to Settings manually.
5. **Enable flow test:** Toggle fertility ON → disclaimer bottom sheet appears → tap "I understand" → input form appears with auto-filled cycle length → adjust luteal phase slider → tap Save → toggle is now ON.
6. **Calendar test:** Calendar shows small teal diamond dots on estimated fertile days. Legend includes "Fertile (est.)" entry. Tap a fertile day → day detail shows "Estimated fertile day" label.
7. **Home test:** Home shows "Fertile window" card with date range, "Estimate only" footer.
8. **Disable test:** Settings → toggle fertility OFF → calendar dots disappear, home card disappears. Settings preserved (toggle back ON → no disclaimer, input pre-filled).
9. **Dismiss suggestion test:** Toggle OFF, go home, dismiss suggestion card (X) → card never comes back even after app restart.
10. **German test:** Switch language to German → all fertility strings display in German.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `cd apps/ptrack && dart analyze --no-fatal-infos` passes for all modified files
2. Home card renders when enabled, hides when disabled
3. Suggestion card appears for new users, disappears permanently on dismiss
4. Tab shell callback wires correctly to both VMs
5. Full enable → use → disable → re-enable round-trip works cleanly
</verification>

<success_criteria>
- HomeViewModel exposes fertileWindow, showSuggestionCard, hasEnoughDataForFertility
- Home screen renders fertility card with teal accent, date range, and "Estimate only" footer
- Suggestion card shown for undiscovered users, grayed when insufficient data, permanently dismissable
- Tab shell callback updates both CalendarViewModel and HomeViewModel immediately on toggle
- Human verification confirms visual quality and full user flow
</success_criteria>

<output>
After completion, create `.planning/phases/12-optional-fertility-window-estimator/12-04-SUMMARY.md`
</output>
