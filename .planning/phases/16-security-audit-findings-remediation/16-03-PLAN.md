---
phase: 16-security-audit-findings-remediation
plan: 03
type: execute
wave: 2
depends_on: [16-05]
files_modified:
  - packages/ptrack_data/lib/src/export/import_service.dart
  - apps/ptrack/lib/features/backup/import_view_model.dart
  - packages/ptrack_data/lib/src/export/backup_service.dart
  - apps/ptrack/lib/main.dart
  - packages/ptrack_data/test/export/import_service_test.dart
  - apps/ptrack/test/features/lock/reset_flow_test.dart
autonomous: true
requirements: [SEC-F5, SEC-F7]

must_haves:
  truths:
    - "Import rejects files larger than the hard maximum before parsing"
    - "Import rejects payloads with excessive period or day-entry counts"
    - "Oversized/malformed imports fail with user-safe error messages, no crash"
    - "Factory reset deletes all backup files in luma_backups directory"
    - "Reset UI reports which data stores were wiped via SnackBar and surfaces failures to user"
  artifacts:
    - path: "packages/ptrack_data/lib/src/export/import_service.dart"
      provides: "File size guard and payload count limits"
      contains: "maxImportFileSize"
    - path: "packages/ptrack_data/lib/src/export/backup_service.dart"
      provides: "deleteAllBackups method for reset flow"
      contains: "deleteAllBackups"
    - path: "apps/ptrack/lib/main.dart"
      provides: "Reset flow includes backup directory cleanup"
      contains: "deleteAllBackups"
  key_links:
    - from: "apps/ptrack/lib/features/backup/import_view_model.dart"
      to: "packages/ptrack_data/lib/src/export/import_service.dart"
      via: "size check before parseFileMeta"
      pattern: "bytes\\.length.*maxImportFileSize"
    - from: "apps/ptrack/lib/main.dart"
      to: "packages/ptrack_data/lib/src/export/backup_service.dart"
      via: "_resetApp calls backupService.deleteAllBackups()"
      pattern: "deleteAllBackups"
---

<objective>
Add file size and payload complexity limits to the import path (DoS prevention), and ensure factory reset removes all backup artifacts alongside the database.

Purpose: Finding 5 (MEDIUM) — crafted imports can cause OOM/hangs. Finding 7 (MEDIUM) — reset leaves backup files behind, violating user expectation of full data removal.
Output: Import guards with typed exceptions, backup deletion in reset flow, tests for both.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/SECURITY_AUDIT_FINDINGS.md — Findings 5, 7
@packages/ptrack_data/lib/src/export/import_service.dart
@apps/ptrack/lib/features/backup/import_view_model.dart
@packages/ptrack_data/lib/src/export/backup_service.dart
@apps/ptrack/lib/main.dart
@.planning/phases/15-address-full-app-code-review-findings/15-02-SUMMARY.md
@.planning/phases/16-security-audit-findings-remediation/16-05-SUMMARY.md (Plan 05 also modifies _resetApp in main.dart — this plan runs after it)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Import file size and payload complexity guards</name>
  <files>packages/ptrack_data/lib/src/export/import_service.dart, apps/ptrack/lib/features/backup/import_view_model.dart, packages/ptrack_data/test/export/import_service_test.dart</files>
  <action>
  **Constants** (in `import_service.dart` or a shared constants location):

  1. Define import limits:
     - `static const int maxImportFileSize = 10 * 1024 * 1024;` (10 MB — generous for period data, blocks multi-GB attacks)
     - `static const int maxPeriodCount = 5000;` (far beyond any realistic use — menstrual cycles for ~400 years)
     - `static const int maxDayEntryCount = 200000;` (enough for decades of daily entries)

  2. Add `LumaImportFileTooLargeException extends LumaImportException` with the file size and limit.

  3. Add `LumaImportPayloadTooLargeException extends LumaImportException` with the count and limit.

  **Import path guards**:

  4. In `parseFileMeta()`: add a size check as the FIRST operation:
     ```dart
     if (bytes.length > maxImportFileSize) {
       throw LumaImportFileTooLargeException(
         fileSize: bytes.length,
         maxSize: maxImportFileSize,
       );
     }
     ```

  5. In `parseFileData()` after decoding (both encrypted and plaintext paths): after `LumaExportData.fromJson` succeeds, check:
     - `data.periods != null && data.periods!.length > maxPeriodCount` → throw payload-too-large
     - `data.dayEntries != null && data.dayEntries!.length > maxDayEntryCount` → throw payload-too-large

  **ImportViewModel** (`import_view_model.dart`):

  6. After `FilePicker.pickFiles(withData: true)`, check `result.files.single.bytes!.length` against `ImportService.maxImportFileSize` BEFORE calling `parseFileMeta`. Show user-safe error via the existing error mechanism. This prevents even the initial JSON parse on enormous files.

  **Tests** (`import_service_test.dart`):

  7. Add test group "import guards":
     - `parseFileMeta rejects file exceeding max size` — pass a `Uint8List(maxImportFileSize + 1)` and expect `LumaImportFileTooLargeException`.
     - `parseFileData rejects payload with too many periods` — craft a valid JSON with period count above limit.
     - `parseFileData rejects payload with too many day entries` — craft valid JSON with entry count above limit.
     - `parseFileMeta accepts file at exactly max size` — verify no exception for boundary value (content can be invalid JSON — the size check passes, then format check may fail separately).

  Export the new exception types from `packages/ptrack_data/lib/ptrack_data.dart` if they need to be visible to the app layer.
  </action>
  <verify>
    <automated>cd packages/ptrack_data && fvm flutter test test/export/import_service_test.dart --reporter expanded</automated>
  </verify>
  <done>Import rejects files > 10 MB before parsing; rejects payloads with > 5000 periods or > 200000 day entries after decode; boundary tests pass; user-facing errors are clear and non-crashing.</done>
</task>

<task type="auto">
  <name>Task 2: Include backup artifacts in factory reset flow</name>
  <files>packages/ptrack_data/lib/src/export/backup_service.dart, apps/ptrack/lib/main.dart, apps/ptrack/test/features/lock/reset_flow_test.dart</files>
  <action>
  **BackupService** (`backup_service.dart`):

  1. Add `Future<({int deleted, int failed})> deleteAllBackups()` method:
     - List all backup files via `listBackups()`.
     - Attempt to delete each. Count successes and failures.
     - Also attempt to remove the `luma_backups` directory itself if empty after deletion.
     - Return a record with counts so the caller can report.

  **Reset flow** (`main.dart`):

  2. In `_resetApp()`, after the database close+delete step and before setting state to onboarding:
     - Create a `BackupService(_db)` (the DB is already closed at this point, but BackupService only needs the support directory for listing/deleting — it does NOT query the DB in `listBackups`/`deleteAllBackups`).

     Actually, `BackupService` takes `PtrackDatabase` in its constructor. Since the DB is closed, we need an alternative approach:
     - Option A: Extract the backup directory logic into a static method or accept `ApplicationSupportDirectory` directly.
     - Option B: Call `deleteAllBackups` BEFORE closing the DB.

     Use **Option B**: call `BackupService(widget.database!).deleteAllBackups()` before `closeAndDeletePtrackDatabaseFile`. This keeps the constructor happy and the DB object exists (even if the service doesn't query it for deletion).

     Actually, `BackupService` constructor takes `_db` but `deleteAllBackups` only needs `_supportDir`. So we can safely call it before or after close. Call it BEFORE close to keep the object valid:

     ```dart
     // In _resetApp, after prefs.clear() and before closeAndDelete:
     final backupResult = await BackupService(widget.database!).deleteAllBackups();
     ```

  3. After all reset steps complete (prefs clear, backup delete, DB close+delete), show a **SnackBar** summarizing the result. Use the `ScaffoldMessenger` to display feedback:
     - On full success: "All data cleared: database, backups, and preferences." (l10n key: `resetSuccessMessage`)
     - On partial failure (backupResult.failed > 0): "Reset complete. Failed to delete {count} backup file(s)." (l10n key: `resetPartialFailureMessage` with `count` placeholder)
     - Add EN/DE ARB keys for both messages.
     - Also keep a `debugPrint` for the failure case (developer diagnostics), but the user-visible feedback is the SnackBar.
     - This satisfies Finding 7 acceptance criteria: "UI confirms which data stores were wiped" and "Failure surfaced to user."

  **Tests** (`reset_flow_test.dart`):

  4. Extend the existing `reset_flow_test.dart`:
     - Add test "resetApp deletes backup artifacts" — mock or stub `BackupService.deleteAllBackups` and verify it is called during reset.
     - Add test "resetApp reports backup deletion failure" — verify debugPrint is called when `backupResult.failed > 0`.

  Note: The existing reset_flow_test.dart uses test hooks on `LumaApp`. The backup service call may need a similar hook or direct integration depending on how the test is structured. If `_resetApp` is not directly testable for the backup call, add a `BackupService` parameter/override to `LumaApp` similar to `deletePtrackDatabaseOverride`.
  </action>
  <verify>
    <automated>cd apps/ptrack && fvm flutter test test/features/lock/reset_flow_test.dart --reporter expanded</automated>
  </verify>
  <done>Factory reset deletes all files in `luma_backups/` directory before DB deletion; user sees SnackBar confirming which stores were wiped (or partial failure count); failures also logged via debugPrint; tests verify backup cleanup is called during reset and user feedback is shown.</done>
</task>

</tasks>

<verification>
- Import of a 15 MB file fails with `LumaImportFileTooLargeException` before any JSON parsing
- Import of a file with 6000 periods fails with `LumaImportPayloadTooLargeException`
- Factory reset removes all `auto-backup-*.luma` files
- All import and reset tests pass
</verification>

<success_criteria>
Finding 5 acceptance criteria met:
- Hard maximum file size enforced before decoding (10 MB)
- Structured payload limits enforced during import validation (5000 periods, 200000 entries)
- Oversized/malformed imports fail gracefully without app crash
- Tests cover boundary values and adversarial files

Finding 7 acceptance criteria met:
- Reset flow deletes backup artifacts in luma_backups directory
- UI confirms which data stores were wiped via SnackBar
- Failure to wipe any location is surfaced to user via SnackBar (and logged via debugPrint)
- Automated test verifies reset removes both DB and backup artifacts
</success_criteria>

<output>
After completion, create `.planning/phases/16-security-audit-findings-remediation/16-03-SUMMARY.md`
</output>
