---
phase: 16-security-audit-findings-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/ptrack/android/app/build.gradle.kts
  - apps/ptrack/android/key.properties.example
  - .github/workflows/firebase-app-distribution.yml
autonomous: true
requirements: [SEC-F3]

must_haves:
  truths:
    - "Release build type uses a dedicated release signing config, not debug"
    - "CI build fails when release signing config points to debug keystore"
    - "Signing process is documented and reproducible"
  artifacts:
    - path: "apps/ptrack/android/app/build.gradle.kts"
      provides: "Release signingConfig reads from key.properties, falls back to nothing (not debug)"
      contains: "signingConfigs.create"
    - path: "apps/ptrack/android/key.properties.example"
      provides: "Template for local/CI keystore properties"
      contains: "storeFile"
    - path: ".github/workflows/firebase-app-distribution.yml"
      provides: "CI guard that decodes signing secrets and verifies release artifact"
  key_links:
    - from: "apps/ptrack/android/app/build.gradle.kts"
      to: "key.properties"
      via: "file-based property read"
      pattern: "rootProject\\.file.*key\\.properties"
    - from: ".github/workflows/firebase-app-distribution.yml"
      to: "apps/ptrack/android/app/build.gradle.kts"
      via: "CI writes key.properties from secrets before flutter build"
---

<objective>
Configure proper Android release signing with a dedicated keystore and CI-managed secrets, replacing the current debug-signing fallback.

Purpose: Finding 3 (HIGH) — release builds signed with debug keys weaken app integrity guarantees. Must use dedicated release keystore.
Output: Updated build.gradle.kts, key.properties example, CI workflow with signing secret injection and release artifact verification.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/SECURITY_AUDIT_FINDINGS.md — Finding 3
@apps/ptrack/android/app/build.gradle.kts
@.github/workflows/firebase-app-distribution.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure release signing in build.gradle.kts</name>
  <files>apps/ptrack/android/app/build.gradle.kts, apps/ptrack/android/key.properties.example</files>
  <action>
  1. Create `apps/ptrack/android/key.properties.example` with template:
     ```
     storePassword=
     keyPassword=
     keyAlias=release
     storeFile=../keystore/release.jks
     ```
     Add a comment header explaining this is a template and the real `key.properties` is gitignored.

  2. Ensure `apps/ptrack/android/.gitignore` (or the repo `.gitignore`) includes `key.properties` and `*.jks` / `*.keystore` so real secrets are never committed.

  3. Modify `apps/ptrack/android/app/build.gradle.kts`:
     - At the top of the `android {}` block, add a `val keystorePropertiesFile = rootProject.file("key.properties")` block that conditionally loads properties.
     - Create a `signingConfigs { create("release") { ... } }` block that reads `storeFile`, `storePassword`, `keyAlias`, `keyPassword` from the loaded properties.
     - Change `buildTypes.release.signingConfig` from `signingConfigs.getByName("debug")` to `signingConfigs.getByName("release")` — but ONLY when `keystorePropertiesFile.exists()` evaluates true. When the properties file is absent (e.g. fresh checkout without secrets), the release block should NOT fall back to debug signing. Instead, omit `signingConfig` so unsigned/missing-key builds fail explicitly rather than silently debug-signing.
     - Remove the TODO comments about debug signing.

  4. Verify the Gradle config is syntactically valid: `cd apps/ptrack/android && ./gradlew tasks --dry-run` (or equivalent quick check). If Gradle is not on PATH, verify by reading the file for syntax correctness.
  </action>
  <verify>
    <automated>cd apps/ptrack && fvm flutter analyze --no-fatal-infos</automated>
    <manual>Inspect build.gradle.kts: release block references "release" signing config, not "debug"</manual>
  </verify>
  <done>Release buildType uses dedicated release signingConfig from key.properties; debug fallback is removed; key.properties.example documents the required fields; real secrets are gitignored.</done>
</task>

<task type="auto">
  <name>Task 2: CI signing secrets injection + release artifact verification</name>
  <files>.github/workflows/firebase-app-distribution.yml</files>
  <action>
  1. In `.github/workflows/firebase-app-distribution.yml`, add a step before "Build Android APK (release)" that:
     - Decodes a base64-encoded keystore from a GitHub secret (`ANDROID_KEYSTORE_BASE64`) into `apps/ptrack/android/keystore/release.jks`.
     - Writes `apps/ptrack/android/key.properties` from secrets: `ANDROID_KEY_ALIAS`, `ANDROID_KEY_PASSWORD`, `ANDROID_STORE_PASSWORD`.
     - The step should be named clearly, e.g. "Decode Android signing secrets".
     - Fail the workflow if any secret is empty.

  2. Add a post-build verification step after "Build Android APK (release)" that:
     - Runs `apksigner verify --print-certs apps/ptrack/build/app/outputs/flutter-apk/app-release.apk` (apksigner is available in the Android SDK on ubuntu-latest).
     - Greps the output to confirm the certificate DN does NOT contain "Android Debug" — fail the step if it does.
     - Name the step "Verify release signing certificate".

  3. Add a comment block at the top of the workflow (near existing setup docs) listing the new GitHub secrets required:
     - `ANDROID_KEYSTORE_BASE64` — base64-encoded release.jks
     - `ANDROID_KEY_ALIAS`, `ANDROID_KEY_PASSWORD`, `ANDROID_STORE_PASSWORD`

  Do NOT embed any actual secret values. Only reference `${{ secrets.* }}` variables.
  </action>
  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/firebase-app-distribution.yml'))" && echo "YAML valid"</automated>
    <manual>Inspect workflow: signing step writes key.properties from secrets, verify step rejects debug cert</manual>
  </verify>
  <done>CI workflow decodes signing secrets before build, writes key.properties, verifies the release APK is NOT debug-signed, and documents the required secrets. Building without secrets fails explicitly.</done>
</task>

</tasks>

<verification>
- `build.gradle.kts` no longer references `signingConfigs.getByName("debug")` in release block
- `key.properties.example` exists with documented fields
- `.gitignore` covers `key.properties`, `*.jks`, `*.keystore`
- CI workflow has signing-secret decode step and release-cert verification step
- YAML is syntactically valid
</verification>

<success_criteria>
Finding 3 acceptance criteria met:
- Release build type uses only release signing config
- CI/build step fails when debug signing is detected for release
- Signing process is documented and reproducible in CI
- A release artifact verification step confirms expected signing certificate
</success_criteria>

<output>
After completion, create `.planning/phases/16-security-audit-findings-remediation/16-01-SUMMARY.md`
</output>
