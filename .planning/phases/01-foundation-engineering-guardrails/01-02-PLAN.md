---
phase: 01-foundation-engineering-guardrails
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - .github/workflows/ci.yml
  - tool/ci/verify_pubspec_policy.py
  - melos.yaml
autonomous: true
requirements:
  - NFR-03
  - NFR-04
must_haves:
  truths:
    - "GitHub Actions runs on Ubuntu for pull_request and push to main."
    - "CI uses the same FVM-pinned Flutter as local via fvm install/use, then melos bootstrap, analyze (errors fail), and full test/ discovery."
    - "A Python check fails CI if forbidden dependency sources appear in pubspecs (git deps; path deps outside allowed monorepo roots)."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "PR/main CI gate with fvm flutter pub get/analyze/test"
    - path: "tool/ci/verify_pubspec_policy.py"
      provides: "Portable pubspec policy enforcement (git + path rules)"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "tool/ci/verify_pubspec_policy.py"
      via: "python3 invocation after checkout"
      pattern: "verify_pubspec_policy"
    - from: ".github/workflows/ci.yml"
      to: ".fvmrc"
      via: "FVM install uses repo pin"
      pattern: "fvm install|fvm use"
---

<objective>
Add Ubuntu GitHub Actions for `pull_request` and `push` to `main` that bootstraps the Melos workspace with FVM, runs `flutter analyze` with **errors-only** gating (do not elevate all warnings to failures), runs **all** tests under each package’s `test/` tree, and runs a small **Python** pubspec policy script for reviewable privacy/deps constraints.

Purpose: Automates NFR-04 reviewability and protects NFR-03 by blocking disallowed dependency sources in CI.
Output: `ci.yml`, `tool/ci/verify_pubspec_policy.py`, optional `melos.yaml` script hooks for CI-friendly one-liners.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-engineering-guardrails/01-CONTEXT.md
@.planning/phases/01-foundation-engineering-guardrails/01-RESEARCH.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: GitHub Actions workflow (Ubuntu, FVM, Melos)</name>
  <files>.github/workflows/ci.yml,melos.yaml</files>
  <action>
    Create `.github/workflows/ci.yml` triggered on `pull_request` and `push` to `main` only (no iOS/mac jobs). Use `ubuntu-latest`.
    Steps (order matters): checkout; install a fixed Python 3.x **only if** needed for the script step (or always install for simplicity); install Dart SDK compatible with FVM; `dart pub global activate fvm`; `fvm install` / `fvm use` per repo `.fvmrc`; add pub cache / fvm to PATH as required; `dart pub global activate melos`; `melos bootstrap`.
    Run `fvm flutter pub get` where needed (bootstrap may suffice). Run **`fvm flutter analyze`** from each Flutter package **or** use `melos exec` with a filter so **analyzer errors fail** the job; **do not** pass flags that treat all warnings as failures (errors-only policy). Then run **`fvm flutter test`** for every package that contains a `test/` directory (use `melos exec` with `dirExists:test` filter or equivalent so **all** `test/` suites run—match CONTEXT).
    Document in workflow comments that CI Flutter **must** match `.fvmrc` (no bare `flutter` without FVM).
    If helpful, add `melos.yaml` scripts `ci:analyze` / `ci:test` that wrap the above for local parity—still must use `fvm flutter`.
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml &amp;&amp; grep -q pull_request .github/workflows/ci.yml &amp;&amp; grep -q push .github/workflows/ci.yml</automated>
    <manual>Optional: run actionlint on ci.yml if installed locally.</manual>
    <sampling_rate>after Task 1</sampling_rate>
  </verify>
  <done>Workflow file exists with correct triggers; job runs analyze + test via FVM; no macOS/iOS matrix entries.</done>
</task>

<task type="auto">
  <name>Task 2: Python pubspec policy script and CI integration</name>
  <files>tool/ci/verify_pubspec_policy.py,.github/workflows/ci.yml</files>
  <action>
    Add `tool/ci/verify_pubspec_policy.py` (Python 3) that recursively finds `pubspec.yaml` files under `apps/` and `packages/` (and root if present). **Fail** if any pubspec declares a **`git:`** dependency (anywhere dependencies/dev_dependencies/dependency_overrides apply). For **`path:`** dependencies, **allow** only targets that resolve **inside** this repository’s `packages/` or `apps/` trees (relative paths normalized); **fail** for path deps pointing outside the repo or unexpected locations. Ignore `sdk:` and normal `pub.dev` version constraints.
    Wire the script into CI **after** checkout and **before** or **after** bootstrap (after is fine—read-only scan). Use **`uv run --python 3.12 --with pyyaml python3 tool/ci/verify_pubspec_policy.py`** from repo root (not `pip`).
    On failure, print a clear message citing the offending pubspec key so reviewers see NFR-03/NFR-04 rationale.
  </action>
  <verify>
    <automated>uv run --python 3.12 --with pyyaml python3 tool/ci/verify_pubspec_policy.py</automated>
    <sampling_rate>after Task 2</sampling_rate>
  </verify>
  <done>Script exits 0 on clean tree; CI invokes it; intentional test with a fake git dep locally exits non-zero (executor may delete temp change without committing).</done>
</task>

<task type="auto">
  <name>Task 3: Local CI parity check</name>
  <files>README.md</files>
  <action>
    Add a short README subsection **CI parity** listing the same commands the workflow runs (melos bootstrap, analyze, test, python verify)—bash one-liners—so contributors can reproduce locally. Do not duplicate long policy text (SECURITY.md comes in plan 01-03).
  </action>
  <verify>
    <automated>melos bootstrap &amp;&amp; melos exec -c 1 -- fvm flutter analyze &amp;&amp; melos exec -c 1 --dir-exists=test -- fvm flutter test</automated>
    <sampling_rate>after Task 3</sampling_rate>
  </verify>
  <done>Documented commands succeed on a clean checkout; README references CI steps.</done>
</task>

</tasks>

<verification>
CI workflow validates on push; Python scanner passes; `melos exec` analyze/test commands match README parity section.
</verification>

<success_criteria>
Automated gate exists for PR + main; FVM-driven analyze/test; Python policy script enforces git-forbidden and constrained path deps—supporting NFR-03/NFR-04 traceability in CI.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-engineering-guardrails/01-02-SUMMARY.md`
</output>
