---
phase: 01-foundation-engineering-guardrails
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - SECURITY.md
  - README.md
  - .github/dependabot.yml
autonomous: true
requirements:
  - NFR-03
  - NFR-04
must_haves:
  truths:
    - "SECURITY.md states forbidden analytics/ads/profiling/hidden telemetry and dependency review expectations; README links to it prominently."
    - "Dependabot opens pub version bump PRs for each Dart/Flutter package location in the monorepo."
    - "README documents Android-first local development and points contributors to FVM/Melos/CI parity; full analyze+test+policy script still pass."
  artifacts:
    - path: "SECURITY.md"
      provides: "Dependency and privacy policy for NFR-03/NFR-04"
    - path: ".github/dependabot.yml"
      provides: "Automated pub dependency update PRs"
    - path: "README.md"
      provides: "Entrypoint with SECURITY link and Android-first onboarding"
  key_links:
    - from: "README.md"
      to: "SECURITY.md"
      via: "markdown link"
      pattern: "SECURITY\\.md"
    - from: ".github/dependabot.yml"
      to: "apps/ptrack"
      via: "directory entry"
      pattern: "apps/ptrack"
---

<objective>
Publish dependency/privacy policy in `SECURITY.md`, enable Dependabot for **pub** across monorepo package roots, and polish README (Android-first, link to policy, contributor flow)—then confirm analyze, tests, and the Python pubspec policy script still pass.

Purpose: Closes the documentation and automation loop for NFR-03 and NFR-04 beyond raw CI behavior.
Output: `SECURITY.md`, `.github/dependabot.yml`, updated `README.md`.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation-engineering-guardrails/01-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SECURITY.md and README link</name>
  <files>SECURITY.md,README.md</files>
  <action>
    Create `SECURITY.md` covering: **no** analytics, ad identifiers, third-party ads, profiling SDKs, or hidden telemetry of health/reproductive events (NFR-03); dependency expectations—prefer `pub.dev`, avoid `git:`/`path:` except approved internal monorepo paths (NFR-04); encourage review of transitive deps and Dependabot PRs; optional short “reporting concerns” section (email/ISSUE template pointer—keep minimal).
    Update `README.md` with a clear **Security & privacy** (or similar) bullet/section linking to `SECURITY.md` as the detailed policy. Add **Android-first** developer note: Android SDK/emulator as default local run target; iOS supported by Flutter but not primary early focus per CONTEXT. Keep contributor setup README-only for Phase 1 (no extra docs folder).
  </action>
  <verify>
    <automated>grep -n SECURITY.md README.md</automated>
    <sampling_rate>after Task 1</sampling_rate>
  </verify>
  <done>SECURITY.md exists with NFR-aligned rules; README contains a working relative link to it.</done>
</task>

<task type="auto">
  <name>Task 2: Dependabot configuration for pub</name>
  <files>.github/dependabot.yml</files>
  <action>
    Add `.github/dependabot.yml` (version 2) with separate `package-ecosystem: pub` entries for **each** directory containing a `pubspec.yaml` that should receive version updates—at minimum `apps/ptrack`, `packages/ptrack_domain`, `packages/ptrack_data`, and root if applicable. Use a sensible `schedule.interval` (e.g. weekly). Optional: group minor/patch updates per ecosystem if supported—use discretion; do not block on exotic grouping features.
    Ensure **no** git/path dependency workarounds are introduced while editing Dependabot file.
  </action>
  <verify>
    <automated>test -f .github/dependabot.yml &amp;&amp; grep -q package-ecosystem .github/dependabot.yml</automated>
    <sampling_rate>after Task 2</sampling_rate>
  </verify>
  <done>dependabot.yml parses; includes pub entries for all shipped package directories.</done>
</task>

<task type="auto">
  <name>Task 3: Final guardrail verification</name>
  <files>README.md</files>
  <action>
    Run the full local parity sequence from repository root: `uv run --python 3.12 --with pyyaml python3 tool/ci/verify_pubspec_policy.py`, `melos bootstrap`, then Melos/FVM-driven `flutter analyze` and `flutter test` across packages with `test/`—matching plan 01-02 README instructions. Fix any regressions (e.g. README typos breaking copy-paste only if discovered). **Do not** add Riverpod. Edit `README.md` only if needed to fix broken CI parity commands.
  </action>
  <verify>
    <automated>uv run --python 3.12 --with pyyaml python3 tool/ci/verify_pubspec_policy.py &amp;&amp; melos bootstrap &amp;&amp; melos exec -c 1 -- fvm flutter analyze &amp;&amp; melos exec -c 1 --dir-exists=test -- fvm flutter test</automated>
    <sampling_rate>before phase summary</sampling_rate>
  </verify>
  <done>All automated commands exit 0; workspace remains free of forbidden dependency patterns.</done>
</task>

</tasks>

<verification>
Policy doc + Dependabot + README align with CONTEXT; automated analyze/test/policy script succeed from repo root.
</verification>

<success_criteria>
NFR-03/NFR-04 are explicitly documented and linked from README; Dependabot watches all pub packages; end-to-end local commands match CI expectations.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-engineering-guardrails/01-03-SUMMARY.md`
</output>
