---
phase: 01-foundation-engineering-guardrails
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .fvmrc
  - .fvm/fvm_config.json
  - melos.yaml
  - pubspec.yaml
  - README.md
  - analysis_options.yaml
  - packages/ptrack_domain/pubspec.yaml
  - packages/ptrack_domain/lib/ptrack_domain.dart
  - packages/ptrack_domain/test/ptrack_domain_test.dart
  - packages/ptrack_data/pubspec.yaml
  - packages/ptrack_data/lib/ptrack_data.dart
  - packages/ptrack_data/test/ptrack_data_test.dart
  - apps/ptrack/pubspec.yaml
  - apps/ptrack/pubspec.lock
  - apps/ptrack/analysis_options.yaml
  - apps/ptrack/lib/main.dart
  - apps/ptrack/test/widget_test.dart
autonomous: true
requirements:
  - NFR-03
  - NFR-04
must_haves:
  truths:
    - "Checkout documents exact stable Flutter via FVM and boots Melos workspace without hand-rolled pub orchestration."
    - "apps/ptrack is a valid Flutter app that resolves path deps to ptrack_domain and ptrack_data."
    - "No analytics, ads, profiling, or telemetry SDKs appear in any pubspec; dependencies are pub.dev or internal path only."
    - "flutter_lints and mocktail (dev) are configured; at least one automated test runs under apps/ptrack/test/."
  artifacts:
    - path: ".fvmrc"
      provides: "Exact stable Flutter version pin for dev and CI"
    - path: "melos.yaml"
      provides: "Melos monorepo discovery for apps/* and packages/*"
    - path: "apps/ptrack/pubspec.yaml"
      provides: "Flutter app with path deps and committed lockfile"
    - path: "apps/ptrack/pubspec.lock"
      provides: "Reproducible dependency resolution for the app"
    - path: "packages/ptrack_domain/lib/ptrack_domain.dart"
      provides: "Minimal domain package surface (placeholder export)"
    - path: "packages/ptrack_data/lib/ptrack_data.dart"
      provides: "Minimal data package surface (placeholder export)"
  key_links:
    - from: "apps/ptrack/pubspec.yaml"
      to: "packages/ptrack_domain"
      via: "path dependency"
      pattern: "path:\\s*\\.\\./\\.\\./packages/ptrack_domain"
    - from: "apps/ptrack/pubspec.yaml"
      to: "packages/ptrack_data"
      via: "path dependency"
      pattern: "path:\\s*\\.\\./\\.\\./packages/ptrack_data"
    - from: "melos.yaml"
      to: "apps/ptrack"
      via: "package glob"
      pattern: "apps/\\*\\*"
---

<objective>
Establish the Melos monorepo, FVM exact stable pin, early `packages/ptrack_domain` and `packages/ptrack_data`, and a minimal Flutter app at `apps/ptrack` with `flutter_lints`, `mocktail`, committed `pubspec.lock`, and runnable tests—without Riverpod (placeholder UI only).

Purpose: Satisfies reproducible tooling and structural boundaries before CI and policy docs land.
Output: Root workspace files, two internal packages, one app scaffold, README-only FVM/Melos contributor steps (bash-oriented).
Riverpod is omitted in Phase 1 because placeholder UI does not need a state-management dependency.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-engineering-guardrails/01-CONTEXT.md
@.planning/phases/01-foundation-engineering-guardrails/01-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: FVM stable pin and Melos root workspace</name>
  <files>.fvmrc,.fvm/fvm_config.json,melos.yaml,pubspec.yaml,README.md</files>
  <action>
    Add `.fvmrc` with an **exact** stable Flutter version string (pick current stable patch at scaffold time; document the same version in README). Run `fvm use` so `.fvm/fvm_config.json` exists and is committed per FVM docs.
    Add root `pubspec.yaml` suitable for Melos (name e.g. `ptrack_workspace`, publish_to: none) with `environment.sdk` compatible with the pinned Flutter.
    Add `melos.yaml` listing `packages/**` and `apps/**`, with `ignore` for build artifacts if needed. Do **not** hand-roll a custom package runner beyond Melos.
    README (contributor doc for Phase 1 only): short bash-first steps—install FVM, `fvm use`, `dart pub global activate melos`, `melos bootstrap`, then `melos exec` or documented `fvm flutter` entrypoints under `apps/ptrack`. Note Windows devs may use WSL/Git Bash; canonical automation is Linux/bash. **Do not** add Riverpod or other state-management deps in Phase 1.
  </action>
  <verify>
    <automated>fvm flutter --version</automated>
    <manual>Optional: confirm README paths match repo layout after scaffold.</manual>
    <sampling_rate>after Task 1 commit</sampling_rate>
  </verify>
  <done>FVM reports the pinned stable version; `melos.yaml` and root `pubspec.yaml` exist; README documents FVM + Melos bootstrap without PowerShell-only scripts.</done>
</task>

<task type="auto">
  <name>Task 2: Internal packages and app pubspec wiring</name>
  <files>packages/ptrack_domain/pubspec.yaml,packages/ptrack_domain/lib/ptrack_domain.dart,packages/ptrack_data/pubspec.yaml,packages/ptrack_data/lib/ptrack_data.dart,apps/ptrack/pubspec.yaml,apps/ptrack/pubspec.lock,analysis_options.yaml,apps/ptrack/analysis_options.yaml</files>
  <action>
    Create `packages/ptrack_domain` and `packages/ptrack_data` as **minimal** Dart/Flutter packages: each has `lib/&lt;package&gt;.dart` exporting a placeholder API (e.g. a trivial class or top-level doc). Use **only** `pub.dev` or SDK deps—**no** `git:` dependencies. Internal linking uses `path:` only between app and these packages.
    Create `apps/ptrack` Flutter application `name: ptrack` (or consistent name) with `path:` dependencies to both packages. Add `dependency_overrides` only if required (prefer clean resolution without overrides).
    Add root and app `analysis_options.yaml` including `include: package:flutter_lints/flutter.yaml`. Add `flutter_lints` and `mocktail` as **dev_dependencies** where appropriate (app + packages that run tests).
    **Commit** `apps/ptrack/pubspec.lock`. For packages, commit lockfiles if Melos/bootstrap expects them for reproducibility; at minimum app lockfile is mandatory per CONTEXT.
    **Omit** `flutter_riverpod` entirely—placeholder `MaterialApp`/`StatelessWidget` is sufficient for Phase 1.
  </action>
  <verify>
    <automated>melos bootstrap</automated>
    <sampling_rate>after Task 2</sampling_rate>
  </verify>
  <done>`melos bootstrap` succeeds; app pubspec resolves path deps; lockfile committed; analysis_options include flutter_lints; no forbidden SDK categories in pubspecs.</done>
</task>

<task type="auto">
  <name>Task 3: Minimal app entrypoint and tests</name>
  <files>apps/ptrack/lib/main.dart,apps/ptrack/test/widget_test.dart,packages/ptrack_domain/test/ptrack_domain_test.dart,packages/ptrack_data/test/ptrack_data_test.dart</files>
  <action>
    Implement minimal `main.dart` (default counter or simple static home) that **imports** at least one symbol from `ptrack_domain` (and optionally `ptrack_data`) so package wiring is real, not dead.
    Add `apps/ptrack/test/widget_test.dart` with a basic pump/test. Add at least one **unit** test under each package `test/` using `mocktail` only if needed (simple `expect` without mock is acceptable if imports prove test layout).
    Ensure **no** Riverpod: do not add `ProviderScope` or related APIs.
  </action>
  <verify>
    <automated>cd apps/ptrack &amp;&amp; fvm flutter analyze &amp;&amp; fvm flutter test</automated>
    <sampling_rate>after Task 3</sampling_rate>
  </verify>
  <done>`fvm flutter analyze` exits 0; `fvm flutter test` runs all tests under `apps/ptrack/test/` successfully; packages compile.</done>
</task>

</tasks>

<verification>
Melos workspace resolves; FVM pin matches README; tests pass locally under `apps/ptrack/test/`; pubspecs remain free of analytics/ads/telemetry packages and git-hosted deps.
</verification>

<success_criteria>
Observable: new contributor can follow README (bash), run `melos bootstrap` and `fvm flutter test` in the app; repo contains `.fvmrc`, `melos.yaml`, both packages, and `apps/ptrack` with committed lockfile.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-engineering-guardrails/01-01-SUMMARY.md`
</output>
