---
phase: 04-core-logging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/ptrack_domain/lib/src/period/logging_types.dart
  - packages/ptrack_domain/lib/ptrack_domain.dart
  - packages/ptrack_domain/test/logging_types_test.dart
  - packages/ptrack_data/lib/src/db/tables.dart
  - packages/ptrack_data/lib/src/db/ptrack_database.dart
  - packages/ptrack_data/lib/src/db/ptrack_database.g.dart
  - packages/ptrack_data/lib/src/mappers/day_entry_mapper.dart
  - packages/ptrack_data/test/day_entry_mapper_test.dart
  - packages/ptrack_data/test/migration_test.dart
  - packages/ptrack_data/test/fixtures/ptrack_v2.sqlite
  - packages/ptrack_data/tool/create_v2_fixture.dart
autonomous: true
requirements: [LOG-02, LOG-03, LOG-06]

must_haves:
  truths:
    - "FlowIntensity, PainScore, Mood enums exist with correct values and round-trip through int conversion"
    - "DayEntries table exists in schema v2 with foreign key to periods and unique (periodId, dateUtc) constraint"
    - "Upgrading a v1 database to v2 preserves existing period rows and creates the DayEntries table"
    - "Foreign keys are enforced at runtime via PRAGMA foreign_keys = ON"
  artifacts:
    - path: "packages/ptrack_domain/lib/src/period/logging_types.dart"
      provides: "FlowIntensity, PainScore, Mood enums and DayEntryData value type"
      min_lines: 40
    - path: "packages/ptrack_data/lib/src/db/tables.dart"
      provides: "DayEntries Drift table definition"
      contains: "class DayEntries extends Table"
    - path: "packages/ptrack_data/lib/src/db/ptrack_database.dart"
      provides: "Schema v2 with migration and FK pragma"
      contains: "ptrackSupportedSchemaVersion = 2"
    - path: "packages/ptrack_data/lib/src/mappers/day_entry_mapper.dart"
      provides: "DayEntry row to domain mapping and insert/update companions"
      min_lines: 20
    - path: "packages/ptrack_data/test/fixtures/ptrack_v2.sqlite"
      provides: "Committed v2 fixture for future migration tests"
  key_links:
    - from: "ptrack_database.dart"
      to: "tables.dart DayEntries"
      via: "@DriftDatabase(tables: [Periods, DayEntries])"
      pattern: "DriftDatabase.*tables.*DayEntries"
    - from: "day_entry_mapper.dart"
      to: "logging_types.dart enums"
      via: "int-to-enum index conversion"
      pattern: "FlowIntensity\\.values"
    - from: "ptrack_database.dart onUpgrade"
      to: "dayEntries table"
      via: "m.createTable(dayEntries) inside from < 2 guard"
      pattern: "from < 2.*createTable"
---

<objective>
Create the data foundation for per-day symptom logging: domain enums for flow/pain/mood, a DayEntries Drift table linked to periods via FK, schema v1→v2 migration with FK enforcement, and bidirectional mappers.

Purpose: LOG-02, LOG-03, and LOG-06 require per-day granularity for flow intensity, pain, mood, and notes. This plan creates the schema and type infrastructure that all subsequent UI and repository work depends on.
Output: Domain enums, DayEntries table at schema v2, mappers, v2 fixture, migration and mapper tests.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-core-logging/04-CONTEXT.md
@.planning/phases/04-core-logging/04-RESEARCH.md
@packages/ptrack_domain/lib/src/period/period_models.dart
@packages/ptrack_domain/lib/ptrack_domain.dart
@packages/ptrack_data/lib/src/db/tables.dart
@packages/ptrack_data/lib/src/db/ptrack_database.dart
@packages/ptrack_data/lib/src/db/migrations.dart
@packages/ptrack_data/lib/src/mappers/period_mapper.dart
@packages/ptrack_data/lib/ptrack_data.dart
@packages/ptrack_data/test/migration_test.dart
@packages/ptrack_data/test/test_utils.dart
@packages/ptrack_data/tool/create_v1_fixture.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Domain enums and DayEntryData value type</name>
  <files>
    packages/ptrack_domain/lib/src/period/logging_types.dart
    packages/ptrack_domain/lib/ptrack_domain.dart
    packages/ptrack_domain/test/logging_types_test.dart
  </files>
  <action>
Create `packages/ptrack_domain/lib/src/period/logging_types.dart` with:

1. **`FlowIntensity` enum** with values `light`, `medium`, `heavy`. Add a `String get label` getter returning `'Light'`, `'Medium'`, `'Heavy'`. Add `static FlowIntensity fromDbValue(int v)` returning `values[v - 1]` and `int get dbValue` returning `index + 1` (1-indexed for DB storage, 0 is reserved for null).

2. **`PainScore` enum** with values `none`, `mild`, `moderate`, `severe`, `verySevere`. Add `String get label` returning `'None'`, `'Mild'`, `'Moderate'`, `'Severe'`, `'Very Severe'`. Same `fromDbValue`/`dbValue` pattern (1-indexed).

3. **`Mood` enum** with values `veryBad`, `bad`, `neutral`, `good`, `veryGood`. Add `String get label` returning `'Very Bad'`, `'Bad'`, `'Neutral'`, `'Good'`, `'Very Good'`. Add `String get emoji` returning `'😢'`, `'😟'`, `'😐'`, `'🙂'`, `'😄'`. Same `fromDbValue`/`dbValue` pattern.

4. **`DayEntryData`** — `@immutable` class with `final DateTime dateUtc`, `final FlowIntensity? flowIntensity`, `final PainScore? painScore`, `final Mood? mood`, `final String? notes`. Constructor with named required `dateUtc` and optional nullable fields. Override `==`, `hashCode`, `toString`. Import `package:meta/meta.dart` for `@immutable`.

Add `export 'src/period/logging_types.dart';` to `packages/ptrack_domain/lib/ptrack_domain.dart`.

Create `packages/ptrack_domain/test/logging_types_test.dart` testing:
- Each enum has the expected number of values (3, 5, 5)
- `fromDbValue` round-trips with `dbValue` for every value of each enum
- `fromDbValue(0)` throws `RangeError` for each enum
- `DayEntryData` equality: same fields are equal, different fields are not
- `DayEntryData` with all-null optional fields is valid
  </action>
  <verify>
    <automated>cd packages/ptrack_domain &amp;&amp; fvm dart test test/logging_types_test.dart -r expanded</automated>
  </verify>
  <done>FlowIntensity (3 values), PainScore (5 values), Mood (5 values) enums with label/dbValue/fromDbValue. DayEntryData value type. All exported from ptrack_domain barrel. Tests pass for round-trip conversions and equality.</done>
</task>

<task type="auto">
  <name>Task 2: DayEntries table, schema v2 migration, and mappers</name>
  <files>
    packages/ptrack_data/lib/src/db/tables.dart
    packages/ptrack_data/lib/src/db/ptrack_database.dart
    packages/ptrack_data/lib/src/db/ptrack_database.g.dart
    packages/ptrack_data/lib/src/mappers/day_entry_mapper.dart
    packages/ptrack_data/lib/ptrack_data.dart
    packages/ptrack_data/test/day_entry_mapper_test.dart
    packages/ptrack_data/test/migration_test.dart
    packages/ptrack_data/test/fixtures/ptrack_v2.sqlite
    packages/ptrack_data/tool/create_v2_fixture.dart
  </files>
  <action>
**tables.dart** — Add `DayEntries` table below the existing `Periods` class:
```dart
class DayEntries extends Table {
  IntColumn get id => integer().autoIncrement()();
  IntColumn get periodId => integer().references(Periods, #id)();
  DateTimeColumn get dateUtc => dateTime()();
  IntColumn get flowIntensity => integer().nullable()();
  IntColumn get painScore => integer().nullable()();
  IntColumn get mood => integer().nullable()();
  TextColumn get notes => text().nullable()();

  @override
  List<Set<Column>> get uniqueKeys => [{periodId, dateUtc}];
}
```

**ptrack_database.dart** — Three changes:
1. Bump `ptrackSupportedSchemaVersion` from `1` to `2`.
2. Change `@DriftDatabase(tables: [Periods])` to `@DriftDatabase(tables: [Periods, DayEntries])`.
3. Update `onUpgrade` body: inside the existing transaction, add `if (from < 2) { await m.createTable(dayEntries); }`.
4. Add `beforeOpen` callback to the `MigrationStrategy`: `beforeOpen: (details) async { await customStatement('PRAGMA foreign_keys = ON'); },`.

After editing, run `cd packages/ptrack_data && fvm dart run build_runner build --delete-conflicting-outputs` to regenerate `ptrack_database.g.dart`.

**day_entry_mapper.dart** — Create `packages/ptrack_data/lib/src/mappers/day_entry_mapper.dart`:
- `DayEntryData dayEntryRowToDomain(DayEntry row)` — converts nullable int columns to nullable enums via `row.flowIntensity != null ? FlowIntensity.fromDbValue(row.flowIntensity!) : null` (same for painScore → PainScore, mood → Mood). Maps `row.dateUtc` and `row.notes` directly.
- `DayEntriesCompanion dayEntryDataToInsertCompanion(int periodId, DayEntryData data)` — uses `DayEntriesCompanion.insert(periodId: periodId, dateUtc: data.dateUtc, flowIntensity: Value(data.flowIntensity?.dbValue), painScore: Value(data.painScore?.dbValue), mood: Value(data.mood?.dbValue), notes: Value(data.notes))`.
- `DayEntriesCompanion dayEntryDataToUpdateCompanion(DayEntryData data)` — uses `DayEntriesCompanion(dateUtc: Value(data.dateUtc), flowIntensity: Value(data.flowIntensity?.dbValue), painScore: Value(data.painScore?.dbValue), mood: Value(data.mood?.dbValue), notes: Value(data.notes))`.

Import from `package:drift/drift.dart`, `package:ptrack_domain/ptrack_domain.dart`, and `'../db/ptrack_database.dart'`.

**ptrack_data.dart** — Add exports: `export 'src/mappers/day_entry_mapper.dart' show dayEntryRowToDomain, dayEntryDataToInsertCompanion, dayEntryDataToUpdateCompanion;`

**day_entry_mapper_test.dart** — Test:
- `dayEntryRowToDomain` maps a row with all fields set to correct domain types
- `dayEntryRowToDomain` maps a row with all-null optional fields to DayEntryData with nulls
- `dayEntryDataToInsertCompanion` produces correct companion values including null → Value.absent() handling
- Round-trip: domain → companion → (conceptual row) preserves values

**migration_test.dart** — Add a new test:
- `'v1 fixture upgrades to v2 with DayEntries table and preserves periods'`: copy `test/fixtures/ptrack_v1.sqlite` to temp, open with `openPtrackDatabase`, verify `ptrackSupportedSchemaVersion` is 2, verify existing period rows survived, insert a day entry row referencing an existing period, verify it persists. Also verify FK enforcement: inserting a day entry with a non-existent `periodId` throws.

**v2 fixture** — Create `packages/ptrack_data/tool/create_v2_fixture.dart` following the pattern of `create_v1_fixture.dart`: opens a fresh in-memory or file DB at schema v2, inserts sample period rows and sample day entry rows, copies the file to `test/fixtures/ptrack_v2.sqlite`. Run it to generate the fixture.

After all changes: run `melos exec -- fvm dart analyze` in `packages/ptrack_data` and fix any issues.
  </action>
  <verify>
    <automated>cd packages/ptrack_data &amp;&amp; fvm dart run build_runner build --delete-conflicting-outputs &amp;&amp; fvm dart test test/migration_test.dart test/day_entry_mapper_test.dart -r expanded</automated>
  </verify>
  <done>DayEntries table exists in Drift schema v2. v1→v2 migration creates the table and preserves existing periods. PRAGMA foreign_keys = ON enforced in beforeOpen. day_entry_mapper converts rows ↔ DayEntryData with enum conversions. v2 fixture committed. All migration and mapper tests pass.</done>
</task>

</tasks>

<verification>
```bash
cd packages/ptrack_domain && fvm dart test && fvm dart analyze
cd packages/ptrack_data && fvm dart test && fvm dart analyze
```
All tests pass, no analysis errors. Schema version is 2. v1 fixture upgrades cleanly to v2.
</verification>

<success_criteria>
1. `ptrackSupportedSchemaVersion == 2` in ptrack_database.dart
2. `DayEntries` table has columns: id, periodId (FK→periods), dateUtc, flowIntensity?, painScore?, mood?, notes?
3. Unique constraint on (periodId, dateUtc)
4. PRAGMA foreign_keys = ON in beforeOpen
5. FlowIntensity (3), PainScore (5), Mood (5) enums with dbValue round-trip
6. All tests in both packages pass
7. v2 fixture file committed
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-logging/04-01-SUMMARY.md`
</output>
