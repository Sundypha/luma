---
phase: 03-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/ptrack/pubspec.yaml
  - apps/ptrack/lib/features/onboarding/onboarding_content.dart
  - apps/ptrack/lib/features/onboarding/onboarding_state.dart
  - apps/ptrack/lib/features/onboarding/onboarding_page.dart
  - apps/ptrack/lib/features/onboarding/onboarding_screen.dart
  - apps/ptrack/lib/features/onboarding/first_log_screen.dart
  - apps/ptrack/lib/main.dart
  - apps/ptrack/test/widget_test.dart
autonomous: true
requirements: [ONBD-01, ONBD-02, ONBD-03, ONBD-04]
must_haves:
  truths:
    - "User sees local-only data storage and no-account messaging on a required screen"
    - "User sees non-medical-advice estimates messaging on a required screen"
    - "User reaches first period-start logging within about one minute of starting onboarding"
    - "Required screens block swipe and show only Continue; optional screens also show Skip"
    - "Closing the app mid-onboarding and relaunching resumes at the correct step"
    - "No network calls during the entire onboarding flow"
  artifacts:
    - path: "apps/ptrack/lib/features/onboarding/onboarding_content.dart"
      provides: "Page data model and content definitions"
      contains: "OnboardingPageData"
    - path: "apps/ptrack/lib/features/onboarding/onboarding_state.dart"
      provides: "SharedPreferencesWithCache persistence wrapper"
      contains: "OnboardingState"
    - path: "apps/ptrack/lib/features/onboarding/onboarding_screen.dart"
      provides: "PageView wizard with required/optional gating and dot indicator"
      contains: "OnboardingScreen"
    - path: "apps/ptrack/lib/features/onboarding/first_log_screen.dart"
      provides: "Minimal first period-start entry and save"
      contains: "FirstLogScreen"
    - path: "apps/ptrack/lib/main.dart"
      provides: "Launch routing: onboarding → first-log → home"
      contains: "OnboardingState"
  key_links:
    - from: "apps/ptrack/lib/features/onboarding/onboarding_screen.dart"
      to: "onboarding_state.dart"
      via: "saveStep on page change, markCompleted on finish"
      pattern: "onboardingState\\.saveStep|onboardingState\\.markCompleted"
    - from: "apps/ptrack/lib/features/onboarding/first_log_screen.dart"
      to: "PeriodRepository"
      via: "insertPeriod call on save"
      pattern: "insertPeriod"
    - from: "apps/ptrack/lib/main.dart"
      to: "onboarding_state.dart"
      via: "isCompleted check for routing decision"
      pattern: "onboardingState\\.isCompleted"
---

<objective>
Build the complete first-run onboarding flow: a multi-screen wizard communicating local-only storage (ONBD-01) and non-medical estimates (ONBD-02), with required/optional gating (ONBD-04), step persistence for mid-session resume, a minimal first-period-log screen (ONBD-03), and app-launch routing.

Purpose: Gives new users the privacy and estimates context they need, then fast-tracks them to their first period log — all offline.
Output: Working onboarding → first-log → home flow, routed from main.dart.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-onboarding/03-CONTEXT.md
@.planning/phases/03-onboarding/03-RESEARCH.md
@apps/ptrack/lib/main.dart
@apps/ptrack/pubspec.yaml
@packages/ptrack_data/lib/src/repositories/period_repository.dart
@packages/ptrack_domain/lib/src/period/period_models.dart
@packages/ptrack_domain/lib/src/period/period_validation.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dependencies, create onboarding data model and state persistence</name>
  <files>
    apps/ptrack/pubspec.yaml
    apps/ptrack/lib/features/onboarding/onboarding_content.dart
    apps/ptrack/lib/features/onboarding/onboarding_state.dart
  </files>
  <action>
**pubspec.yaml changes:**
- Add `smooth_page_indicator: ^2.0.1` and `shared_preferences: ^2.5.5` under `dependencies` in `apps/ptrack/pubspec.yaml`.
- Move `timezone: ^0.10.1` from `dev_dependencies` to `dependencies` (main.dart needs `initializeTimeZones()` at runtime for PeriodCalendarContext used by PeriodRepository).
- Run `fvm flutter pub get` in `apps/ptrack`.

**onboarding_content.dart:**
- Create immutable `OnboardingPageData` class with fields: `title` (String), `body` (String), `icon` (IconData — placeholder for future illustrations per CONTEXT.md "light illustration or hero graphic"), `isRequired` (bool, default false).
- Define `const List<OnboardingPageData> onboardingPages` with 3 entries:
  1. Title: "Your data stays here" — Body: everything stored on this device, no account needed, no cloud, no sign-up. Icon: `Icons.phone_android`. `isRequired: true`. (Satisfies ONBD-01.)
  2. Title: "Estimates, not medical advice" — Body: predictions based on history you enter, personal estimates not diagnosis or substitute for professional medical advice. Icon: `Icons.lightbulb_outline`. `isRequired: true`. (Satisfies ONBD-02.)
  3. Title: "Ready to start" — Body: log your period start to get going, predictions improve with more history entered. Icon: `Icons.calendar_today`. `isRequired: false`. (Skippable per ONBD-04.)
- Tone: warm, conversational, reassuring, plain language per CONTEXT.md locked decisions. Medium copy length — short paragraph or a few bullets, scannable.

**onboarding_state.dart:**
- Create `OnboardingState` class wrapping `SharedPreferencesWithCache`.
- Preference keys: `onboarding_completed` (bool), `onboarding_current_step` (int).
- Synchronous getters (reads from cache): `bool get isCompleted` (default false), `int get currentStep` (default 0).
- Async mutators: `Future<void> saveStep(int step)`, `Future<void> markCompleted()` (sets completed=true, removes step key).
- Static async factory: `static Future<OnboardingState> create()` using `SharedPreferencesWithCache.create(cacheOptions: SharedPreferencesWithCacheOptions(allowList: {'onboarding_completed', 'onboarding_current_step'}))`.
  </action>
  <verify>
    <automated>cd apps/ptrack; fvm flutter pub get; fvm flutter analyze --no-fatal-infos</automated>
  </verify>
  <done>Dependencies resolve. OnboardingPageData and OnboardingState importable with no analyzer errors. Content list has 3 pages (2 required, 1 optional).</done>
</task>

<task type="auto">
  <name>Task 2: Build onboarding page layout and wizard screen</name>
  <files>
    apps/ptrack/lib/features/onboarding/onboarding_page.dart
    apps/ptrack/lib/features/onboarding/onboarding_screen.dart
  </files>
  <action>
**onboarding_page.dart:**
- Stateless `OnboardingPage` widget accepting `OnboardingPageData`.
- Layout: centered Column inside Padding (horizontal 32, vertical 24). Top: hero area — large Icon (size ~80) inside a circular decorated Container using `Theme.of(context).colorScheme.primaryContainer` background (placeholder for real illustrations — per CONTEXT.md "light illustration or hero graphic"). Below hero: SizedBox(height: 32). Title: Text with `Theme.of(context).textTheme.headlineMedium`, bold, centered. SizedBox(height: 16). Body: Text with `bodyLarge`, centered, muted secondary color. Keep the page Expanded-friendly so it fills the PageView slot.

**onboarding_screen.dart:**
- `OnboardingScreen` StatefulWidget. Constructor: `required OnboardingState onboardingState`, `required VoidCallback onComplete`.
- State: `late PageController _controller` initialized to `onboardingState.currentStep`. `int _currentPage` tracking current index. `bool _isAdvancing = false` (debounce guard per research pitfall #6).
- Dispose `_controller`.
- `_goToNext()`: If `_isAdvancing` return early (prevent rapid taps). Set `_isAdvancing = true`. If not last page: `await onboardingState.saveStep(_currentPage + 1)`, then `_controller.nextPage(duration: 300ms, curve: Curves.easeInOut)`. If last page: `await onboardingState.markCompleted()`, then `widget.onComplete()`. Set `_isAdvancing = false` in a finally block.
- Build: Scaffold > SafeArea > Column:
  - Expanded: `PageView.builder` with `_controller`, `itemCount: onboardingPages.length`.
    - `physics`: On required pages (`onboardingPages[_currentPage].isRequired == true`) use `NeverScrollableScrollPhysics()` to block swiping past without Continue. On optional pages use default (allow swipe). Note: physics must be reactive — rebuild when `_currentPage` changes.
    - `onPageChanged`: `setState(() => _currentPage = index)`.
    - `itemBuilder`: `OnboardingPage(data: onboardingPages[index])`.
  - Padding(all: 24) > Column:
    - `Semantics(label: 'Step ${_currentPage + 1} of ${onboardingPages.length}', child: SmoothPageIndicator(...))`. Use `ExpandingDotsEffect` with `dotHeight: 8, dotWidth: 8, expansionFactor: 3, spacing: 6`, `activeDotColor: Theme.of(context).colorScheme.primary`, `dotColor: Theme.of(context).colorScheme.outline.withValues(alpha: 0.3)`.
    - SizedBox(height: 24).
    - Controls row: If `isRequired`: full-width `FilledButton` with text "Continue" (or "Get Started" on last page). If optional: `Row` with `TextButton('Skip')` left + `Spacer` + `FilledButton('Continue'/'Get Started')` right. Both Skip and Continue call `_goToNext()`.
  </action>
  <verify>
    <automated>cd apps/ptrack; fvm flutter analyze --no-fatal-infos</automated>
  </verify>
  <done>OnboardingScreen renders 3 pages with animated dot indicator. Required pages block swipe and show Continue only. Optional pages show Skip + Continue. Step index persists via OnboardingState on each advance.</done>
</task>

<task type="auto">
  <name>Task 3: First-log screen, app routing, and existing test update</name>
  <files>
    apps/ptrack/lib/features/onboarding/first_log_screen.dart
    apps/ptrack/lib/main.dart
    apps/ptrack/test/widget_test.dart
  </files>
  <action>
**first_log_screen.dart:**
- `FirstLogScreen` StatefulWidget. Constructor: `required PeriodRepository repository`, `required VoidCallback onComplete`.
- State: `DateTime _selectedDate` initialized to today's local date. `bool _isSaving = false`.
- Build: Scaffold > AppBar(title: "Log your first period") > body: Padding > Column:
  - Text hint: "When did your current or most recent period start?" (`bodyLarge`, secondary color). Per CONTEXT.md: "One short inline hint on first logging screen."
  - SizedBox(height: 24).
  - Row or Card showing the selected date (formatted as locale-friendly string, e.g. "April 5, 2026") + `TextButton('Change date')` that opens `showDatePicker(firstDate: DateTime.now().subtract(Duration(days: 365)), lastDate: DateTime.now(), initialDate: _selectedDate)`. On date picked: `setState(() => _selectedDate = picked)`.
  - Spacer.
  - Full-width `FilledButton('Save & Continue')` — disabled when `_isSaving`.
- On save: `_isSaving = true`, `setState`. Create `PeriodSpan(startUtc: DateTime.utc(_selectedDate.year, _selectedDate.month, _selectedDate.day))` (no end). Call `await widget.repository.insertPeriod(span)`. Switch on result: `PeriodWriteSuccess` → show SnackBar "Period logged — you're all set!" then `widget.onComplete()`. `PeriodWriteRejected` → show SnackBar with "Could not save — please try a different date." and reset `_isSaving = false`. Handle unexpected outcomes defensively.

**main.dart rewrite:**
- Make `main()` async. `WidgetsFlutterBinding.ensureInitialized()`.
- `import 'package:timezone/data/latest.dart' as tz; tz.initializeTimeZones();` — required before any PeriodCalendarContext usage (Phase 2 decision).
- Create `OnboardingState` via `await OnboardingState.create()`.
- Open database via `final db = await openPtrackDatabase();` (from ptrack_data).
- Create `PeriodCalendarContext` for the device local timezone: `final calendar = PeriodCalendarContext.fromTimeZoneName(DateTime.now().timeZoneName);` — note: `DateTime.now().timeZoneName` may return abbreviations like "EST" on some platforms. If that fails, fall back to `'UTC'`. Alternatively, use `tz.local.name` after `initializeTimeZones()` if the timezone package exposes the device zone. Check if `tz.local` is set after init — if so use it; if not, default to UTC with a TODO for later improvement.
- Create `PeriodRepository(database: db, calendar: calendar)`.
- `runApp(PtrackApp(onboardingState: onboardingState, repository: repository))`.
- `PtrackApp` becomes a `StatefulWidget`. State holds a simple `_AppScreen` enum: `onboarding`, `firstLog`, `home`. Initialize based on `onboardingState.isCompleted` (true → `home`, false → `onboarding`).
- `MaterialApp` with `title: 'ptrack'`, same teal theme. `home:` switches on `_AppScreen`:
  - `onboarding` → `OnboardingScreen(onboardingState: ..., onComplete: () => setState(() => _screen = _AppScreen.firstLog))`.
  - `firstLog` → `FirstLogScreen(repository: ..., onComplete: () => setState(() => _screen = _AppScreen.home))`.
  - `home` → `HomePage()`.
- Keep `HomePage` as existing placeholder (later phases flesh it out).

**widget_test.dart update:**
- The existing test creates `PtrackApp()` with no args — it will break since the constructor now requires parameters. Update it:
  - Use `SharedPreferences.setMockInitialValues({'onboarding_completed': true})` so routing goes to HomePage.
  - Create a mock or in-memory DB if needed, or restructure the test to only verify the MaterialApp renders. Simplest: make PtrackApp accept an optional `Widget? homeOverride` for testing, or extract HomePage verification into a standalone test. Use `mocktail` to mock dependencies if necessary.
  - At minimum the test must pass `fvm flutter test`.
  </action>
  <verify>
    <automated>cd apps/ptrack; fvm flutter analyze --no-fatal-infos; fvm flutter test</automated>
  </verify>
  <done>App launches → shows onboarding wizard. After completing wizard → shows first-log screen with today's date. After saving → shows home. On relaunch with completed onboarding → goes straight to home. All existing tests pass.</done>
</task>

</tasks>

<verification>
- `melos run ci:analyze` — no analyzer errors across workspace
- `melos run ci:test` — all tests pass (apps/ptrack, ptrack_domain, ptrack_data)
- Manual: launch app → see privacy screen → Continue → see estimates screen → Continue → see optional screen → Skip or Continue → see first-log → save → see home
</verification>

<success_criteria>
- Onboarding shows 3 screens in order: local-first privacy (required), estimates (required), quick-start (optional)
- Required screens show Continue only; optional shows Skip + Continue
- Dot indicator animates between pages
- Closing and reopening mid-onboarding resumes at saved step
- First-log screen defaults to today, allows date change, saves via PeriodRepository
- Post-onboarding launches go straight to home
- No network calls in the entire flow (ONBD-04)
- All analyzer and test checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-onboarding/03-01-SUMMARY.md`
</output>
