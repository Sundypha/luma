---
phase: 03-onboarding
plan: 03
type: execute
gap_closure: true
wave: 1
depends_on: [03-02]
files_modified:
  - .planning/phases/03-onboarding/03-UAT.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [ONBD-01, ONBD-02, ONBD-03, ONBD-04]
must_haves:
  truths:
    - "03-UAT.md documents current onboarding UX: swipe advances all steps; final optional step has no redundant Skip; first log supports optional end date for ended periods"
    - "UAT gap entries from 03-UAT are marked resolved with pointer to implementation commit or file paths"
    - "melos run ci:analyze and melos run ci:test pass after reconciliation"
  artifacts:
    - path: ".planning/phases/03-onboarding/03-UAT.md"
      provides: "Reconciled UAT expectations and resolved gap records"
    - path: ".planning/phases/03-onboarding/03-03-SUMMARY.md"
      provides: "Gap-closure execution summary"
---

<objective>
Formalize closure of Phase 3 UAT gaps (swipe vs Continue, redundant Skip on last wizard step, first-log end date). Implementation already landed on branch (see `03-UAT.md` “Gap closure”); this plan reconciles planning artifacts and proves CI still green.

Purpose: Gives `/gsd-execute-phase 3 --gaps-only` a real `gap_closure` target and leaves traceability for auditors.
Output: Updated `03-UAT.md`, `03-03-SUMMARY.md`, STATE/ROADMAP touch-up if needed.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-onboarding/03-UAT.md
@.planning/phases/03-onboarding/03-CONTEXT.md
@apps/ptrack/lib/features/onboarding/onboarding_screen.dart
@apps/ptrack/lib/features/onboarding/first_log_screen.dart
@apps/ptrack/test/features/onboarding/onboarding_screen_test.dart
@apps/ptrack/test/features/onboarding/first_log_screen_test.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile 03-UAT.md with shipped gap fixes</name>
  <files>
    .planning/phases/03-onboarding/03-UAT.md
  </files>
  <action>
**Tests section (expected text — align with code, not old plan):**

- **Test 1:** Expected should state that the privacy screen still shows local-only messaging and **Continue** only (no Skip), and that **horizontal swipe** can advance to the next step (same effect as Continue for forward navigation). Remove any wording that says swipe does *not* advance on required pages.

- **Test 3:** Expected should state the third screen still carries optional quick-start copy, but the **last** step shows **Get Started** (or Continue label on last page) **only** — **no Skip** on that final page.

- **Test 4:** Update to expect optional **“This period has already ended”** with **Change end date**, and that save can persist a **closed** `PeriodSpan` (`endUtc` set) when the toggle is on. Set `result` to **pass** if you accept automated coverage (`first_log_screen_test.dart`) as sufficient; otherwise leave a short `notes` that manual spot-check is recommended.

**Summary section:** Set `issues: 0` if Test 4 is marked pass; keep `feedback_logged` or zero it if no longer relevant.

**Gaps section:** For each of the three YAML gap entries (Skip, swipe, first-log end date), set `status: resolved` and add fields:
- `resolution: "..."` (one line: what shipped, e.g. commit `37471f7` or file paths)
- Clear `root_cause` / `missing` if superseded, or prefix with `resolved —`

**Gap closure (implemented in repo)** subsection: Either fold into resolved gaps or shorten to a single line pointing at this plan and `03-03-SUMMARY.md` after execution.

**Frontmatter `updated`:** ISO timestamp for this edit.
  </action>
  <verify>
    <automated>Manual review: no contradictory “swipe blocked” vs code in onboarding_screen.dart</automated>
  </verify>
  <done>UAT reads consistently with apps/ptrack onboarding + first log; gaps marked resolved.</done>
</task>

<task type="auto">
  <name>Task 2: CI verification</name>
  <files>
    [none unless fixes required]
  </files>
  <action>
Run from repo root: `melos run ci:analyze` and `melos run ci:test`. If anything fails, fix minimal drift (prefer test or copy fixes over feature changes unless blocker).
  </action>
  <verify>
    <automated>melos run ci:analyze; melos run ci:test</automated>
  </verify>
  <done>Workspace analyze and test green.</done>
</task>

<task type="auto">
  <name>Task 3: Summary and tracking files</name>
  <files>
    .planning/phases/03-onboarding/03-03-SUMMARY.md
    .planning/STATE.md
    .planning/ROADMAP.md
  </files>
  <action>
- Create `03-03-SUMMARY.md` per GSD summary template: gap_closure plan, tasks run, key files touched, `requirements-completed` as appropriate (reference ONBD-* if unchanged functionally).

- Update **STATE.md**: note Phase 3 gap plan `03-03` complete; adjust **Current Plan** / progress if your convention marks gap closure as extra plan count (e.g. Total Plans 3).

- Update **ROADMAP.md** for Phase 3 if a row exists for plan progress — use `node ~/.cursor/get-shit-done/bin/gsd-tools.cjs roadmap update-plan-progress 3` when applicable, or a minimal manual checkbox note for `03-03-PLAN.md`.

Commit each logical chunk with conventional commits (docs/test/fix as appropriate), or one `docs(03-onboarding):` commit if your repo batches planning updates.
  </action>
  <verify>
    <automated>Files exist; STATE mentions 03-03 closure</automated>
  </verify>
  <done>03-03-SUMMARY.md present; STATE/ROADMAP reflect gap closure.</done>
</task>

</tasks>

<verification>
- `melos run ci:analyze` — clean
- `melos run ci:test` — all packages with tests pass
- `03-UAT.md` has no stale “swipe does not advance” expectation on Test 1
</verification>

<success_criteria>
- UAT gaps resolved in documentation with clear resolution pointers
- CI passes
- SUMMARY and state files updated for plan 03-03
</success_criteria>

<output>
After completion, create `.planning/phases/03-onboarding/03-03-SUMMARY.md`
</output>
