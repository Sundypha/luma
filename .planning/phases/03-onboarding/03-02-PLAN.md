---
phase: 03-onboarding
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - apps/ptrack/lib/features/settings/about_screen.dart
  - apps/ptrack/lib/main.dart
  - apps/ptrack/test/features/onboarding/onboarding_state_test.dart
  - apps/ptrack/test/features/onboarding/onboarding_screen_test.dart
  - apps/ptrack/test/features/onboarding/first_log_screen_test.dart
autonomous: false
requirements: [ONBD-04]
must_haves:
  truths:
    - "User can replay privacy and estimates content from the home screen"
    - "Widget tests verify required screen gating, skip on optional, state persistence, first-log save, and content display"
  artifacts:
    - path: "apps/ptrack/lib/features/settings/about_screen.dart"
      provides: "Replay of privacy and estimates disclosures"
      contains: "AboutScreen"
    - path: "apps/ptrack/test/features/onboarding/onboarding_screen_test.dart"
      provides: "Wizard flow widget tests"
      min_lines: 40
    - path: "apps/ptrack/test/features/onboarding/first_log_screen_test.dart"
      provides: "First-log interaction tests"
      min_lines: 30
  key_links:
    - from: "apps/ptrack/lib/features/settings/about_screen.dart"
      to: "onboarding_content.dart"
      via: "imports onboardingPages for content reuse"
      pattern: "import.*onboarding_content"
    - from: "apps/ptrack/lib/main.dart"
      to: "about_screen.dart"
      via: "HomePage navigates to AboutScreen"
      pattern: "AboutScreen"
---

<objective>
Add a Settings/About screen for replaying privacy and estimates content (CONTEXT.md locked decision: "Replay from Settings → About"), then write comprehensive widget tests verifying the entire onboarding flow from Plan 01.

Purpose: Completes the skip-and-replay story (users who skip optional screens can revisit disclosures) and proves all onboarding requirements via automated tests.
Output: AboutScreen accessible from home, full widget test coverage for onboarding.
</objective>

<execution_context>
@~/.cursor/get-shit-done/workflows/execute-plan.md
@~/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-onboarding/03-CONTEXT.md
@.planning/phases/03-onboarding/03-RESEARCH.md
@.planning/phases/03-onboarding/03-01-SUMMARY.md
@apps/ptrack/lib/features/onboarding/onboarding_content.dart
@apps/ptrack/lib/features/onboarding/onboarding_state.dart
@apps/ptrack/lib/features/onboarding/onboarding_screen.dart
@apps/ptrack/lib/features/onboarding/first_log_screen.dart
@apps/ptrack/lib/main.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: About screen for disclosure replay and widget tests</name>
  <files>
    apps/ptrack/lib/features/settings/about_screen.dart
    apps/ptrack/lib/main.dart
    apps/ptrack/test/features/onboarding/onboarding_state_test.dart
    apps/ptrack/test/features/onboarding/onboarding_screen_test.dart
    apps/ptrack/test/features/onboarding/first_log_screen_test.dart
  </files>
  <action>
**about_screen.dart:**
- `AboutScreen` StatelessWidget.
- Build: Scaffold > AppBar(title: "About ptrack") > body: `ListView` with:
  - A leading section header: "Your privacy & how estimates work" (or similar warm phrasing).
  - For each entry in `onboardingPages` where the content is informational (index 0 = privacy, index 1 = estimates — filter out the "Ready to start" page): render a `Card` with `ListTile`-style layout showing the icon in a small CircleAvatar, title in bold, body text below. Reuse data from `onboardingPages` in `onboarding_content.dart`.
  - Keep it simple — a scrollable list, no PageView, no dots, no buttons. Just readable content.
- Import `onboarding_content.dart` for data reuse.

**main.dart update (HomePage):**
- Add an `IconButton` (or `TextButton`) in `HomePage`'s AppBar actions that navigates to `AboutScreen` via `Navigator.push`. Use an info icon (`Icons.info_outline`) with tooltip "About". This provides the CONTEXT.md required replay entry point; it will move to a proper Settings screen in later phases.

**onboarding_state_test.dart:**
- Setup: `SharedPreferences.setMockInitialValues({})` before each test.
- Test: `isCompleted` defaults to false on fresh state.
- Test: `currentStep` defaults to 0 on fresh state.
- Test: `saveStep(2)` → `currentStep` returns 2.
- Test: `markCompleted()` → `isCompleted` is true, `currentStep` resets to 0.
- Test: round-trip — save step 1, create new `OnboardingState` from same prefs → step is 1 (verifies persistence, not just in-memory).

**onboarding_screen_test.dart:**
- Use `SharedPreferences.setMockInitialValues({})` in setUp. Create a real or test-double `OnboardingState`.
- Test: first page shows the privacy/local-first title text (verifies ONBD-01 content).
- Test: second page shows the estimates/not-medical-advice title text (verifies ONBD-02 content).
- Test: on a required page, "Skip" button is NOT present; "Continue" IS present.
- Test: on an optional page (advance to page 3 via the controller or tapping Continue twice), both "Skip" and "Continue" are present.
- Test: swiping right (drag gesture) on a required page does not change the current page (NeverScrollableScrollPhysics blocks it).
- Test: tapping "Continue" on the last page calls the `onComplete` callback.
- Test: `SmoothPageIndicator` (or its Semantics wrapper) is present with correct step count.
- Pump with `MaterialApp(home: OnboardingScreen(...))`. Use `mocktail` if mocking OnboardingState, or construct a real one from mock SharedPreferences.

**first_log_screen_test.dart:**
- Create `class MockPeriodRepository extends Mock implements PeriodRepository {}` using mocktail.
- Test: screen shows the inline hint text ("When did your current or most recent period start?" or substring).
- Test: default selected date label shows today's date.
- Test: tapping "Save & Continue" calls `repository.insertPeriod` with a PeriodSpan whose startUtc matches the selected date. Stub `insertPeriod` to return `PeriodWriteSuccess(1)`. Verify `onComplete` callback fires.
- Test: when `insertPeriod` returns `PeriodWriteRejected`, a SnackBar with error text appears and `onComplete` is NOT called.
- Pump with `MaterialApp(home: FirstLogScreen(repository: mockRepo, onComplete: callback))`.
  </action>
  <verify>
    <automated>cd apps/ptrack; fvm flutter analyze --no-fatal-infos; fvm flutter test</automated>
  </verify>
  <done>AboutScreen renders privacy and estimates content, reachable from HomePage. All widget tests pass: onboarding state persistence, wizard gating behavior, first-log save interaction, content display for ONBD-01 and ONBD-02.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete onboarding flow</name>
  <action>
Complete onboarding flow has been built: 3-screen wizard with step dots, required/optional gating, state persistence for resume, first period-log screen, app routing, and About replay screen. Verify the full experience end-to-end.
  </action>
  <verify>
    <manual>
    1. Kill the app if running. Launch fresh: `cd apps/ptrack &amp;&amp; fvm flutter run`
    2. **Privacy screen (required):** Verify it shows local-only storage messaging. Confirm there is NO "Skip" button — only "Continue". Try swiping right — it should NOT advance.
    3. Tap "Continue". **Estimates screen (required):** Verify it shows non-medical-advice messaging. Confirm no "Skip", only "Continue". Dot indicator should show step 2 of 3.
    4. Tap "Continue". **Quick-start screen (optional):** Verify it shows both "Skip" and "Continue".
    5. Tap "Skip" or "Continue". **First-log screen:** Verify it shows today's date by default and a hint about logging period start. Tap "Change date" and pick a past date. Tap "Save &amp; Continue."
    6. **Home screen:** Verify the app shows the home page. Tap the info/about icon in the AppBar. **About screen:** Verify privacy and estimates content is displayed.
    7. **Resume test:** Relaunch the app — it should go straight to home (onboarding already completed).
    8. **Resume mid-onboarding:** Clear app data or uninstall/reinstall. Start onboarding, advance to screen 2, then kill the app (not just background — force stop). Relaunch — it should resume on screen 2.
    9. **Offline:** Enable airplane mode before launching. Verify the entire flow works without errors.
    </manual>
  </verify>
  <done>User approves the visual and functional onboarding experience. All screens render correctly, gating works, resume works, first-log saves, About replay accessible.</done>
</task>

</tasks>

<verification>
- `melos run ci:analyze` — clean across workspace
- `melos run ci:test` — all tests pass (apps/ptrack, ptrack_domain, ptrack_data)
- Manual: full flow per checkpoint steps above
</verification>

<success_criteria>
- AboutScreen renders privacy and estimates content reusing onboarding data model
- AboutScreen reachable from HomePage
- Widget tests cover: state defaults, persistence round-trip, wizard content (ONBD-01, ONBD-02), required gating, optional skip, page indicator, first-log save, error handling
- Human verification confirms visual flow, gating, resume, and offline operation
- All analyzer and test checks pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-onboarding/03-02-SUMMARY.md`
</output>
